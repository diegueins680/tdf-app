# Test execution report

## Environment and isolation

- Repository baseline: detached `origin/main` commit `560ac9954b655ba8aa719963a2c890110b74a232` in an isolated worktree; unrelated changes in the primary checkout were not touched.
- Mobile submodule: `e1f9707eb130ec24892b544e0f470d254d4b2176`.
- Host: macOS 14.7.7 (23H723), Node 24.8.0, npm 11.6.0, Stack 3.7.1, GHC 9.10.3, America/Guayaquil.
- Data/providers: isolated route mocks, disposable in-memory SQLite schemas, and one local PostgreSQL 16 cluster under `/private/tmp` on a loopback-only socket/port. The compiled backend ran only against that disposable database; the cluster and token-bearing cookie files were removed after execution. No staging/production, payment, notification, OAuth, social, WhatsApp, ticket delivery or distribution provider was contacted.
- Browser evidence: ignored `artifacts/persona-playwright/` locally; CI upload retention is 14 days. Failure-only screenshot/video/trace policy; JSON axe attachments are sanitized.

## Runs

### RUN-001

`npm ci` — passed. Installed 1,561 packages. npm reported 6 moderate and 6 high audit findings; these counts are signals pending advisory-level reachability review, not a claim of exploitable production vulnerabilities.

### RUN-002

`npm --prefix tdf-mobile ci` — passed. Installed 1,402 packages. npm reported 4 moderate and 35 high audit findings; see PT-007.

### RUN-003

`npm run quality:repo` on the unchanged baseline — passed. Formal audit reported 8,676 findings, 0 errors and 307 warnings. Continuous-improvement, formal-method, production-release, CI-pipeline and visual-artifact Node tests passed.

### RUN-004

`npm run quality:ui` on the unchanged baseline — passed with warnings. ESLint had 0 errors/93 warnings; TypeScript passed; Jest passed 162 suites/1,651 tests; Vite production build passed with existing bundle-size warnings.

### RUN-005

`REQUIRE_MOBILE_WORKSPACE=1 npm run quality:mobile` on the unchanged baseline — failed after lint/typecheck passed. Jest reported 47 suites passed and 2 failed: the feature-registry test found seven nonexistent native destinations (PT-001), and one TicketCheckout test exceeded the default 5-second timeout (PT-006).

### RUN-006

`npm --prefix tdf-mobile test -- --runInBand __tests__/TicketCheckout.test.tsx` — passed 14/14. The previously timed case completed in 806 ms. No retries or enlarged timeout were introduced.

### RUN-007

`REQUIRE_STACK=1 npm run quality:backend` — **blocked before backend compilation**. Stack attempted the required GHC 9.10.3 installation when the host had about 1.3 GiB free; archive unpack failed for lack of space. Only the incomplete worktree-local `.stack-root` generated by this run was removed. A targeted `hlint tdf-hq/src/TDF/Seed.hs` completed with seven existing hints outside the added persona-seed block and an exit status of 1. No Haskell compilation or tests are claimed for RUN-007; the later successful retry is RUN-015.

### RUN-008

`npm run generate:features && npm run audit:features` — passed after PT-001 fix: 137 features, 156 actual web routes and 38 mobile routes.

### RUN-009

`ALLOW_PENDING_FEATURE_DISPOSITIONS=1 npm run generate:feature-audit-reports` — evidence generation passed: 137 features; 44 primary, 57 submodule, 26 contextual and 10 technical records; 35 native features; 11 role fixtures; 3,014 matrix rows; 548 expanded endpoints, 493 mapped and 40 pending explicit disposition. The normal command intentionally fails while those pending rows remain, so this is `passed-with-gate`, not full audit success.

### RUN-010

`npm run test:e2e:web` — passed in 1.3 minutes: 15 passed and 2 expected skips. Chromium desktop/phone/tablet ran protected login, UI signup, public search and guest-ticket offer/hold; Firefox and WebKit ran the `@critical` auth case. The phone project alone runs the 320-CSS-pixel reflow test, so desktop/tablet skip it by design. Playwright has zero retries.

#### PW-PER-01-AUTH

Verified: protected `/crm/contactos` redirects to login with encoded intent; rejected fictional credentials show a visible recovery alert; the real signup dialog submits a reserved fictional identity to an isolated `/signup` mock and enters the fan surface; one main landmark; malformed optional catalog response does not blank the shell; serious/critical axe result `[]`; zero unexpected console errors and zero user-relevant failed requests. Not verified: backend registration mutation, verification inbox, session expiration and password-reset lifecycle.

#### PW-PER-01-DISCOVERY

Verified: isolated Quito city/search result, query/city URL state, detail navigation, back-state restoration, phone/tablet/desktop presentation, 320 CSS-pixel reflow with no document overflow, one main landmark, serious/critical axe result `[]`, zero unexpected console errors and zero user-relevant failed requests. Not verified: live index/database, profession/genre/event/venue breadth or timing under real network throttling.

#### PW-PER-01-TICKET-OFFER

Verified against isolated storefront/checkout responses: a public guest sees the fictional tier price, enters reserved fictional contact data, accepts versioned terms, sends a generated idempotency key, receives a capability-scoped order route, sees the authoritative total and is explicitly told the hold is unpaid and unissued with no provider enabled. The covered state has zero serious/critical axe violations. No charge, ticket or message was created. Sold-out, stale-capacity and back-navigation cases remain.

WebKit on macOS 14 emitted Playwright's upstream frozen-build warning. Optional rate/catalog reads cancelled by navigation are filtered only for browser-native `ERR_ABORTED`/`cancelled`; all other request failures remain test failures.

### RUN-011

`REQUIRE_MOBILE_WORKSPACE=1 npm run quality:mobile` after registry fix — passed. ESLint and TypeScript passed. Jest passed 49 suites/256 tests in 33.153 seconds; the initial TicketCheckout timeout did not recur. After adding stable persona identifiers to the test descriptions, a focused rerun passed 2 suites/18 tests in 5.41 seconds.

#### MOB-PER-02-TICKET-IDEMPOTENCY

The component/storage contract verifies cancelled PaymentSheet release and message, ambiguous results remain pending with “do not pay again” guidance, success retains its key until terminal state, lost create responses reuse the same key, only the exact closed-checkout contract rotates once, repeated conflicts stop after one retry, changed buyer details produce a new key, and persisted storage keys contain no buyer fingerprint/email. Backend webhook authority and provider reconciliation were not executed.

### RUN-012

`npm run quality:repo` after implementation — passed. Formal audit remained at 8,676 findings, 0 errors and 307 warnings. All repository Node suites passed, including validation of 26 personas, 78 scenarios, 17 epics and 79-line traceability CSV. After adding the persona E2E job assertion, the focused CI-pipeline suite also passed 16/16.

### RUN-013

`npm run quality:ui` after implementation — passed. ESLint initially reported the baseline 93 warnings plus one new memo-dependency warning; that new warning was corrected and a final lint run returned to 93 warnings/0 errors. TypeScript passed. Jest passed 162 suites/1,652 tests in 333.255 seconds. The production build and initial bundle budget passed; Vite retained its existing chunk-size warning. After the memoization-only lint correction, `npm run typecheck:ui` and the focused theme/locale/directory run passed 3 suites/7 tests.

### RUN-014

`REQUIRE_STACK=1 npm run quality:backend` after disk capacity was restored — failed during the first cold source build. Stack downloaded and installed GHC 9.10.3, compiled the dependency graph and reached `TDF.Seed`, where both explicit persona catalog `FromJSON` instances failed because the import did not expose the `parseJSON` class method. Tests did not execute in this attempt. The import was corrected without changing decoder behavior.

### RUN-015

`REQUIRE_STACK=1 npm run quality:backend` after the decoder-import fix — passed, then passed again after removing a new name-shadowing warning. GHC 9.10.3 built and linked `tdf-hq-exe` and the 180-module `tdf-hq-test` target. Hspec finished in 3.7565 seconds with **2,419 examples and 0 failures**, including the two `syntheticPersonaSeedingAllowed`/password guard examples. The command exited 0. Existing Stack/Cabal warnings remain: `allow-newer` selects `http2-5.3.10` outside the declared `<5.3` bound, and the test stanza omits several compiled modules from `other-modules`. No PostgreSQL service, external provider or network-facing backend journey was exercised.

### RUN-016

`hlint tdf-hq/src/TDF/Seed.hs` — exit 1 with seven hints. They are existing redundant-`void`/`pure` and `isJust`/`fromMaybe` suggestions outside the added synthetic-persona block; no new hint targets that block. They were not mixed into this scoped change.

### RUN-017

`ormolu --mode check tdf-hq/src/TDF/Seed.hs tdf-hq/test/Spec.hs` — not clean. The installed formatter proposed an extensive whole-file rewrite of legacy formatting beginning with `Seed.hs`, so the changed-file set is not formatter-clean. A redundant output-suppressed confirmation remained active for more than four minutes and was terminated with exit 130. No sweeping mechanical formatting change was applied, and formatting is not claimed as passed.

### RUN-018

Disposable PostgreSQL 16 bootstrap and persona seed lifecycle — passed. The compiled backend ran with `APP_ENV=test`, migrations and the explicit synthetic-persona gate enabled, a runtime-only password, external workers disabled, and no provider credentials. Health reached `{"db":"ok","status":"ok"}`. Fixture-to-database verification found 25 account-bearing personas, 25 parties, 25 active bcrypt credentials, 47 active canonical role assignments across 21 role codes, zero duplicate reserved emails and zero pre-login persona API tokens. A second no-reset boot produced the exact same counts, demonstrating count- and relationship-level idempotency. The 26th persona is intentionally an unauthenticated visitor. The schema created by this boot had no `directory_profile` relation; the seed itself creates no public-directory projection.

### RUN-019

`E2E-PER-02-PROFILE` authentication/logout slice before the fix — failed by direct local observation. PER-02 authenticated successfully and `/session` returned only the expected Customer/Fan roles and Packages module. Logout returned 200 and expired the client cookie, but replaying a preserved pre-logout cookie still resolved the session; the matching server token remained active. The token and runtime-only password were not copied into committed evidence. This confirmed PT-011.

### RUN-020

Focused logout regression plus repeated PostgreSQL journey — passed. `stack test tdf-hq --test-arguments='--match sessionServer'` passed 2 examples/0 failures, including deactivation of two distinct valid tokens presented simultaneously through Authorization and Cookie headers. From a zero-active-token PER-02 precondition, the repaired live backend accepted login, exposed exactly one active token, returned 200 for logout, returned `null` when the preserved stale cookie was replayed, and left zero active PER-02 tokens. No unrelated account or external system was involved.

### RUN-021

`npm run generate:api` in both `tdf-hq-ui` and `tdf-mobile` — passed. The OpenAPI contract now describes logout revocation through bearer authentication or the `tdf_session` cookie, preserves anonymous/idempotent logout, and regenerated both client type files from the same source.

### RUN-022

`REQUIRE_STACK=1 npm run quality:backend` on the final repaired backend and embedded OpenAPI — passed. GHC 9.10.3 built/linked the executable and test target; Hspec finished in 8.2138 seconds with **2,420 examples and 0 failures**. Existing warnings remain for `allow-newer` selecting `http2-5.3.10` beyond the declared `<5.3` bound and Cabal test modules missing from `other-modules`.

### RUN-023

Final contract, generated-data and repository checks — passed: `REQUIRE_MOBILE_WORKSPACE=1 npm run generate:api`; `npm run typecheck:ui`; `npm --prefix tdf-mobile run typecheck`; `npm run generate:persona-program`; `npm run test:persona-program`; and `npm run quality:repo`. Both generated clients were current, both TypeScript projects passed, persona validation passed for 26 personas/78 stories/17 epics and all 3 program tests, and repository quality retained 8,676 formal findings with 0 errors/307 warnings plus all Node suites green.

### RUN-024

`hlint src/TDF/API.hs src/TDF/ServerAuth.hs` — exit 1 with 30 pre-existing hints; none targets the changed `SessionAPI` route or logout handler. As with RUN-016/017, no unrelated broad cleanup was mixed into the security fix.

### RUN-025

Final integrity and baseline checks — passed: `git diff --check`; JSON parse of personas, blueprints, generated scenarios and execution results; exact 79-line traceability CSV assertion (header plus 78 stories); and remote read-only verification that `refs/heads/main` still equals `560ac9954b655ba8aa719963a2c890110b74a232`.

### RUN-026

Draft-PR CI diagnosis and regression verification — passed after two scoped fixes. The first hosted run passed UI, mobile, repository, API, migration, build and preview checks, while `hardcoded-list-audit` rejected four unreviewed fingerprints plus one stale mobile fingerprint and `persona-web-e2e` reported 14 passes, 2 expected skips and one WebKit-only optional-request cancellation (`Load request cancelled`). No application assertion, accessibility result, console error or genuine network failure failed in that WebKit case.

The reviewed decision registry now classifies the three persona-program validation/documentation constants as technical constants, restores the generated mobile feature registry as a security/system-registry mirror, and removes its stale predecessor. `npm run test:catalog-list-audit` passed 1/1; `npm run audit:catalog-lists` passed with 871 candidates, zero unreviewed candidates and zero stale decisions; and both committed catalog inventory formats were regenerated. The request observer now ignores only the three browser-native cancellation strings `net::ERR_ABORTED`, `cancelled` and WebKit's `Load request cancelled`; every other failed request remains fatal.

`npx playwright test e2e/web/persona-public.spec.mjs --project=webkit-critical --repeat-each=3` passed three consecutive critical WebKit runs with zero retries. A later full run against the development server produced 10 passes, 2 expected skips and 5 local timeouts because Vite had not finished transforming the large module graph; trace evidence showed both mocked 401 session responses arrived while later modules remained unfinished. This diagnostic run is not reported as a pass. `npm run build --workspace=tdf-hq-ui` then passed type checking, production bundling and the initial bundle budget. Against that freshly built local preview, `npx playwright test` passed 15 tests with 2 expected skips in 3.9 minutes across Chromium desktop/phone/tablet plus Firefox and WebKit critical coverage, still with zero retries. Final `npm run quality:repo` passed with all repository Node suites and the unchanged formal-audit baseline of 8,676 findings, 0 errors and 307 warnings.

## Exploratory defect discovery

During the first instrumented browser runs, an intentionally malformed 200 catalog response reproduced a blank app shell and console exception (PT-002). After recovery guards, axe exposed serious dark-mode contrast failures (PT-003). DOM/landmark inspection then exposed nested/duplicate mains on shared public-shell pages (PT-009). The later PostgreSQL-backed PER-02 journey exposed logout token replay (PT-011). Each received a regression assertion; the final browser/backend results above are post-fix evidence.

## Not executed and why

- PostgreSQL breadth: persona seeding, login/session/logout and token-state verification ran locally; the remaining domain lifecycles, audit/outbox effects, ownership negatives and role changes were not exercised against PostgreSQL.
- Migration rollback and standalone runtime scripts: the backend's forward Persistent/catalog/trials migration path completed repeatedly, but rollback and the repository's separate domain migration scripts were not run in this program.
- OpenAPI breadth: the logout contract and both generated clients were refreshed; full endpoint/schema conformance beyond that changed operation remains a separate baseline gate.
- Detox iOS: requires the configured simulator identifier, native app build and isolated auth/API environment; not available in this worktree run.
- Detox Android: no repository configuration exists.
- Provider/payment/refund/reconciliation, email/notification/OAuth/social/WhatsApp/ticket delivery/distribution: require local fakes or authorized sandboxes and credentials; none were invoked.
- Human keyboard/screen-reader/usability sessions: no participants were recruited; automated axe is not a substitute.
- Staging/production: intentionally untouched.

The authoritative machine-readable record is [execution-results.json](execution-results.json). Unlisted story rows in [traceability.csv](traceability.csv) remain `specified-not-executed`; that status is not a failure concealment or implied pass.
