# Identity consolidation status — 2026-09-18

PR #426 is merged and deployed as `a81e48ea9e68b495562ae1f6592654c043dc97eb`.
Production cleanup completed two confirmed merge groups, archived three redundant records and
linked one complementary group. Fourteen groups remain blocked for field, authentication or
relationship review. Comprehensive entry-point prevention is still in progress.

| Measure | Verified result |
| --- | ---: |
| Party records examined | 241 |
| Credential records examined (not unique people) | 99 |
| Core artist profiles | 53 |
| Directory profiles | 3 |
| Artist inventory source references | 425 |
| Social synchronization accounts | 108 |
| Declared foreign keys referencing Party | 216 |
| Candidate groups under exact-attribute hints | 17 |
| Duplicate groups attested by the repository owner | 16 |
| Complementary groups explicitly retained for linking | 1 |
| Production merges / archived records | 2 / 3 |
| Active contacts verified through paginated production API | 238 |
| Active complementary links | 1 |
| Groups awaiting identity classification | 0 |
| Attested groups still awaiting safety/dependency resolution | 14 |
| Applied merge operations / failed merge operations | 2 / 0 |

Source identifiers were checked within their issuing source type. No source-type/source-record
pair mapped to multiple Party IDs. Matching attributes and near-identical rows did not establish
identity. Subsequent direct owner attestations establish whole-group identity for 16 duplicate
groups (10 named people, 4 bands, 2 test/reviewer account groups), and one complementary person/contact/account group. One attestation
expands an existing pair to include its established canonical Party. These attestations do not
bypass authentication, ownership, field or dependency blockers. Multiple credentials and
person/organization boundaries remain explicit blockers.
Names, handles, email, phone numbers and private member mappings are excluded from this report.

The deployment observed at inventory time was backend commit
`9f0da14de84919f8fba391fa752b7df7412a600d`; production and staging health endpoints returned database
OK. GitHub/Fly/database access worked. Main requires one independent approving review with stale
approvals dismissed. Unrelated dirty checkout and mobile edits were preserved in place.

Validation completed before production cleanup:

- Nine candidate-classifier tests, including 300 generated permutation/nontransitivity cases.
- Synthetic PostgreSQL migration application and rerun, contact request concurrency, access
  restrictions, blocked sensitive dependencies, merge retry, archive write protection and undo.
- Staging host: isolated `tdf_identity_rehearsal_20260917` migration and integration tests passed.
- Staging synthetic backup restored to a separate database; integration tests passed after restore.
- Final archive/rollback guards reapplied and tested in both isolated staging databases.
- Twelve CRM UI tests, including request-key reuse after a failed response; UI typecheck passed.
- Stack backend suite: 2,573 examples, zero failures, one pending; verified again after the artist claim fix.
- All 105 registered migrations: full production-schema fixture, API startup, schema gates and repeat execution passed locally.
- Contact HTTP tests: authorization, eight concurrent retries, actor-scoped keys, payload conflict,
  shared contact details, archive visibility, authorized alias lookup and repeatable rollback passed.
- Artist claim HTTP tests include mixed ownership and invalid canonical profile kinds; the integrated
  prerequisite tightens this to artist-only targets and rejects canonical kind changes.
- Complementary-link SQL checks passed: unchanged Party fields, no archival, repeatable link/unlink
  and preservation of later edits.
- Companion mobile PR #92 merged as `50e680ae62850e2d1a30e9d0c75af9adad9a9043`; full typecheck and Metro startup passed.

Staging validation now also includes:

- All 105 migrations applied and repeated against a full synthetic schema on the staging host;
  the backup was restored into a separate database and schema checks passed after restoration.
- A snapshot of the existing staging application database was restored with owners and ACLs to
  an isolated clone; the candidate migrations, repeat execution and schema gates passed there.
- The operator CLI exercised review, stale-plan rejection, merge/link retries and selective undo
  against the full staging fixture. The ledger reports one applied-and-reverted merge, no active
  archives and no active complementary links after rehearsal.
- Candidate `caec2a6b2af3986df9f82c635a553287ca992c85` deployed to the existing staging app through
  its release command, with database reset/seeding and background automation disabled. Image digest:
  `sha256:9531490ac497d8f6a9deecbe399312704e63b3b8d5850bb3bb2297c43f824392`.
  `/version` confirmed the commit; Fly release command and machine health checks passed.
- Deployed staging HTTP tests passed: authorization, eight concurrent retries, actor scope,
  shared contact details, archive visibility, canonical lookup, archived edit conflict, rollback
  and preservation of later edits. Temporary fixture API tokens and roles were revoked afterward.
- Mobile contract integration PR #95 merged as `8b65d16d10fa1b15eac74413a98f82d409ccdd58`;
  typecheck and hosted validation passed, preserving recovery locale and the contact retry header.

The staging database host needed `postgresql-18-pgvector` 0.8.6 to exercise the full fixture.
The package installation was verified without PostgreSQL package upgrades; a replacement host
must provision that dependency too.

A private production backup was also restored into a local PostgreSQL 18 container with networking
disabled. All 605 data-table row counts matched the dump. Ownership/ACL definitions were restored
using NOLOGIN placeholder roles; production login credentials were not replicated as database roles.
The candidate migrations applied and repeated against that snapshot. Rehearsal of all 17 attested
groups yielded two executable merge groups (three archives), one complementary link, and 14 blocked
merge groups. After merge rollback and unlinking, all 605 original tables had identical row counts
and deterministic row hashes. These were isolated clone operations, not production cleanup.

A forward migration now derives archive guards from all 222 model-declared Party references,
including legacy review/approval fields without foreign keys. The 106-migration batch applied and
repeated against the restored production clone; the same two merge groups and one complementary
link passed another rehearsal and selective undo, with all 605 original tables unchanged. Focused
regressions also cover reviewer/approver insert/update guards, nonstandard requested-by references,
and avoiding archive guards on an unrelated issuing system's reviewer identifier.

The operator review workflow also passed a staging check for retaining earlier review evidence,
rejecting supplied replacement history, preventing review changes after execution, and rollback.

The exact merged image, `sha256:77dac9f4dcab74b0bc17f1886ff26652eb4030340f057ca0f369d5087e8150cf`,
passed staging HTTP checks before the existing guarded release workflow deployed and verified
both production machines. The release finished at 2026-09-18 05:00 UTC. Production remained
healthy after every cleanup operation.

A refreshed backup restored all 605 data tables and 241 Party rows before release. The fresh
production review recorded all 17 owner classifications. Each eligible merge revalidated scalar
and JSON dependencies under transaction locks, then verified unchanged row hashes for 607
unaffected tables and all contacts outside its group. Both operation retries returned already
applied, with no additional effects. The complementary link retry was also idempotent. No
financial, authentication, permission, consent or ownership records were moved or combined.

The live API verification traversed every contact-list page: 238 contacts remain active, all
three retired IDs resolve to their canonical contact for the authorized operator, anonymous
requests remain denied, and both complementary records remain active. Temporary Fly tunnel
failures affected read-only review calls; saved cases were reused and no merge operation failed.
The operator-specific rollback commands, exact operation IDs, private before-state evidence and
backup are retained in the owner's private recovery directory. No production undo was performed.

The provider-subject and additional contact-form prevention follow-up is PR #430, with mobile
companion #97. It is not yet deployed. Remaining ingestion and caller gaps are documented in
[provider-prevention.md](provider-prevention.md); this is not a claim of comprehensive completion.

Private evidence lives outside Git in `/private/tmp/tdf-identity-evidence-20260917/`, directory mode
0700, inventory/evidence files mode 0600. Inventory content SHA-256:
`70906feec6827df4e0b774690d7e1af38e470442406acfccd6947d80732b1ce7`.
