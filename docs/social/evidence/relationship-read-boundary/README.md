# Actual local results

Text logs have trailing whitespace normalized. The full Stack log is represented by
an explicitly labeled compilation/result excerpt and its original digest; the other
logs retain their output. `raw-log-digests.sha256` fingerprints original files.
Model and SQL unsafe-control failures are expected and checked. The first benchmark
failed because fixture account 2 remained an organization; the corrected all-eligible
fixture retains the same assertions and thresholds. No production-scale claim.
`http-final.txt` records 116 tests after the final self-link/UTC fixture additions;
`benchmark.txt` records the prior 115 tests and the unchanged production queries'
performance. SQL volatility checks are recorded separately. Hosted results are separate.
