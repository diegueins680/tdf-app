# 2026-07-12

- Fixed the public course landing form where clicking "Enviar inscripción" appeared to do nothing. Root cause: `CourseProductionLandingPage` reset the React Query registration mutation on every render because the effect depended on the whole mutation object. The reset detached the in-flight mutation observer before delayed requests could resolve into visible success/error UI.
- Added a delayed-submit regression test in `tdf-hq-ui/src/pages/CourseProductionLandingPage.test.tsx`.
- Applied and pushed `70792ad2b` for marketplace Stripe checkout, then added a follow-up hardening pass: UUID route parsing, persisted Stripe idempotency markers, safe retry/retrieval behavior, purpose-gated webhook recovery, active-cart uniqueness, and regression coverage. The dated `tdf-hq/sql/2026-07-12_marketplace_active_stripe_payment.sql` migration must be applied before deploying the new binary when `RUN_MIGRATIONS=false`; quiesce old checkout writers during cutover and reconcile any pre-existing unbound `stripe_pending` rows first.
- Completed the production Stripe cutover for follow-up commit `876413b721049da71d66a8d67c0162f97c72bef8` as Fly release `v2049`: expanded the `tdf-hq-db` volume from 3 GB to 5 GB, cordoned/stopped both old API machines, applied the 2026-07-07 and 2026-07-12 marketplace migrations, verified both columns/indexes and zero unsafe pending rows, deployed the pinned CI image, then uncordoned both machines. `/health` returned DB/service OK, `/version` returned the full SHA, both regions were started, and `RUN_MIGRATIONS=false` was verified on both machines.
- Resolved the pre-existing production `notification` schema drift with guarded migration commit `3f39fe94e`: a read-only catalog preflight confirmed a clean target, the narrow migration applied transactionally on PostgreSQL 17.2, and verification confirmed the table, sequence, nine columns, three validated constraints, recipient index, and a fully rolled-back insert/update/delete cycle. Both API instances remained healthy and subsequent logs contained no `42P01` or notification-schema errors.
- Audited and rebuilt the mobile ticket-buying journey: nearest-first discovery and direct ticket CTAs, dedicated tier/quantity/contact/promo checkout, free and 100%-discount completion without Stripe, authoritative PaymentSheet totals, explicit cancellation vs ambiguous-payment reconciliation, persistent checkout idempotency keys, a global **Mis entradas** QR wallet, auth/signup return paths, localized availability/errors, bounded polling, and buyer-facing API error copy. Mobile verification finished with strict lint/typecheck, 36 Jest suites / 188 tests, and a successful Expo web bundle.
- Hardened ticket fulfillment in `tdf-hq`: public exact-raw-body Stripe webhook, dispatch-before-dedup persistence, CAS payment transitions, atomic tier reservations plus event-row serialization for event-wide capacity, corrected pending inventory accounting, paid-direct-order protection, sale eligibility, zero-total issuance, stable local/Stripe idempotency reconciliation, promo basis-point/fixed-amount compatibility, confirmation email deep links, and a buyer-scoped global order endpoint. The dated ticket-checkout idempotency migration must be applied before deploying when `RUN_MIGRATIONS=false`.
- Implemented daily Ticketmaster event discovery for active users' fan/artist profile cities. The import uses strict returned-venue city matching with an optional deployment-wide country filter, creates or refreshes provider-owned venues, artists, genres, events, lineups, prices/currencies, images, and external ticket links transactionally, and uses provider reference tables for idempotency. It has a PostgreSQL leader lock, persistent daily run ledger, bounded rotating city/page budgets, API throttling/retry, sale/lifecycle reconciliation, production timezone support, and mobile display of provider currencies. Before deployment with `RUN_MIGRATIONS=false`, apply `tdf-hq/sql/2026-07-12_event_discovery_imports.sql` and configure `TICKETMASTER_API_KEY` as a secret.
