- Implemented a lightweight formal verification gate: `npm run verify:formal` now runs the improvement-loop model check and formal-methods audit, with CI-style blocking only on `error`/`critical` findings.
- Added formal verification docs and tests for the formal audit CLI/scope. The audit intentionally skips archives, generated clients, native mobile generated folders, build outputs, and dependencies.
- While verifying, the active continuous-improvement loop committed several intermediate changes automatically. Treat remaining edits in `scripts/continuous-improvement-loop.mjs` and `scripts/lib/discovery.mjs` as the latest manual refinements from this session.
