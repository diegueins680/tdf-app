- Implemented a lightweight formal verification gate: `npm run verify:formal` now runs the improvement-loop model check and formal-methods audit, with CI-style blocking only on `error`/`critical` findings.
- Added formal verification docs and tests for the formal audit CLI/scope. The audit intentionally skips archives, generated clients, native mobile generated folders, build outputs, and dependencies.
- While verifying, the active continuous-improvement loop committed several intermediate changes automatically. Treat remaining edits in `scripts/continuous-improvement-loop.mjs` and `scripts/lib/discovery.mjs` as the latest manual refinements from this session.
- Fixed the `tdf-hq-ui` Cloudflare build break by adding a production `tsconfig.app.json` that excludes tests from `tsc` during deploy builds, and by carrying Stripe checkout success through the backend `spiOrderId` instead of unsupported browser `paymentIntent.metadata`. `npm run build --workspace=tdf-hq-ui` passed locally. A targeted Jest run was blocked by local optional `@unrs/resolver-binding-darwin-x64` registry access and an existing `ts-jest` preset resolution issue.
- Social inbox handoff rule: when the AI reply generator returns `HOLD`/`NEED`, TDF HQ should not put that text in the customer reply box. It should ask Diego by WhatsApp through `/whatsapp/operator-question`; configure `AI_HANDOFF_WHATSAPP_NUMBER` for Diego's number.
