# 2026-08-11

## Workstreams advanced

### PR #141 — Community Attribution Foundations
- Fixed missing `import { describe, it, expect, beforeEach, jest } from '@jest/globals'` in `growthAttribution.test.ts`
- Fixed eslint `no-control-regex` error (intentional control-char stripping regex) and `prefer-nullish-coalescing` warning in `growthAttribution.ts`
- Full suite: 129 suites, 1,552 tests passing
- Both commits pushed to `origin/main` (`37520d045`, `8b930350a`)

### PR #142 — Admin Operations Control Center
- Merged into main by continuous-improvement-loop during session (`e555b65ac`)
- All CI checks green (Cloudflare, Vercel, api-contracts, build, production-migrations, quality)
- Code review completed — architecture strong, 16 issues documented (4 high, 8 medium, 4 low)
- Key concerns before production enablement: `error` in toJson, `fail` in SQL monad producing 500s, hardcoded UUIDs, no integration tests for handlers/SQL

### Production Readiness Assessment
- Production intentionally frozen — Fly org billing blocker prevents canary deploys
- Both PRs now merged; emergency-admin path still single-verified
- 6 unregistered post-07-12 migrations need reconciliation in `production-migrations.json`
- Operations Control Center runbook ready at `docs/operations-control-center/runbook.md`

## PRs merged (by continuous-improvement-loop)
- PR #140 (feature discoverability) — merged Aug 7
- PR #141 (growth foundations) — merged today
- PR #142 (admin operations) — merged today

## Production blockers (priority order)
1. Fly org billing — `diego-saa` org requires billing info
2. Second emergency-administrator path needs verification
3. Encrypted authorization/preferences backups
4. Historical seed credential rotation
5. Unregistered post-07-12 migrations reconciliation
6. Dependency audit + accessibility/E2E acceptance

### Prácticas playbook payload fix
- Fixed the HQ "Generar plan base" flow: create-project and create-task requests now omit unset optional fields instead of sending `null`, matching the backend's strict create contract.
- Tightened `InternProjectCreate` and `InternTaskCreate` UI types and added a regression covering the generated project plus all 12 tasks.
- Verified the full `InternshipsPage` test file (14 tests), UI typecheck, targeted ESLint, and production build.
- Published commit `51ef4ca5c` on `fix/internship-playbook-create-payloads`; Vercel and Cloudflare previews passed, and the public Cloudflare chunk was checked to confirm the corrected create payloads.

### Asignación de agenda de ensayos al pasante vigente
- Production data identified Stewart Moreira (party 129) as the current intern: start 2026-06-15, no end date, and a clock-in on 2026-08-11. The two other active Intern roles belong to profiles whose placements ended in January/February.
- Added the Reception role through the production API while preserving Customer, Fan, and Intern, giving Stewart the CRM and Scheduling modules required by the booking UI. Verified his own session can load bookings, parties, rooms, and rehearsal services.
- Created Customer organizations Milo Mae (party 140) and Alex Crack (party 141), plus internship project `efd8feba-12de-40f4-ab85-06cee6973935` and assigned task `7e1f7364-8e02-453e-bdf9-b3f17a165fa2`, due 2026-08-14.
- The task contains a Spanish quick guide for the rehearsals on Aug 15 at 10:00 (Milo Mae), Aug 17 at 16:00 (Milo Mae), and Aug 19 at 14:00 (Alex Crack), all in America/Guayaquil. It calls out the system's 90-minute rehearsal default, Live Room, Confirmed status, and the Aug 15 course-calendar overlap that needs a real room-availability check.
- Temporary API tokens created only for the bounded production operation and Stewart access verification were deleted after the task became visible in his own `/internships/tasks` response.
- Opened draft PR #143. Its first CI run exposed two local form declarations that violated the repo's interface-only lint rule; fixed and pushed `adb66e93b`. The refreshed run has both builds, API contracts, migration checks, Cloudflare, and Vercel green while the long full-stack `quality` job remains in progress.

### DDEX inbox production failure
- Fly logs confirmed `/ddex/documents` failed with PostgreSQL `42P01` because production had no `ddex_document` relation; a read-only production query found zero DDEX/catalog tables and no DDEX migration marker.
- Registered `2026-08-02_ddex_catalog_core.sql` in the guarded production manifest, added the three inbox-selected metadata columns missing from the raw schema, and extended release verification/CI coverage. The complete migration batch, a second batch run, raw DDEX reapplication, and schema verification pass in disposable PostgreSQL 17.
- Updated the web client's default Frankfurter endpoint from the redirecting `.app` URL to `https://api.frankfurter.dev/v1`; targeted tests and UI typecheck pass.
- Committed and pushed the DDEX fix as `a18436ee7`; all checks passed. With Diego's authorization, applied the guarded DDEX migration to production and verified the exact 15-column runtime query. The production inbox failure is resolved.
- Registered the previously omitted campaign-automation and feature-discovery/navigation migrations, moved the feature migrations into the authoritative SQL directory, and expanded release preflight/verification coverage. Committed and pushed as `b3d952079`.
- Applied the complete 15-entry authoritative migration batch to production. Its final schema verification passed; the ledger records campaign, feature-discovery/navigation, and operations schemas from `b3d952079`, DDEX from `a18436ee7`, and all earlier migrations. Campaign/nav missing-relation errors stopped, and unauthenticated live DDEX/navigation requests now reach their handlers and return `401` instead of `500`/`404`.
- Repaired missing runtime source metadata on both production Machines, one at a time on the identical immutable image digest, to report the exact running commit `3b9c992f3`; both Machines remained healthy with migration/event-discovery safety flags disabled. Fixed the release fallback smoke check to pin requests to the selected Fly Machine and pushed `cb670f3ee`.
- After the full CI quality gate and Docker publication passed, the guarded production release completed at `b3d952079` with canary `0807ee9cd34668`, second Machine `3d8d2500c67168`, no rollbacks, and both regions healthy on platform digest `sha256:9c4728e29f39a730f9e64675120535429f9334df9bccfb5a4193a91c578ac95b`. Post-release checks found 15 ledger entries, no release lease, no SQL/runtime errors, and DDEX/navigation routes reaching authentication instead of failing. The live Cloudflare bundle uses `https://api.frankfurter.dev/v1`, whose browser-origin response is 200 with permissive CORS.

### Persisted feedback catalogs candidate
- Replaced feedback category/severity arrays and backend allowlists with specialized UUID catalogs, bilingual public batch reads, scoped defaults, strict multipart UUID writes, persisted notification labels, database integrity, and guarded migration/rollback scripts in the isolated catalog-persistence candidate.
- Disposable PostgreSQL rehearsal run `b7a8f35a-32d5-433d-afdb-3de497fd872a` mapped one controlled `Idea`/`P2` legacy row to two UUIDs, retained one evidence row and two mappings across reruns, restored exactly on rollback, and rejected copied, unknown, missing, inactive, over-threshold, unresolved, default-deactivation, and hard-delete cases.
- Candidate startup, health, bilingual catalog batch, ETag/304, Haskell build and 24 focused examples, six web tests, 10 mobile administration/API tests, both typechecks, lint, and generated clients passed. Web/mobile now expose strict feedback catalog draft/review editors from persisted definitions. Production was not changed; its inventory has one `idea` category but no captured severity, so rollout stays fail-closed pending backup and current dry-run.

### Andrés Efraín Riofrío student onboarding
- Created production party/student 142 for Andrés Efraín Riofrío and linked him to active teacher Esteban Muñoz (party 43) for Producción Musical.
- Recorded his first attended class as class session 1 on 2026-08-11 from 18:00 to 19:00 America/Guayaquil in Control Room, consuming 60 minutes.
- Recorded payment 5 as a USD 240 partial Produbanco bank transfer received at 16:41, reference 558496020900. Created active Producción Musical package 1 for 12 classes / 24 hours at USD 480 under the established 120-day/credit-only defaults, created open purchase 1 commissioned to Esteban with USD 240 paid and USD 240 outstanding, and linked class session 1. Verified 60 of 1,440 minutes consumed and 1,380 minutes remaining, with exactly one package, purchase, payment, and linked class.
- The supplied receipt remains unattached. A browser-side reauthorization initially did not replace Fly `DRIVE_REFRESH_TOKEN`, so repeated server uploads returned 401. Release v2080 rolled a value containing a control character and the backend rejected it with 503. Release v2081 rolled a sanitized 206-character value with the expected Google refresh-token prefix, but Google still rejected it as `invalid_grant`, indicating that the token itself is revoked/stale despite having the correct shape. All failed upload attempts made no payment changes, all bounded temporary API tokens were deleted, and payment 5 currently has no attachment URL.
