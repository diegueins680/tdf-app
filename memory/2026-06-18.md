# 2026-06-18

- Created local draft campaign record `1`: `TDF App Mobile Internal Testers - Jun 2026`, status `draft`, for recruiting existing email/WhatsApp contacts to join Android and iPhone internal testing.
- Added campaign packet at `docs/release/mobile-internal-testers-campaign-2026-06-18.md` with email and WhatsApp copy plus approval gate.
- Updated campaign copy to include iPhone users with TestFlight public link `https://testflight.apple.com/join/7k3VE2JJ`.
- Local recipient counts at draft time: 10 parties with email, 5 with WhatsApp, 4 with primary phone; active registered users include 10 with email and 3 with WhatsApp/phone.
- Diego's local party record has email but no WhatsApp/phone, so an actual WhatsApp test send needs a target number before sending.
- Tried sending the preview messages through production API `https://tdf-hq.fly.dev` to Diego. Email test to `cuco.saa@gmail.com` reached the API but failed at SMTP with authentication failure. WhatsApp manual reply to Diego's known phone reached the API and created message id `3`, but delivery failed because production Fly secrets are missing `WHATSAPP_TOKEN`/`WA_TOKEN` and `WHATSAPP_PHONE_NUMBER_ID`/`WA_PHONE_ID`. Do not send full campaign until these production delivery configs are fixed and Diego approves the delivered previews.
- Reimported SMTP secrets from `tdf-hq/.env` into Fly for `tdf-hq` and retried the email preview. It still failed with SMTP authentication failure, so the local/prod SMTP credential is stale or invalid. Need a valid SMTP password/app password for `info@tdfrecords.net` or a replacement SMTP account before email delivery can work.
