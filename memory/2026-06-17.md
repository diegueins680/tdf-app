# 2026-06-17

- TDF mobile Android dev client: Samsung SM-S928B (`R5CX12YLKMZ`) connected over ADB after enabling Developer options and USB debugging.
- Local Android runs need Android Studio's bundled JBR:
  `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"`.
- Created `tdf-mobile/android/local.properties` with `sdk.dir=/Users/diegosaa/Library/Android/sdk`.
- Existing release-signed `com.tdf.records` could not be replaced by a debug build; uninstalled it from the phone, then installed the debug dev client.
- Bumped local Android `versionCode` to 8 to avoid downgrade conflicts with the previously installed Play build.
- Fixed Expo native crash by aligning packages to Expo SDK 54 versions and adding `expo-localization` to `app.config.ts` plugins. `npx expo install --check` passes.
- The Android dev server ran on Metro at `http://10.229.1.127:8081`; app process `com.tdf.records` was alive after install.
- On-device local backend login requires `adb reverse tcp:8080 tcp:8080`; otherwise `http://localhost:8080` inside the phone points at the phone and login shows `Network Error`.
- Verified local `/login`: `admin` / `password123` returns party 9 with Admin role; `tdf-owner` / `TDFowner2025!` returns party 33 with Manager role.
