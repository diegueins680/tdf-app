# 2026-09-10

- Diagnosed Diego's live `/social` report: Andrea's newly created personal account exists and is active, but Party search failed before lookup because `DirectoryServer.consumeRate` inserted `party_selector:social_connection` into `directory_rate_limit` while the deployed `directory_rate_limit_scope_check` still rejected both Party-selector scopes. Fly logs captured SQLSTATE `23514`; API health and CORS were otherwise healthy. The bell's `feature_access_request` cards were unrelated to signup approval and had a separate UI defect: only `event_logistics` targets navigated, so access-review cards merely marked themselves read.
- Prepared the isolated branch `fix/social-connection-search-notification-routing-20260910` at `dcb3a1ff11b538c097a28bd0034b5414838e1bed`. Immutable migration anchor `565ed7d0a1c5381719538c6e2bcae83ac30c3a92` adds the two closed rate-limit scopes with fail-closed validation and safe rollback; the follow-up routes reviewer notifications to `/solicitudes-acceso/revision` and requester notifications to `/solicitudes-acceso`, registers/verifies the migration, and adds focused CI coverage. Validation passed: migration forward/idempotency/rollback suite, full production-shaped automatic migration replay with identical second-run schema, 60 release tests, 17 CI tests, 5 notification routing tests, targeted lint, TypeScript, production UI build, and bundle budget. The branch is local only; no push, PR, merge, production migration, deploy, or production data change occurred.
- Diego authorized publication and protected-release preparation. Published the branch and opened PR #328. The first hosted catalog audit found the superseded manifest decision `e5ce39b6bbdd84f9652a`; removed only that stale entry in commit `872d7777bf472d1ca9975000a0dd14a507ee9788`, then the deterministic and full audit passed with the pinned mobile submodule initialized. All hosted CI, migration, backend, UI, mobile, E2E, Datadog, Cloudflare, and Vercel checks passed, `tdfrecords` approved the current head, and the PR is `OPEN/MERGEABLE/CLEAN`. Verified both the remote head and current GitHub merge ref retain migration anchor `565ed7d0a` as an ancestor; replied to and resolved the stale automated P1 review thread. PR #328 must use a merge commit, never squash/rebase, to preserve that immutable ancestry. Fly production remains unchanged and healthy on two Machines; no merge, production migration, or production deployment occurred.
- Diego explicitly continued the Tu Escena Conectada Instagram-follower campaign at 09:37, opening one bounded five-message block inside the permanent 09:00–20:00 window. Five DMs were sent and confirmed between 09:42 and 10:00 to current public adult music followers `domingo.caballero.techno`, `st.giou`, `chuleizurieta`, `mystica_music22`, and `kid_mykez`; the Listening Party was correctly described as happening today at 20:00. Each personalized UTM appeared once, every composer emptied after delivery, and no restriction or warning appeared. Only after confirmation the contacts were created and revalidated: Domingo Caballero Persona 225, Gio Persona 226, Chule Izurieta Persona 228, Mystica Músic Empresa 229, and DJ Kid Mykez Persona 230; all have `has_user=false`. `lo_montoya` was excluded on exact existing CRM match 180, `apu.wasi.cultura` because it hosts a simultaneous 20:00 concert, historical contacts were not repeated, and private/inactive/non-musical/no-composer profiles were skipped without mutation. Ana Fernandes' new positive reply from the prior-day DM was observed but not answered automatically and is not a conversion. Final Sep 10 block totals: 5 confirmed DMs, 5 unique CRM contacts, 0 conversions; the block stopped exactly at five. Report: `docs/campaigns/tu-escena-conectada-seguidores-2026-09-10.md`.
- Completed and soaked the explicitly authorized real-money event-ticket PayPal rollout. PR #323 merged as `1157258b6a6551d49708fa9eb21ab893b6a051f1`; the guarded migration/canary release deployed immutable image index `sha256:f1e05c92e2288cec9dc3debc4f5277cf48a5ba2812a978502d18797c2fa74595` / amd64 digest `sha256:fbec2c1b423c3dbda7d10adb06aa7150f9aae5ae315ad3981006673bc406b612` to both healthy Machines and repaired the provider-worker stale timestamp plus textual ticket-buyer eligibility queries. One canonical live PayPal webhook is registered for capture completed/refunded/reversed, with webhook intake and the provider-event worker enabled. The negative-signature probe returned 401; follow-up soak kept provider inbox/open/dead-letter/reconciliation at zero, payment successes and nonterminal checkouts at zero, payment-specific logs error-free, and event 121 checkout available with inventory 100. PayPal refunds, settlements, and all Datafast gates remain disabled. Snapshot `vs_Do0gN0nzaQ3SReVK99GRAX` remains available; no real money moved during rollout.
- Diagnosed the unrelated `unique_artist_enrichment_run` production log without mutating that subsystem. Scheduled workflow run `34487816181` created only ledger row 107 (`operator:production:full:full:2026-09-10`). Its synchronous backend discovery exceeded the workflow's 300-second API timeout; generic `retryFetch` retried the mutating POST while the original transaction's insert was still uncommitted, so the backend's `getBy`/`insert` sequence exposed a race and logged SQLSTATE 23505 when the first transaction committed. The workflow nevertheless continued, then correctly marked the run failed after two MusicBrainz 503 responses among its first five attempts tripped the safety threshold; three inventory identities completed, nothing auto-published, and the backend's separate daily dry run 106 had completed normally. MusicBrainz returned 200 in a later single rate-limited probe. A proper separate fix should atomically get-or-create the run on unique conflict and lengthen/decouple the synchronous discovery request; provider 5xx backoff may also need hardening. Payment health remained unaffected.
- Completed Google signup consent fix PR #321 after reconciling it with current `main`. Merge commit `a3f13a6fb` brought in base `1157258b6a6551d49708fa9eb21ab893b6a051f1`; follow-up `c690e7db66ba85cb19fd2217892299e93dd55997` narrowly migrated the upstream production-migration manifest's catalog decision ID after an authoritative audit with the mobile submodule initialized. Focused signup tests, ESLint, TypeScript, repo quality, strict catalog audit, hosted CI, Safe Install, persona E2E, Cloudflare/Vercel previews, and fresh Codex review passed. After `tdfrecords` approved the exact updated head, protected auto-merge completed without an admin bypass as `e2086e25bca856a413433a28f8ae83219b05ed38`. Main-branch Safe Install, catalog audit, Datadog, Cloudflare Pages, and Vercel production deployment succeeded. A live `https://tdf-app.pages.dev/login` smoke check returned 200 and its deployed `LoginPage-CuskdIa_.js` contains the first-time Google signup CTA, `tdf-account-terms-v1`, and Spanish consent recovery message.
- Continued the mobile onboarding continuity stack with a direct online error-state feed retry. Mobile commit `6bf3956015ed1d813b0b368f3f1bb1e9d4d60058` adds localized ES/EN retry copy, an accessible busy/disabled action, and Party-scoped coalescing across manual, reconnect, and foreground recovery; automatic retries remain visually silent. Parent commit `be87ba8aab655ad997d868e10b4ed00d21a8c712` records the immutable submodule pointer and evidence report. Opened stacked draft PRs mobile #77 and root #329 against the 2026-09-09 feed-recovery branches; both are OPEN/DRAFT/MERGEABLE/CLEAN and remote SHAs match locally. Validation passed: focused gate 30/30, full mobile 70 suites/437 tests, lint, TypeScript, catalog audit (1,279 files/945 candidates/0 unreviewed/0 stale), repository quality, and every applicable hosted check. No merge, deployment, production mutation, customer communication, or experiment activation occurred.
