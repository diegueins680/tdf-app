# 2026-08-12

- Fixed production booking failures where the UI sent `Room` UUIDs but the booking API expected `Resource` references. The UI now submits canonical room names and the backend resolves legacy room UUIDs by matching active resources by name.
- Published and deployed commit `a819028ee50df8e97399623209cf431bd4d9d5a3`. Cloudflare frontend and both Fly.io backend machines were verified healthy; `/version` reports the exact SHA.
- The automated continuous-improvement loop raced the manual commit and included pre-existing memory/DREAMS changes in the remote checkpoint/merge. Later automated commits and local UI changes were preserved and excluded from the backend release by deploying from a clean detached worktree at the tested SHA.
- Production verification was read-only: `Live Room` remains bookable with UUID `a0130c03-3527-41b3-b370-a65e66823f77`; no synthetic booking or customer data was created.
- Implemented an uncommitted CI/CD optimization candidate: path-scoped repo/UI/mobile/backend/contracts/migration jobs, stable Stack dependency/object caches, cancellation of obsolete runs, a single Stack build+test+artifact pass, and a runtime-only Docker image that packages the tested executable. Warm backend validation completed in 20.8 seconds with 2,292 tests; UI completed 130 suites / 1,567 tests plus build; Docker static checks reported no warnings. No commit, push, image publication, or deployment was performed.
- Prepared and received approval for the first Instagram pilot “Tu escena, conectada”: audited signup/UTM behavior and public production, verified CRM availability, selected 20 visible Quito-scene candidates with a 10A/10B split, checked exact normalized Instagram handles in CRM, drafted the 20 personalized messages, and created a silent 15-second 1080×1920 preview using existing TDF material plus a public unauthenticated platform capture. `unaclaudiapalma` was excluded as already registered. Report: `docs/campaigns/tu-escena-conectada-piloto-2026-08-12.md`.
- Sent the first approved DM to `blue_records333`; Instagram confirmed the message in-thread and showed it as seen, with no reply, warning, CAPTCHA, restriction, duplicate, or no-contact request. Created `Blue Records` in CRM only after the confirmed send. Saving Instagram initially failed with HTTP 400 because the deployed UI sent optional party fields as JSON null. Implemented frontend omission of null update fields and deployed a backend compatibility hotfix at `d10e7cdf9429e548aefaa60708610cc8a71e80bd`; official Build Image workflow 31645491034 passed, both production machines reported health/db ok and the exact commit, and `blue_records333` then saved successfully. The other four planned messages were deferred because the 18:00 operating cutoff passed. The video was not published because no concrete rights-cleared audio track has been identified.
- The user explicitly extended the 2026-08-12 operating window to 20:00. Re-reviewed public `hno613613`, confirmed exact Instagram CRM search returned no match, sent the approved artist/A message at 19:25, and Instagram showed the full message as seen. After creating an HNO person contact, a name search revealed two indistinguishable HNO contacts with neither email nor Instagram. Stopped immediately under the campaign's accidental-duplication rule without editing or deleting either record and without contacting Teez, Rovlex, or Llama Este Pez. HNO's Instagram field remains unrecorded pending human resolution.
- The user then explicitly resumed the pilot and extended the 2026-08-12 window to 21:00. Completed the first five-message block by contacting and recording `teez__t`, `el_rovlex`, and `jam_estepez`; opened a second block and contacted/recorded `planet_303` and `federico__molinari_`. All seven total pilot DMs were verified in-thread and seen. CRM has exact Instagram values for six; HNO remains ambiguous. Blue Records replied `Listo :)` and HNO replied `Ya le hago`, both positive; neither proves registration or activation. While preparing approved `oximoron_tu_tentaculo`, Instagram identified the exact account and exposed a 2023 message request, but the visible Accept button remained after two confirmed clicks and no composer appeared. No Tentáculos DM or CRM contact was created. Stopped immediately for unexpected browser behavior; Gabby and Takijana were not contacted, and the two positive replies were not answered before the stop.
- Continued the uncommitted catalog-persistence candidate in `.tmp/catalog-persistence-20260807`. Closed DDEX validation result/severity/layer persistence with UUID registries, reversible guarded backfill, strict OpenAPI/client/UI consumption, startup gates, PostgreSQL apply/rerun/rollback/reapply rehearsal, and refreshed inventory (857 files, 527 candidates; no unreviewed/stale decisions).
- Strengthened the emergency-administrator rollout gate without mutating production. A privacy-safe aggregate read-only Fly PostgreSQL query found two active legacy `Admin` assignments on distinct parties but only one party with an active credential. Added a legacy/canonical preflight, wired it before and after migrations in the release runner, and hardened DB/runtime coherence against loss of the final assignment, critical grant/registry row, credential, or emergency role. Local evidence: backend compiled, 17/17 focused Haskell security tests, 24/24 release-runner tests, canonical/legacy preflight fixtures, and rolled-back PostgreSQL security integration passed. Production deployment remains blocked until an authorized security administrator establishes and independently verifies a second credential path; no credential, backup, migration, deploy, commit, push, or PR was created.
- Continued the isolated catalog-persistence candidate and published coordinated creator-badge commits: mobile `0992403346d8d8dc156ac3e0daefc340aa994c82` and parent `0e41edacebcd7ff381ccd6c2fa7401d6f8476cf2` on `codex/catalog-persistence-20260807`. The slice replaces badge strings with persisted UUID types, typed bilingual leaderboard objects, web/mobile administration, mobile snapshot schema 9, guarded dry-run/apply/rollback, and PostgreSQL apply/rerun/exact-rollback/reapply evidence. Opened draft PR #144; it remains unmerged and currently conflicts with a substantially advanced `main`. Production was untouched and remains blocked by the second coherent emergency-administrator path. The next bounded slice is a governed notification-type registry with typed artist/event/post/memory destination FKs; current evidence shows two real producers, three dormant historical types, and zero production rows.
