# 2026-03-30

- Completed a backend hardening pass for the new service marketplace booking flow in `tdf-hq`.
- Fixed a half-baked shortcut where marketplace bookings always created `Recording` service orders instead of inheriting `ServiceCatalog.kind`.
- Added catalog validation so missing/inactive service catalogs are rejected before creating service ads or marketplace bookings.
- Added focused Haskell tests for the catalog guard and service-kind derivation.
- While verifying, uncovered and fixed a latent `partyDisplayName` ambiguity/type issue in `TDF.Server` plus two escrow-handler warning-cleanup nits.
- Verified with `stack test tdf-hq:test:tdf-hq-test` passing.
- Continued the cleanup pass in `tdf-hq-ui` by making `AdminUsersPage` searchable and adding visible-state summary chips so large admin user lists are easier to scan.
- Added a focused Jest test covering search filtering and empty-filter state, then verified with the page test and `npm --workspace=tdf-hq-ui run typecheck`.
- Verified and simplified the public trial lesson flow in `tdf-hq-ui/src/pages/TrialsPage.tsx`.
- Fixed a production-breaking bug where choosing a subject could crash because the MUI select value arrived as a number and the page assumed a string.
- Reduced friction by turning suggested slots into clickable chips, keeping only one preferred time visible at first, and moving error copy to friendlier user-facing messages.
- Added focused Jest coverage for the trial page helpers and progressive slot UX, then verified with the new test, `npm --workspace=tdf-hq-ui run typecheck`, and `npm --workspace=tdf-hq-ui run build`.
- Confirmed the local backend trial request API works end-to-end after seeding (`POST /trials/v1/trial-requests` returned `201 Created`).
- Noted a local environment trap: `tdf-hq-ui/.env` currently points `VITE_API_BASE` to `http://localhost:8080`, but that port was occupied by an unrelated `trader-hs` service during verification.
