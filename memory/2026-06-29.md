- Ajuste en el reporte financiero de Esteban Muñoz: los cursos sabatinos de producción que TDF le debe se recalculan a USD 25/hora en vez de USD 30/hora. Con dos cursos de 16 horas cada uno, los honorarios bajan a USD 800 y el saldo neto por cobrar a Esteban sube a USD 450.
- Se agrego al mismo reporte el pago de Paula Roman por realizacion de mastering: USD 200 recibidos, 40% para Esteban = USD 80. El total a favor de Esteban queda en USD 880 y el saldo neto por cobrar baja a USD 370. La pagina ahora incluye un enlace de descarga PDF generado en frontend.
- Se corrigio el fallo de `npm audit --omit=dev` actualizando solo `package-lock.json`: `dompurify` 3.4.11, `react-router-dom`/`react-router` 6.30.4 y `@remix-run/router` 1.23.3. El audit de produccion queda en 0 vulnerabilidades.
- Tras ese push, el workflow `Build Image` fallo por `COPY tdf-hq/stack-snapshot.yaml`, archivo que ya no existe. Se ajusto el Dockerfile del backend para usar `haskell:9.10.3-bullseye` y copiar `stack.yaml.lock`, en linea con `resolver: lts-24.42`.
- El siguiente fallo Docker fue `postgresql-libpq-configure-0.11`: `libpq-dev` en bullseye resolvia PostgreSQL 13.23, pero el paquete exige >= 14.12. Se cambio el builder a `haskell:9.10.3-bookworm`, que usa `libpq-dev` 15.x.
- Diagnostico de reset de password en produccion: `info@tdfrecords.net` no tiene `party.primary_email` ni credencial activa asociada. La cuenta admin existente es username `admin`, party `Diego Saa`, con `primary_email` vacio; por eso `/v1/password-reset` devuelve exito generico pero no crea ni envia enlace.
- Se actualizo en produccion el party admin `Diego Saa` (username `admin`) para usar `primary_email = info@tdfrecords.net`. Verificado que el lookup de reset devuelve 1 credencial activa; se llamo `/v1/password-reset` y Fly logs confirmo token creado y SMTP enviado a `info@tdfrecords.net`.
- Se corrigio en produccion el bloqueo de strict admin: el usuario `admin` tenia roles extra `Engineer` y `Teacher`; se desactivaron esos `party_role`, quedando roles activos `Admin`, `Customer`, `Fan`.
- Se corrigio en produccion el duplicado de credenciales activas de Esteban Munoz: se desactivo la credencial vieja/malformada `mixandlivesoundgmail.com` y se dejo activa la credencial canonica `mixandlivesound@gmail.com`. El lookup de password reset para `mixandlivesound@gmail.com` ahora devuelve 1 candidato.
- Verificado que Esteban Munoz puede acceder al reporte `/finanzas/reporte-esteban-munoz` con la app desplegada: su cuenta canonica activa tiene rol `Accounting`, que concede modulo `invoicing`, y la ruta `/finanzas/*` requiere ese modulo.
- Login web: `tdf-app.pages.dev` mostro `Failed to fetch` en `/login` y 404 en `/fans/me/notifications/count`. El backend live responde `/health`, pero `/version` sigue con build `2026-05-23`/commit `dev` y Fly status muestra imagen version 2043 del 2026-06-18; la ruta de notificaciones existe localmente pero produccion esta vieja. Se parcheo frontend para mostrar error estable de conexion en auth y tratar 404 de notificaciones como estado vacio hasta redeploy.
- Se redisenó visualmente el reporte de Esteban Munoz en web y PDF: tablas por seccion, resumen ejecutivo, saldos destacados y PDF multipagina mas legible. Pasaron `npm test --workspace=tdf-hq-ui -- estebanMunozReport.test.ts`, `npm run typecheck --workspace=tdf-hq-ui` y `npm run build --workspace=tdf-hq-ui`.
- Commit `4054738f7` endurecio login y notificaciones: reintentos ante fallos de red en auth y fallback vacio para notificaciones 404. Se pusheo a `origin/main`, CI de GitHub paso, se disparo `Build Image`, se desplego `diegueins680/tdf-hq:4054738f7cdc10e94d2ca0d47ba099da223a4758` en Fly y `/version` ya reporta ese commit; `/fans/me/notifications/count` paso de 404 a 401 sin token. Cloudflare Pages fallo el build de ese commit y requiere dashboard o `CLOUDFLARE_API_TOKEN` para ver logs/reintentar; el host live sigue sirviendo el bundle anterior.
