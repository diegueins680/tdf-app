# 2026-07-14

- Fixed a marketplace admin routing bug where `GET /marketplace/orders` could be swallowed by the public `/marketplace/:id` capture and return `Invalid listing id`. The fix was to move `AuthProtect "bearer-token" :> ProtectedAPI` ahead of the public marketplace branch in `tdf-hq/src/TDF/API.hs` and to reorder the server composition in `tdf-hq/src/TDF/Server.hs` to match.
- Verified the change with `stack test tdf-hq:test:tdf-hq-test --fast`; the full suite passed after recompilation.
- Production was briefly on the unsafe local image `cd75fb412dfe746105ab4330a23745dc8d1e57f5` during event-discovery work. Forced the safety flags back to `RUN_MIGRATIONS=false` and `EVENT_DISCOVERY_ENABLED=false`, stopped the local continuous-improvement launchd loop, and restored Fly to the known-good image `registry.fly.io/tdf-hq:deployment-01KXCW71SVXCFR8YBGBYAPGTYK` (`/version` reports `a3bf267066689415f09aa68aaacd4630406cbd4b`).
- The guarded backend release lane for ticketing/event discovery is prepared around target SHA `393bf4fc9708a051409dbef3606564eb2e446933`. The remaining backend blocker is publication/verification of the Docker image for that SHA.
- Set the live `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` on Fly app `tdf-hq` from the user-provided local secret file. Fly rolled both Machines; both returned to `started`, and public `/health` returned `{"db":"ok","status":"ok"}`. The release lane still needs the target Docker image to publish/verify before it can be executed.
- Updated the local UI environment with the live Stripe publishable key and started a successful production UI build. Cloudflare Pages remains unconfigured because this machine has no `CLOUDFLARE_API_TOKEN`; set `VITE_STRIPE_PUBLISHABLE_KEY` for the production Pages environment and redeploy before using live checkout.
- The guarded production release completed at 2026-07-15T06:03:32Z for image `diegueins680/tdf-hq:393bf4fc9708a051409dbef3606564eb2e446933` (manifest digest `sha256:cd7a5fdaf95fb877d216291d076dfe08776cf16c18ac313dd2dd812953f38228`). All three schema migrations were applied; both API Machines run commit `393bf4fc9708a051409dbef3606564eb2e446933`, report healthy DB status, retain `RUN_MIGRATIONS=false` and `EVENT_DISCOVERY_ENABLED=false`, and have both Stripe Fly secret names. The release report is in `artifacts/releases/production-release-393bf4fc9708a051409dbef3606564eb2e446933-2026-07-15T06-03-39.901Z-9de79491-56b6-4e21-9386-709b2d46ed21.json` (ignored by Git).
