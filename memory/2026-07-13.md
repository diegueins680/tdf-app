# 2026-07-13

- Built a guarded Fly backend release lane for the ticket checkout and event discovery rollout. The lane is commit-based, requires a full SHA, reads `fly.toml`/migration files from the target commit, validates effective runtime flags on every Machine, checks Stripe Fly secret names, verifies Docker image provenance/digests, applies schema through a checksum ledger and advisory lock, holds a database release lease, deploys one exact Machine at a time, verifies per-Machine health/version/digest, and rolls back only Machines touched before failure.
- Added CI coverage for the release lane: production release unit tests, PostgreSQL 17 migration fixture, read-only preflight, authoritative migration batch apply/skip behavior, raw SQL idempotency, schema verification, and checksum-mismatch rejection.
- Read-only production audit after an external release to `d51bdd981df777c1b9046d70ce6a9661ef2ad49a`: public health/version are OK; effective `RUN_MIGRATIONS=false`; effective `EVENT_DISCOVERY_ENABLED` is unset, so discovery ran once by default. Discovery tables match the verifier and contain one completed Ticketmaster run for two cities with zero seen/created/updated records. Ticket runtime schema remains absent, with zero base ticket rows and no reconciliation conflicts. `tdf_schema_migration` and `app_release_migration` are absent.
- Current production release blockers: `EVENT_DISCOVERY_ENABLED=false` must be made effective before the guarded preflight can pass; `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` are missing from Fly secrets; and pushing to `main` may trigger an unguarded auto-deploy path, so do not push this lane until that risk is disabled or explicitly accepted.
