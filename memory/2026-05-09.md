# Memory 2026-05-09

## tdf-label-release run (cron)

### Catastrophic mistake: used `write` instead of `edit` to append report
- **What happened**: Appending a checkpoint to `/Users/diegosaa/.openclaw/orgs/tdf-label/reports/tdf-label-release.md` using `write` overwrote ~1.2 MB of historical release data spanning 2026-03-13 to 2026-05-09.
- **Why**: `write` creates/overwrites files. `edit` is the correct tool for appending to existing files.
- **Recovery**: No backups, no git history, no Time Machine snapshots. Data is permanently lost.
- **Lesson**: When appending to existing files, ALWAYS use `edit`, NEVER `write`. Double-check the tool name before calling.

### Release Director progress
- Simulator booted, app launched, deep link to auth screen works.
- Login screen reachable with all expected UI elements.
- AppleScript cannot inject text into React Native TextInput in iOS Simulator.
- Blocker: need proper UI automation (Detox/XCUITest/Appium) or manual operator test.
- RC verdict: NO-GO. Shipping: NOT YET SHIPPABLE.
