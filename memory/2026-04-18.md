# 2026-04-18

- Synced the local backend e-mail configuration from Fly app `tdf-hq` into ignored local env files: `tdf-hq/config/default.env` and `tdf-hq/.env`. Values include `HQ_APP_URL` and the Fly SMTP vars (`SMTP_HOST`, `SMTP_PORT`, `SMTP_FROM_NAME`, `SMTP_USERNAME`, `SMTP_PASSWORD`, `SMTP_FROM`, `SMTP_TLS`), stored shell-quoted and not printed.
- Tightened local env file permissions to `0600` and cleared the temporary Fly env dump at `/tmp/tdf-hq-fly-email.env`.
- Tried a direct local SMTP test from the synced env; local outbound TCP to the configured SMTP port `587` was unreachable, so no local message could be sent from this environment. Sent a controlled provider-side SMTP test from the Fly `tdf-hq` machine using its deployed `SMTP_*` env vars; the SMTP transaction returned `REMOTE_SMTP_SENT`.
- Checked common local SMTP ports against the same configured provider host: `25`, `465`, `587`, and `2525` were all unreachable from this local environment. Local SMTP is blocked here independently of the app config.
- Verified the deployed backend route with a controlled `POST https://tdf-hq.fly.dev/admin/email-test` call, authenticated with the seeded admin bearer token and addressed to the deployed `SMTP_FROM`. The route returned `status=sent`. Cleared `/tmp/tdf-hq-email-test-recipient.txt` afterward.
- Revoked production seeded access in `tdf_hq`: deactivated the known static API tokens (`admin-token`, `manager-token`, `reception-token`, `accounting-token`, `scheduling-token`, `packages-token`) and the matching seeded username/password credentials (`admin`, `manager`, `reception`, `accounting`, `scheduling`, `packages`). Also revoked active API/session tokens for those seeded parties, including tokens minted by previous `password-login:admin` checks. Verified there are zero active tokens for the seeded parties, and `admin-token` plus `admin/password123` now return HTTP `401`. Production env currently has `SEED_DB=false` and `RUN_MIGRATIONS=false`.
- Hardened `TDF.Seed` so hosted/production runtimes skip static demo API tokens/passwords and default-password core staff credentials even if database seeding is accidentally enabled later. Added focused tests for the new env guard and verified with `stack test --fast --jobs 1 --test-arguments "--match seededCredentialSeedingAllowed"`: 3 examples, 0 failures. The first parallel build attempt failed because GHC exhausted temp disk space; single-job compilation passed.
- Deployed the hardened backend to Fly app `tdf-hq` with image `diegueins680/tdf-hq:0894ec193f717eeeddb02d024fb37863f1911c96`. Docker Desktop was wedged at first and had to be restarted; after the cold Docker/Stack build, Fly rolled machine `1850776a6d9d18` to version `1132` in `ord`, smoke checks passed, DNS verified, and `flyctl status --app tdf-hq` showed the machine started with that image.
- Added a backend startup guard in `TDF.App.Boot`: if `SEED_DB=true` is set in a hosted or production-like runtime, startup now fails before opening the server instead of mutating production data. Added focused tests for local/disabled/hosted-production cases and verified with `stack test --fast --jobs 1 --test-arguments "--match validateSeedDatabaseStartup"` and the related seeded credential guard test.
