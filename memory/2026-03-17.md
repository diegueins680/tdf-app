Continuous improvement loop recovery:
- Loop previously exited after pushing `6bda372` because `waitForGreenCi` let a transient GitHub `fetch failed` error abort the process while polling checks.
- Fixed `scripts/continuous-improvement-loop.mjs` to retry transient GitHub polling failures until the CI timeout, preserve poll errors in the CI report, and avoid crashing when diagnostics fetches fail.
- Added regression coverage in `scripts/__tests__/continuous-improvement-loop.test.mjs` for a transient polling fetch failure.
- Committed and pushed `7cc5220` (`fix: retry transient GitHub CI polling failures`).
- Restarted the CI-enabled loop in persistent session `50699` with `node scripts/continuous-improvement-loop.mjs --config scripts/continuous-improvement-loop.codex.json --allow-dirty`.
