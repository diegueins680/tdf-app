# 2026-03-31

- Resolved the merge conflict for PR #125 (`fix: surface smtp delivery failures`) on branch `codex/service-marketplace-admin-users`.
- Merged `origin/main` into the PR branch as commit `675d804d`.
- Preserved both sides of the password reset merge in `tdf-hq/src/TDF/Server.hs`: the new resolved-recipient-email flow from `main` and the SMTP failure logging from the PR branch.
- Verified the merged backend state with `stack test tdf-hq:test:tdf-hq-test` from `tdf-hq/`; it passed with 127 examples and 0 failures.
- After pushing, GitHub recomputed PR #125 as `MERGEABLE`; Cloudflare Pages was still in progress immediately afterward.
- Implemented a local-first "Momentos del evento" MVP in `tdf-mobile` on top of `app/eventDetail.tsx`, including image/video posting, reactions, comments, featured moments, and contact-connection CTA.
- Added typed storage and sanitization logic in `tdf-mobile/src/lib/eventMoments.ts` with focused tests, plus an `EventDetail` integration test covering the new moments tab.
- Verified the mobile changes with `npm test -- --runInBand __tests__/eventMoments.test.ts __tests__/EventDetailMoments.test.tsx`, `npm run typecheck`, and `npm run lint` from `tdf-mobile/`.
- Continued the feature by adding shared Event Moments backend support in `tdf-hq` (routes, DTOs, Persistent models, handlers, and validation helpers) plus Hspec coverage for the new moment normalizers.
- Connected `tdf-mobile` to the new `/social-events/events/:eventId/moments` API through `src/api/events.ts` and a new `src/lib/eventMomentsRepository.ts`, keeping local drafts as fallback/merge behavior when the backend is unavailable.
- Verified the end-to-end implementation with `stack build --fast`, `stack test --fast` in `tdf-hq`, and `npm test -- --runInBand __tests__/eventMoments.test.ts __tests__/eventMomentsRepository.test.ts __tests__/EventDetailMoments.test.tsx`, `npm run typecheck`, `npm run lint` in `tdf-mobile`.
