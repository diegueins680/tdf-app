# 2026-04-15

- Added `AI_WORKFLOW.md` as the canonical repo guide for AI-assisted development, including safe loop usage, API contract ownership, and verification commands.
- Added `npm run ai:doctor` via `scripts/ai-workflow-doctor.mjs` to check workspace memory files, core CLIs, GitHub auth, git state, backend/mobile test availability, and risky loop config like pushing to `main`.
- Updated `.github/copilot-instructions.md`, `DEVELOPMENT.md`, `CONTRIBUTING.md`, and `README.md` to remove stale AI workflow guidance, especially around OpenAPI ownership and current backend/mobile test reality.
- Hardened the continuous improvement loop so it inherits the current branch by default and refuses to target `main` unless `allowPushToMain` or `CONTINUOUS_LOOP_ALLOW_PUSH_TO_MAIN=1` is set explicitly.
- Changed the default unattended loop config to keep `pollGitHub` off until GitHub auth is intentionally configured, and hardened token lookup so stale `GH_TOKEN` values are ignored instead of poisoning local polling.
- Added one-command toggles for loop GitHub polling via `npm run loop:enable-ci-polling`, `npm run loop:disable-ci-polling`, and `npm run loop:ci-polling-status`.
