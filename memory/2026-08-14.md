# 2026-08-14

- Diego reported that `tdf-app.pages.dev` rendered a blank page. The fatal error was the catalog UI calling `.trim()` on missing `localeId`/`currencyId` fields returned by the still-legacy production backend; `/catalogs/batch` also returned 404 because the frontend and backend releases are out of sync. The `window.cardano` errors came from competing wallet extensions and were incidental.
- Implemented the narrow frontend compatibility hotfix on `hotfix/hq-ui-legacy-preferences-20260814`, commit `02eb8a2c8`: preference normalization now accepts the legacy code-only payload and malformed/missing fields without crashing. Added two regression tests. Targeted tests, full UI typecheck, lint, production build, and the initial-bundle budget/secret guard passed.
- After explicit authorization, opened PR #148 and fixed its only initial CI failure by removing irrelevant hard-coded supported-locale/currency arrays from the regression fixture in commit `1fa88bdd1`. The catalog-list audit, full UI suite, safe-install build, repo quality, API contracts, migration guard, Cloudflare preview, and Vercel preview all passed for the final SHA. `tdfrecords` independently approved it; auto-merge produced `14ef28bb0d7ce61cb1bb78b5c202ed81aa221e70`, and Cloudflare successfully deployed the production bundle `index-wXfBeaXO.js`.
- Post-deployment console evidence confirmed the fatal `.trim()` error is gone. Remaining 404s (`/catalogs/batch`, catalog country/genre items, radio auto-stop options, and records feed) are nonfatal probes from the newer UI against backend `d10e7cdf`, which still lacks those routes. The Cardano property errors remain external wallet-extension injection conflicts; the repository does not assign `window.cardano`.
- A las 09:32 Diego ordenó continuar el piloto de Instagram `Tu escena, conectada`. Se respetó la ventana de 10:00–18:00 y no se enviaron mensajes antes de las 10:00. Kino, Las Abejas y Tentáculos volvieron a revalidarse como activos, pertinentes, sin señales de minoría y sin coincidencia exacta en CRM; el estado de la cuenta seguía completamente en verde.
- Los tres recorridos técnicos se recuperaron: Kino abrió una solicitud normal con compositor vacío, Las Abejas abrió un hilo vacío sin repetir `Something went wrong` y Tentáculos abrió su hilo exacto con compositor habilitado junto al mensaje no compatible de 2023. Los enlaces aprobados cargaron TDF. Primary mostró nuevos Me gusta de Jorge Molina y Kirmana Records; General y Requests no aportaron respuestas nuevas del piloto y Requests estaba vacío.
- Entre las 10:03 y las 10:09 se enviaron y confirmaron los tres mensajes pendientes: Kino, Las Abejas y Tentáculos. Se creó a Kino como persona y a Las Abejas y Tentáculos como empresas; las búsquedas exactas por `kinocavalier`, `lasabejasclubdevinilos` y `oximoron_tu_tentaculo` devolvieron una sola coincidencia cada una.
- Tentáculos respondió inmediatamente `Hola`; se registró como respuesta textual neutral/abierta y no se envió seguimiento sin aprobación nueva. La revisión final dejó General y Requests sin actividad adicional y no mostró CAPTCHA, advertencias, restricciones, objeciones ni duplicados.
- La primera entrega de alcance de `Tu escena, conectada` quedó completada: 20 de 20 destinatarios contactados, 20 DMs confirmados, 19 Instagram exactos en CRM —solo HNO permanece ambiguo—, 4 respuestas de texto (3 positivas y el saludo de Tentáculos), 3 reacciones positivas, 0 solicitudes de no contacto, 0 menores, 0 conversiones atribuibles y 0 publicaciones del video.
- Se diagnosticó el aviso falso de servicio no publicado en `/domo-del-pululahua`: el backend productivo `d10e7cdf` devuelve un arreglo legado en `/services/catalog/public` y espera `pbServiceType` en `/bookings/public`, mientras la UI desplegada espera el sobre/UUID nuevo. Se añadió un adaptador bidireccional limitado a la API pública y se preservó la corrección del doble cobro de uso del Domo; pasaron 16 pruebas dirigidas, typecheck, lint, build Vite y el presupuesto inicial (403.197/409.600 bytes gzip). Durante la validación, un segundo supervisor automático activo desde el 11 de agosto (`PGID 19925`) hizo checkpoint, merge y push no solicitado a `main` como `b8b5d927b`, incluyendo estos cambios y muchos archivos ajenos; Cloudflare, Vercel y `ui-quality` fallaron por accesos TypeScript incompatibles con `noPropertyAccessFromIndexSignature`, y el auditor falló porque el refactor no trasladó dos decisiones ya revisadas. El supervisor completo quedó pausado reversiblemente con `SIGSTOP`. Las correcciones de TypeScript y de los dos IDs del auditor permanecen locales, sin commit ni push; producción continúa sin cambios.
- Con autorización explícita de Diego se recuperó `main` mediante PR #150 y, tras confirmar que el único bloqueo era la aprobación externa requerida, se aplicó el override administrativo autorizado. El PR quedó fusionado en `91f8a5878d633fe0b1719898e99b16ed1dfc9946`; Cloudflare, Vercel y todo CI, incluido el build/push de la imagen Docker, terminaron en verde. Una prueba Playwright contra producción confirmó catálogo público `200`, cotización inicial correcta de `$3.002` sin la línea duplicada, y un payload legacy correcto con `pbServiceType: "Producción de eventos"`; el POST se interceptó y simuló, por lo que no se creó ninguna reserva real. El backend activo sigue en `d10e7cdf9429e548aefaa60708610cc8a71e80bd`; los 404/401 restantes son probes globales no relacionados con el flujo Domo. El supervisor automático continúa pausado.
- El cutover de esquema de catálogos quedó listo en PR #151, rama `fix/catalog-production-schema-cutover-20260814`, SHA `712d4a9ca75a77047b665e41362570d605e2316e`. Todos los checks de ese SHA pasaron y auto-merge está habilitado con método `MERGE`, sin bypass, a la espera de la aprobación independiente de `tdfrecords`. Dos observaciones P1 de Codex se corrigieron: el release aborta si un `introducedBy` no es ancestro (ya no omite migraciones silenciosamente) y el lote expandido de 1.320.000 bytes se transmite a `flyctl ssh console` por stdin en vez de argv; 29 pruebas de release y la auditoría de 658 listas pasaron. Producción sigue sin escrituras ni despliegue: falta confirmación independiente del login de administrador de emergencia de Juan y luego un snapshot nuevo.
