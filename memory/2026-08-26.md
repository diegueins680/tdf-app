# 2026-08-26

- The cross-repository branch sweep found root, mobile, and legacy UI already had every remote side ref contained by `main`, but legacy backend TDF still had conflict-blocked PR #76 plus two unique refs. PR #76 was reconciled without changing the current tree and merged as `1fe4957ad36ae8f27dc9afe4bc6c7bb6e3f70c34`. The fan-club live-broadcast branch then absorbed the historical auth-test ref, repaired its README/test conflicts and Cabal registration, passed a full Stack build with 29/29 tests, and merged through PR #85 as `ad2c9d060fc2411db92e1641cb19c03f801f7323`.
- Root PR #208 preserved the release and Quito campaign operating record. Codex correctly flagged that its first head contained exact CRM/Instagram prospect identities. Those identifiers and per-contact tracking values were removed from both the report and tracked memory, the unmerged branch commit was rewritten, and exact redacted head `5e6598355b283c362efd312e5924ac644b20103b` passed every hosted check and a clean Codex re-review before merging as `14e3d6294560e537d772d45b30e520e54d5e19f0`.
- The 2026-08-25 release artifact proved the rollback runner supplied a correct single Docker Hub digest to `flyctl machine update`, but Fly rewrote it internally as `repo@digest@digest`. Root PR #209 exported one shared guarded deploy-argument builder and moved runtime rollback onto the same `flyctl deploy --only-machines` lane that had already succeeded for canary rollout. Exact head `35151bedf07b121a2968a82f5d6c8b67d3b0bf6c` passed 46 release tests, the complete repository gate, release-plan dry run, all hosted checks, and a clean Codex review, then merged as final root `main` `cf25a507cd05b6e9d64a02e8df57afd56704fa61`.
- Final-main Datadog run `32934079118`, CI Safe Install `32934079131`, and Catalog authority `32934079133` all passed. The exact merged tree passed the 46/46 release suite locally. Root, mobile, TDF, and TDF-ui each have zero open PRs, and every remaining remote side ref is an ancestor of its repository main. A read-only Fly inspection showed both production Machines still healthy on the previously authorized `6afd633e...` digest with discovery enabled and auto-publication disabled. No deployment, migration, external message, campaign activation, or production mutation occurred during this continuation.
- A follow-up local-ref audit found two mobile refs, one backend metadata ref, and two refs in the ignored legacy UI sandbox that were not yet ancestors of their repository mains. Their current or superseded intent was reconciled in clean worktrees: mobile PR #34 merged as `84f4b86eed0634935360ee88cb58995bc752a08c` after lint and 296/296 Jest tests; backend PR #86 merged as `b872c1eaf8004b4e18c876c952bfdfd89db9b691` after 29/29 Stack tests; and legacy UI PR #99 merged as `769272f307de3ff32721c653790f9077bcace042` after a production build and 6/6 Vitest tests. Every local and remote branch ref is now contained by its repository main. The root mobile gitlink was advanced to the ancestry-only mobile merge commit; its mobile tree is unchanged.
- A wider worktree audit found one remaining genuinely unfinished branch: `chore/mobile-release-infra` held substantial uncommitted native and release-infrastructure work. Its exact state was checkpointed, reconciled by ancestry onto current mobile `main`, and modernized with official Expo SDK 54 dependency alignment, pinned Expo Doctor, deterministic release-asset validation, pull-request/main validation, and an explicitly opt-in EAS readiness workflow. Mobile PR #35 merged as `a7af68f0ecccc4a7e96a07cfb87a444691b08145` after the release gate, 59 Jest suites/296 tests, Expo Doctor 17/17, Expo dependency checks, and a full Android release build (818 tasks). No EAS build, deployment, submission, or production mutation was started.
