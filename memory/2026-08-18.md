# 2026-08-18

- Cierre del barrido de ramas listas: #160 quedó fusionada como `5d211d14665629984007daef81c28d3f5f35116d` tras aprobación exacta de `a8e6751cc8425e8e63b3f8ab6c32cc8453aeb23d`; luego #163 recibió aprobación exacta de `172b37422a5cab3e40cc82c2537e360212dd953e` y otra ejecución la fusionó como `c31d979b8c2145f01171cac457f4e346311d505b`, con padres `5d211d1...` y `172b374...`. `origin/main` termina en `c31d979...`; ambos merges están firmados por GitHub.
- CI exacta de #163 terminó verde: backend 18m32s y quality final verde. El Build Image autoritativo de `c31d979...`, run `32099999365`, terminó completamente verde a las 04:59:43Z: backend 17m46s, quality y Docker pass; publicó `tdf-hq:c31d979b8c2145f01171cac457f4e346311d505b` con índice OCI `sha256:e5a4bc4e0953d60f362786deba6a96ce08b00e83bd6268fd0947b9a1441b5d97`. El build anterior de #160 fue cancelado por concurrencia al quedar supersedido, después de pasar sus checks preliminares. No se desplegó la imagen.
- Barrido final: raíz #164 está draft/review-required aunque mergeable; #149 está aprobada pero en conflicto; TDF-mobile#24 sigue draft/mergeable; TDF-ui#94 sigue en conflicto. No queda otra PR elegible para fusión y no se omitió ningún gate.
- Diego confirmó explícitamente que es mayor de edad y autorizó registrar la auto-declaración `adult_attested` de su cuenta administrativa Party `9`. Después de un ensayo íntegro con rollback, se aplicó una sola transacción productiva auditada que creó y publicó tres perfiles del Directorio Musical administrados por esa cuenta: `Diego Saá` como persona/instrumentista con Bajo profesional (`diego-saa-bajista`), `TDF Records` como estudio con cinco servicios de grabación/mezcla/mastering (`tdf-records-estudio`) y `Domo del Pululahua` como venue con producción de eventos (`domo-del-pululahua`). Los tres usan ubicación pública aproximada Quito, completitud `0.85`, tres grants completos y seis eventos de auditoría. La API pública confirmó una coincidencia única a 25 km para `bajista`, `estudio de grabación` y `Domo del Pululahua`; los tres detalles públicos responden y `/health` terminó `db/status=ok`. No se desplegó código ni se alteró el checkout raíz.
- Diego pidió activar migraciones y discovery sobre producción `591abfac...`. Discovery quedó efectivo `true` en ambas Machines con auto-publicación en su default `false`; el slot 00:00 UTC ya estaba completado por Ticketmaster/BuenPlan con 0 vistos/creados/actualizados, así que no hubo duplicado. `RUN_MIGRATIONS=true` ejecutó DDL ORM amplio, mantuvo ambas regiones en `starting` y causó exit code 1/restart en `ord`; se revirtió solo ese flag a `false`, recuperando health/db ok. Ledger final 41, lease 0, `external_event_ref=0` y candidatos de investigación materializados 0.
- El log exacto mostró que Persistent aborta por una operación insegura: intenta `DROP COLUMN ddex_partner.allowed_versions`. La columna fue conservada intencionalmente y vacía como evidencia reversible del cutover DDEX, pero el modelo ya no la declaraba. Se corrigió con un campo `[Text]` `MigrationOnly`, `sqltype=text[]` y el default existente, sin reintroducirlo en tipos/writers de aplicación. Rama `agent/fix-persistent-ddex-migration-only`, commit `400e06e62b2fabf8726408d7981cdba1145c4a05`, PR #165.
- CI de #165 quedó completamente verde: `backend-quality` 18m37s, quality final, production-migrations, contratos, repo y auditoría de catálogos. La compilación local atravesó correctamente el modelo DDEX/Template Haskell y luego falló en ensamblado por disco lleno; se limpió exclusivamente el `.stack-work` reconstruible generado por esa validación. #165 quedó ready, mergeable y con revisión solicitada a `tdfrecords`, pero sigue `REVIEW_REQUIRED` sin reviews. No se habilitó auto-merge, no se construyó/desplegó una imagen nueva y no se volvió a activar migraciones; el canary aislado espera aprobación exacta del SHA.
- Seguimiento de privacidad para discovery: PR #166 (`fix(events): preserve refunds for hidden imports`) quedó en SHA exacto `d4354c0363717f803122a99826baa565cb676a72`, mergeable y con toda CI verde. Corrige el P1 post-merge de #163 para conservar el ciclo de reembolso del comprador cuando el proveedor oculta el evento, y cerró dos P2 posteriores: validaciones de órdenes ajenas/de otro evento ya no filtran existencia y órdenes pendientes/canceladas ya no conceden acceso al listado oculto. Validación local exacta: build Stack, regresión focal 1/1, Haskell 2.298/2.298, release 39/39, CI 13/13. Sigue `REVIEW_REQUIRED`; se renovó la solicitud a `tdfrecords` sin usar bypass, auto-merge ni resolver hilos.
- Preflight read-only de producción durante la espera: `/health` está `db/status=ok`; `/version` reporta `591abfacddd8a3acfbe117421efe65382fa73252`; las dos Machines están iniciadas con `RUN_MIGRATIONS=false` y `EVENT_DISCOVERY_ENABLED=false`; el secreto de Ticketmaster existe. La BD tiene `external_event_ref=0`, cero eventos canónicos importados/futuros, Ticketmaster y BuenPlan habilitados sin fallos, y suscripciones activas a `QUITO/EC` (9) y `Helsinki/EC` (1). El feed público de BuenPlan sí contiene decenas de eventos futuros de Quito, incluidos SHOW DE METAL SINFÓNICO (2026-08-22), Mar Monzon (2026-08-22), IV SISM Quito (2026-08-24) y Erick Brian (2026-08-27). No se mutó producción: el despliegue y la importación/publicación siguen detenidos hasta aprobación exacta de #166.
- Follow-up del Directorio Musical: la PR raíz #167 quedó abierta en borrador desde `c31d979...`, head `fdf5273e949f8f13f4a58a6f7940ec9990e2a94f`, y TDF-mobile#24 avanzó a `5a1e1b07e0e72c6ab936d7e0e9c026094717e1e9`. Se añadieron ocho capturas reproducibles, incluidos editores autenticados web/Expo Web con fixtures sintéticos, Axe 7 superficies/189 passes/0 violaciones/0 browser errors, y se corrigieron almacenamiento web de sesión, bucle idempotente de deep links, etiquetas y contrastes accesibles. La CI exacta de #167 quedó completamente verde; ambas PR siguen draft/review-required. No se fusionó ni desplegó.
- Campaña `Tu escena, conectada`: a las 08:00 el turno aislado `52912461-9e27-4f9a-b872-ba8c9112e691` seguía habilitado para las 10:00, pero el perfil persistente `tdf-instagram` continuaba ocupado por el mismo Chrome/Playwright y su cadena llega a otra sesión de Codex todavía activa. No se terminó ningún proceso ni se envió antes de horario. Si el perfil no queda libre, el turno debe detenerse sin enviar; cerrar la sesión requiere autorización específica o intervención manual del operador.
- Diego respondió `Hazlo` y autorizó terminar exactamente Chrome PID `70778` y Playwright PID `15552` después de revalidar que eran los propietarios de `tdf-instagram`. Se envió `TERM`; ambos y el wrapper npm padre terminaron limpiamente, sin `KILL` ni tocar la sesión de Codex. El transporte visible de este turno ya estaba cerrado y no reconectó, por lo que no se abrió otra instancia ni se envió antes de las 10:00. El job aislado permaneció habilitado y debe crear su propio transporte y revalidar autenticación/hilos.
- PR #165 quedó sin conflictos tras fusionar `main` `cc7d64c1f20e2626df53ef32a0e4ee142c0a220b` en la rama y combinar la retención vacía de #164 con `MigrationOnly`; el merge de rama fue `e8f3ebcd4502e823de554f8c140459b319102a77`. La primera CI compiló y pasó 2.298/2.298 pruebas, pero detectó que el verificador aislado omitía `DEFAULT_LOCALE=es`; el follow-up exacto `7684fc760c9bb7f5605c6a8d13413e0665f7c0f5` corrigió solo ese fixture. CI protegida run `32144823574` quedó completamente verde, incluida verificación idempotente de migraciones sobre el esquema productivo; checks locales focales 43/43 y shell syntax verdes. El approval anterior de `tdfrecords` fue descartado correctamente, la solicitud exacta sigue vigente, no hay threads, y la PR permanece abierta/mergeable bloqueada únicamente por aprobación humana nueva. No se hizo bypass, merge final, despliegue ni mutación de producción. Para liberar el commit se eliminó exclusivamente el `.stack-work` reconstruible generado por esta validación, que había agotado el disco; el checkout raíz sucio quedó intacto.
- A las 08:14 ya no existía ningún proceso Chrome usando el directorio `tdf-instagram`; solo quedaban wrappers MCP de otras sesiones, sin el bloqueo efectivo del perfil. No se terminaron esos procesos. Los cinco seguimientos siguen pendientes y el próximo cambio autorizado es el turno aislado de las 10:00.
- Ingesta de eventos: el piloto administrativo continúa aprobado y limitado a 20 candidatos (8 alta confianza/borrador y 12 media/revisión), pero producción no tenía un endpoint idempotente que materializara candidatos preservando vínculos, referencias externas y auditoría. El preflight productivo de solo lectura confirmó 0 enlaces de evento y 0 referencias para los ocho seleccionados, 0 duplicados secundarios, un único tipo activo `concert`, y que harían falta 4 venues y 11 artistas no reclamados. No se escribió ningún evento ni entidad.
- Se implementó el gap mínimo en `codex/event-research-materialization`, commit `a2a87ffad4ae0cd2f8e501d32cb9061ed465cbe6`, y se abrió el draft PR #170. El endpoint estricto exige ejecución abierta y piloto aprobado, acepta fin nulo, resuelve entidades sin adivinar, crea evento/referencias/vínculo/auditoría en una transacción y devuelve el resultado original en reintentos sin sobrescribir ediciones manuales. Verificación: research 12/12, suite completa previa al rebase no solapado 2.305/2.305, UI typecheck verde, release 43/43, CI pipeline 14/14 y diff limpio. La recompilación completa posterior al rebase se detuvo por APFS lleno (134 MiB), no por error de fuente; GitHub CI de #170 quedó en curso. No se fusionó, desplegó ni materializó: siguiente checkpoint requiere revisión/merge/despliegue expresamente autorizados y luego ejecución por lotes de los ocho candidatos.
- Diego aprobó corregir el primer fallo de CI de #170. Se sustituyó solo la huella obsoleta del registro técnico `EventResearchAPI`, conservando clasificación y justificación; auditoría local y remota verdes. Commit/head nuevo `4fceea1fa97dbe44a2dcaba44aa4b0e8f552dad0`. En CI pasaron build/test backend completo, UI, repositorio, contratos, catálogo, Vercel y Cloudflare; el job backend falló después de aplicar/verificar las 42 migraciones porque el proceso aislado omitió `DEFAULT_LOCALE=es`. Esa corrección ya existe en #171 (`fb06c6c...`), con CI totalmente verde y review solicitado a `tdfrecords`, pero sin aprobación/merge. No se duplicó el fix, no se marcó #170 ready y no hubo merge, despliegue ni escrituras productivas. Checkpoint: esperar #171, rebasar #170 y repetir gates.
- Foto del perfil público: se recuperó la foto escénica ya guardada en `artist_profile.hero_image_url` de Party 9 (la imagen de `fan_profile` era el logo de TDF), se convirtió sin recorte a WebP de 64 KiB y se implementó en la rama local aislada `fix/directory-profile-images-20260818`. Commits rebasados sobre `cc7d64c1f`: `f1665e92c` muestra imágenes accesibles en tarjetas lista/cuadrícula y en el detalle/metadata social, sirve el asset y añade la migración idempotente/rollback para proyectar la primera imagen segura del portafolio; `ca2247aaf` registra la migración automática con SHA inmutable. Pasaron prueba UI+accesibilidad, TypeScript, ESLint, Vite, bundle 406727 bytes gzip, release 40/40 y validación transaccional productiva con rollback. La rama está limpia y no se empujó ni desplegó: falta autorización para publicar/PR/release y luego asociar el URL del asset al portafolio de `diego-saa-bajista` en producción.
- Continuación directa de #165: se fusionó `origin/main` `cc7d64c1f...` en la rama sin force y la resolución combinó el default vacío/SQL revisado de #164 con `MigrationOnly`; head final `7684fc760c9bb7f5605c6a8d13413e0665f7c0f5`. El primer CI exacto compiló y probó todo el backend, aplicó/verificó el bundle DDEX y expuso únicamente que el harness omitía `DEFAULT_LOCALE=es`; el commit `7684fc760` añadió esa variable. El run protegido `32144823574` terminó totalmente verde, incluida la prueba productiva completa e idempotente. PR #171 ya contenía concurrentemente el mismo fix aislado más una aserción preventiva y también está totalmente verde. Ambas PR siguen `REVIEW_REQUIRED` con solicitud activa a `tdfrecords`; se dejó una sola nota en #165 pidiendo reaprobación exacta. No se hizo merge, snapshot ni mutación Fly: producción continúa sana en `591abfac...`, discovery efectivo `true` por secreto y Persistent `RUN_MIGRATIONS=false`.
- Continuación del rollout de discovery: `main` avanzó a `cc7d64c1f` con #164, #167 y #168. Las ramas #165 y #166 se reconciliaron sin force a `e8f3ebcd4502e823de554f8c140459b319102a77` y `612c2e25a146804c10052c786ab6a05dd7ce0916`; #166 conserva el filtro SQL estricto y los dos cierres de side channels. La validación local exacta de #166 pasó Haskell focal 1/1 y release 43/43. La CI de ambos falló únicamente porque el fixture PostgreSQL omitía `DEFAULT_LOCALE=es`, no por los cambios DDEX/eventos.
- El fix aislado del fixture está en PR #171 (`fix(ci): align migration startup locale`), SHA `fb06c6c2971bdcf83025687e4dc40036613ee31a`: solo añade `DEFAULT_LOCALE=es` al test de esquema productivo y una aserción preventiva. Toda su CI quedó verde, se marcó ready y se solicitó revisión a `tdfrecords`; permanece `REVIEW_REQUIRED`. Producción sigue sana en `591abfac...`, dos Machines iniciadas y discovery/migraciones en false; no se desplegó, importó ni publicó nada durante esta espera.
- Campaña `Tu escena, conectada`: el turno aislado de las 10:00 falló en 3,133 segundos por saldo insuficiente del proveedor Anthropic, antes de abrir navegador o enviar. La continuación directa autenticó `tdf.records.label`, confirmó estado de cuenta verde y revalidó una ficha CRM exacta, sin correo ni usuario, además del hilo visto y sin respuesta/baja para cada uno de los cinco candidatos. Se enviaron y confirmaron los seguimientos finales aprobados a `teez__t` (10:08), `el_rovlex` (10:08), `jam_estepez` (10:11), `planet_303` (10:12) y `federico__molinari_` (10:13). Primary confirmó los cinco; Rovlex reaccionó con ❤️ y empezó a seguir a TDF, sin respuesta de texto. No hubo CAPTCHA, advertencia, restricción, duplicado, no-contacto ni conversión nueva; el estado posterior siguió verde y Chrome se cerró limpiamente. El bloque alcanzó 5/5 y este grupo no debe recibir más seguimientos por silencio. Tentáculos sigue pendiente de disponibilidad/capacidad; no se llamó ni se escribió por WhatsApp.
- Revisión posterior de campaña: Rovlex reaccionó con 🔥 a una historia de TDF y EL DOMO Pululahua mencionó a TDF en una historia con ❤️; no hubo respuestas textuales nuevas. General y Requests no mostraron actividad del piloto. No se respondió a esas señales sin un texto aprobado ni se enviaron mensajes adicionales.
- Seguimiento del Directorio tras el merge de #167 (`cc7d64c1f`): el review automatizado detectó que las capturas autenticadas por alcance sobrescribían los JSON agregados de accesibilidad/errores. Se corrigió en la rama `fix/music-directory-scoped-evidence-20260818`, commit `1faea5f395defb0a34d272a3d574b7b7be361b82`, draft PR #172: las ejecuciones completas conservan los artefactos canónicos y `web-managed`/`mobile-managed` escriben archivos propios; regresión 2/2, `quality:repo` completa, catálogos y sintaxis pasaron localmente. El intento E2E Android nativo arrancó Metro y la app `com.tdf.records`, pero el AVD quedó bloqueado por ANR de Pixel Launcher/SystemUI; no se declaró un pase falso ni se guardaron esas capturas. El único fallo de CI de #172 fue el verificador backend ya conocido: 2.298/2.298 pruebas pasaron y el proceso abortó porque el harness no fijaba `DEFAULT_LOCALE=es`. PR #171 se fusionó después como `bbd296097163d433ffa93470dd2a52ae9ca85cad`, ahora HEAD de `main`, y su `backend-quality`/CI completa están verdes. Siguiente paso para #172: con autorización explícita, sincronizar `main` sin duplicar el fix y esperar nuevamente todos los checks; no se fusionó ni desplegó #172.
- Diego autorizó la sincronización y supervisión de #172. GitHub incorporó `main` `bbd296097...` mediante un merge no destructivo; head final remoto/local `293f08b45217385297d98a287ee07be11f1ea7f6`, checkout limpio. La CI protegida run `32151702371` quedó completamente verde: backend 19m03s con suite y preflight automático/idempotente de migraciones, UI 7m07s, contratos, móvil, repositorio, catálogo, migraciones, builds, Vercel y Cloudflare; agregador `quality` verde. Revalidación local post-sync: artefactos 2/2 y aserciones CI 7/7. El cuerpo del PR documenta SHA, run y gates. #172 permanece draft, mergeable y `REVIEW_REQUIRED`; `BLOCKED` corresponde solo al gate esperado. No se marcó ready, fusionó ni desplegó. Mientras corría, `main` avanzó independientemente a `e348e766...` por #165, sin conflicto con #172.
- Barrido posterior de ramas listas: #165 recibió aprobación exacta y fue fusionada como `e348e7662ce2b04e5b66496fc9765041df01fe29`; #166 fue fusionada después como `18feaca0fdda1ee1c0c967c10e2f51eb6c340d00`, cuyos padres son #165 y el head exacto `95a4075422259197a62606bc65ae2038f7969ac3`. La CI previa de #166 había fallado únicamente al arrancar el backend de verificación con `Persisted deployment locale default does not match DEFAULT_LOCALE`; las migraciones y el esquema ya habían pasado. El merge posterior hereda la corrección verde de #165, por lo que `main` sí fija `DEFAULT_LOCALE=es`. Como #171 también había aterrizado la misma línea, quedó duplicada de forma inocua; PR #174 (`fix(ci): dedupe migration locale fixture`) elimina el duplicado y sigue en CI/revisión. PR #173 está completamente verde y mergeable pero `REVIEW_REQUIRED`; #172/#170 y TDF-mobile#24 son drafts, #169/#149 y TDF-ui#94 tienen conflictos. No se usó bypass ni se desplegó; el Build Image exacto de `18feaca...` quedó en curso.
- Diego ordenó resolver, fusionar y continuar con #169. La reconciliación exacta quedó publicada concurrentemente como `c61508cca88e054119a45e76ff3dff11e77c0e22`, basada directamente en `main` `18feaca...`: conserva el filtrado anti-enumeración de #166, habilita moderación de reembolsos de eventos importados ocultos solo para administradores estrictos y descarta el follow-up de locale ya supersedido por #171. La comparación del árbol local preparado fue byte a byte idéntica; regresión Haskell focal 1/1 y `diff --check` verdes. GitHub reporta la PR mergeable, sin hilos y con toda la CI exacta verde, pero descartó correctamente la aprobación anterior de `tdfrecords` al cambiar el SHA. Ya hay solicitud fresca, mención exacta y auto-merge `MERGE` armado; tras varios sondeos sigue `REVIEW_REQUIRED`. No se usó bypass, no se fusionó todavía y se eliminó únicamente el worktree/rama temporal redundante creado para la resolución.
- El Build Image autoritativo de #166, run `32152782059`, terminó completamente verde para `18feaca0fdda1ee1c0c967c10e2f51eb6c340d00`: suite/backend, migración automática sobre el esquema productivo, quality y publicación Docker pasaron. El preflight productivo read-only quedó `ready`; verificó ambas Machines iniciadas y sanas sobre el rollback `591abfac...`, `RUN_MIGRATIONS=false`, `EVENT_DISCOVERY_ENABLED=false`, `DEFAULT_LOCALE=es`, Ticketmaster configurado, readiness de seguridad/BD y la imagen inmutable `diegueins680/tdf-hq@sha256:0a7c78b74a0586622c2bd171e66c46f1208139e9dd770444f21f0b1f53053a9d`. El primer intento coincidió con un reemplazo/reinicio transitorio del fleet existente y falló cerrado; tras estabilizarse ambas Machines, el segundo pasó. No se desplegó ni se activó discovery: el siguiente paso mutante requiere autorización exacta para `18feaca...`.
- Diego autorizó el despliegue exacto de `18feaca...`; el release script volvió a fallar cerrado en su preflight antes de mutar porque el estado productivo cambió externamente: ambas Machines ya usan la imagen exacta `18feaca...`, pero `0807ee9cd34668` tiene `EVENT_DISCOVERY_ENABLED=true` y `3d8d2500c67168` `false` (la configuración declarada también quedó divergente). No se sobrescribió la bandera ni se forzó el canary; queda pendiente decidir si se normaliza discovery a `false` en ambas Machines y se reintenta el rollout.
- Diego pidió habilitar discovery. La bandera se persistió correctamente con el secreto Fly `EVENT_DISCOVERY_ENABLED=true` y rolling update de ambas Machines; las dos reportan efectivamente `EVENT_DISCOVERY_ENABLED=true`, `RUN_MIGRATIONS=false`, `AUTO_APPLY_PRODUCTION_MIGRATIONS=true`, `DEFAULT_LOCALE=es`, y health pública `db/status=ok`. Mientras tanto otra automatización avanzó producción a `1b2d7197d16bab229899be4813dd1318fde18b54` (main ya `6e5a8c5e...`), por lo que no se forzó el SHA antiguo `18feaca...`. No se lanzó ingesta/importación; discovery quedó activado de forma persistente.
- Diego ya había autorizado explícitamente snapshot, rollout y cutover; se completó el release productivo guardado de `18feaca0fdda1ee1c0c967c10e2f51eb6c340d00`. Discovery se pausó, se creó el snapshot fresco `vs_7eo03oxPwZzseeg67qzkybJ` (`created`, digest `dca56cc33daa84639b8ddfff4286fdc578a1bf114f43983857a9980437db6728`, retención 5 días) y la vía checksumada aplicó/verificó el bundle bajo lease, desplegó canary `0807ee9cd34668` y luego `3d8d2500c67168`, sin rollback. Ambas Machines terminan `started` sobre amd64 `sha256:855db4cd0ce0d2ddb25c05eae2de2b39f078e98683d4772e27f472093dcf5185`, `/health` y DB `ok`, `RUN_MIGRATIONS=false`, `AUTO_APPLY_PRODUCTION_MIGRATIONS=true`, y el ledger quedó 42 con lease 0. `ddex_partner.allowed_versions` se conservó como `text[] NOT NULL DEFAULT ARRAY[]::text[]`. Discovery volvió efectivo `true` en ambas; auto-publicación permanece sin definir y por tanto en su default validado `false`. Hay 10 fuentes/2 activas, 10 suscripciones/2 ciudades; BuenPlan y Ticketmaster tienen cero fallos y el último slot 12:00 UTC está completado, por lo que el próximo slot es 18:00 UTC sin duplicar el anterior. El reporte está en `artifacts/releases/production-release-18feaca0fdda1ee1c0c967c10e2f51eb6c340d00-2026-08-18T15-52-22.102Z-9c5c771f-809e-40f4-8e7d-875c2c0845f7.json`.
- Reconciliación posterior del mismo release: una reactivación concurrente dejó temporalmente `ord=true` y `lax=false`; se aplicó un único rolling update explícito con `EVENT_DISCOVERY_ENABLED=true` y `EVENT_DISCOVERY_AUTO_PUBLISH=false`. Ambas Machines quedaron `started/passing` en release 2114, mismo digest/SHA, `RUN_MIGRATIONS=false`, `AUTO_APPLY_PRODUCTION_MIGRATIONS=true`, locale `es`; sus logs confirman `Reviewed production migrations are applied and schema verification passed`. El slot 18:00 UTC terminó `completed` para BuenPlan (run 35) y Ticketmaster (run 36), ambos 0 vistos/creados/actualizados y sin error; `external_event_ref=0` y candidatos de investigación materializados=0. El verificador integral de esquema pasó. Smokes públicos: batch ES de géneros+instrumentos 2 catálogos/revisión 2, ETag `"catalog-2"` y 304 condicional; Records 67 lanzamientos/6 grabaciones/5 sesiones/3 colecciones/revisión 162; búsqueda `bajista` una coincidencia y los tres perfiles públicos responden. Negativos: administración sin token 401 e ID no UUID 400. `main` avanzó después a `1b2d7197...` únicamente por deduplicar una línea del fixture CI; no cambia runtime y su Build Image quedó en curso, por lo que producción permanece intencionalmente en el runtime validado `18feaca...`.
- Continuación de la cola: el Build Image `32170629050` de `1b2d7197...` terminó verde y siguió sin requerir redeploy. PR #172 se actualizó mediante el endpoint seguro sobre `main`, quedó en head `21c68217dcf895eba0d195dce2410e424103b906`, conservó un diff propio de seis archivos/96 adiciones/7 eliminaciones y cero hilos, y pasó CI completa `32171548425` (backend 19m24s, incluida verificación de migraciones automáticas). `diegueins680` aprobó ese head exacto en review `4964636695`; auto-merge lo fusionó como `6e5a8c5e1787fb62b4706f70631f01bda64fa7e9`. PR #170 terminó CI verde pero se le desactivó auto-merge al detectar cuatro hilos vigentes: el guard de candidato piloto ya está en código, mientras el hold de no-publicación y la resolución de artistas en la ruta de `ExternalEventRef` siguen requiriendo cambios/revisión. PR #173 falla determinísticamente porque `DirectorySearchPage.test.tsx` no exporta `getStoredSessionToken` en su mock ESM; se propuso el plan `gh-fix-ci` y no se editó a la espera de aprobación explícita. Producción se revalidó sana y sigue intencionalmente en runtime `18feaca...`.
- Cierre de #165 y su release: #165 ya estaba fusionada como `e348e7662ce2b04e5b66496fc9765041df01fe29`. Al continuar se detectó que el merge concurrente de #171 había duplicado `DEFAULT_LOCALE=es`; se corrigió antes del rollout mediante PR #174, exact head `9adac500e9be64d2c18e7de6432ad1d0d114608c`, CI protegida `32152942115` totalmente verde y merge normal `1b2d7197d16bab229899be4813dd1318fde18b54`. El Build Image manual `32170629050` pasó toda la suite y publicó índice `sha256:6d7c2c4ef99773c80cd6d588cfea9e7144d6d8b392c8fb28f616e83d61738245` / amd64 `sha256:700b8747ea7abfe8090c632a2285564c14ed4e7e0ccd9422c7b42b3163531688`. Se normalizó discovery a `false`, el preflight quedó ready y el release guardado aplicó/verificó el bundle, canary y segunda Machine sin rollback. Ambas Machines terminan `started`, `/health` y DB `ok`, `/version=1b2d7197...`, `RUN_MIGRATIONS=false`, `AUTO_APPLY_PRODUCTION_MIGRATIONS=true`, discovery `false` y lease 0. La migración DDEX ya constaba con checksum correcto desde `18feaca...`; `allowed_versions` sigue `ARRAY NOT NULL DEFAULT ARRAY[]::text[]`. Reporte: `artifacts/releases/production-release-1b2d7197d16bab229899be4813dd1318fde18b54-2026-08-18T20-46-03.086Z-b75fd5dc-5d71-473b-b949-2f0746d503da.json`.
- Resultado del barrido de merges: #172 quedó integrado en `main` como `6e5a8c5e1787fb62b4706f70631f01bda64fa7e9`. El follow-up aprobado de #173 se publicó como `8021446264b0691d9af50d94bbfc94fc28483e78`; test focal, typecheck y build locales pasan, pero la PR sigue CI/revisión pendiente. #175 está aprobada pero mantiene despliegues externos en curso; #169/#170 siguen `REVIEW_REQUIRED`.
- PR #175 terminó mergeada como `7a31b064d1d44368c6bbb9a9fc73cc5b464a5e94`; el `main` remoto quedó en ese SHA.
- Resolución exacta de #175: el cruce de historial venía de una rama reutilizada con múltiples merge bases. El merge `28097c5c4bbc2b2b9cb1aa972f8d9502da3f501f` produjo un árbol idéntico a `main`, pasó todos sus checks, recibió aprobación independiente de `diegueins680` y #175 quedó fusionada normalmente como `7a31b064d1d44368c6bbb9a9fc73cc5b464a5e94`, sin bypass ni cambio de producto. Después se fusionaron concurrentemente #173, #170 y #169, llevando `main` a `7ee411c2792f68a994e67d07805cca825911c02a`; el Build Image autoritativo `32188222944` detectó que el merge de #173 había reintroducido por segunda vez `DEFAULT_LOCALE=es`, por lo que bloqueó Docker antes de publicar imagen. Se abrió el hotfix mínimo #176, head `074e7cef938653cf1343830fc76437bc17f43c63`, que elimina solo la línea duplicada; pasó shell syntax, `diff --check`, test CI 15/15, `quality:repo` local y toda la CI remota, incluido quality final, catálogos, contratos, install safety, Cloudflare y Vercel. Auto-merge protegido está armado y la revisión exacta fue solicitada a `tdfrecords`; sigue abierta únicamente por `REVIEW_REQUIRED`. No se usó admin bypass, no se publicó una imagen nueva y producción no se tocó.
- Continuación de materialización: #170 fue aprobada y fusionada externamente como `0b7ce33f6b67b93e6ad122a2490c01f43b9222ad`; el P1 automatizado posterior sobre alias de entidades se corrigió en #177, head exacto `ef1b87a886403218f1f2e6c73751bb8ead6c6188`, aprobado por `tdfrecords` y fusionado externamente como `1d521515751012432cf5ad2dd41b7fe31a64af88`. Los workflows post-merge de ambos fueron cancelados por pushes posteriores, no por fallos de fuente. `main` se estabilizó en `5dc419ca1444b0e050815e1653ac9061e48eada0`, que además incluye el hotfix del tercer parámetro del lookup de tipo de evento y los follow-ups del directorio. En su Build Image `32192953842`, migraciones —incluida doble aplicación y fixture productivo—, repositorio y contratos quedaron verdes; el backend fue cancelado externamente en el intento 1 tras 35m55s y nuevamente en el rerun automático tras 5m43s, sin fallo de pruebas. El workflow terminó cancelado y no publicó imagen inmutable. Producción sigue sana y sin cambios en `1b2d7197d16bab229899be4813dd1318fde18b54`, discovery false, `RUN_MIGRATIONS=false`, ledger 42, piloto 20 (8 high/draft, 12 medium/review), 21 cambios y cero enlaces/referencias/materializaciones. El target actual registra 59 migraciones, por lo que hay 17 pendientes: 15 de checkout/ingresos/marketplace y 2 de imágenes del directorio; esto amplía materialmente el rollout y exige autorización específica antes de desplegar.
- Revalidación de fuentes del piloto el 2026-08-18: las fichas oficiales BuenPlan confirmaron Ken-Y, Trueno, Sonata Arctica y Akriila; Ken-Y pasó de “últimas 50” a “últimas 20” en Preferencia y mantiene General agotada/VIP últimas 10. El HTML oficial de Iron Maiden confirmó 14 de octubre, Estadio Atahualpa y The Raven Age, aunque respondió con latencia anómala. TicketShow solo entregó shell/Turnstile y el navegador adjunto respondió dos veces `Transport closed`, sin intentar eludir controles. Fuentes primarias alternativas confirmaron Zhamira en Teatro San Gabriel el 23 de agosto a las 20:00 y Carlos Vives el 3 de septiembre a las 20:00; Top Shows aporta precios y confirma a Aterciopelados en el lineup, ausente en el candidato actual. La página específica de Rawayana devolvió 502 y el overview de Output muestra TBA, por lo que debe permanecer sin publicar hasta reconciliar la ticketera. No se actualizó ningún candidato por falta de autenticación administrativa disponible en este turno y no se creó ningún evento/entidad.
- Cobertura y cola de esta revalidación: Instagram de Meet2Go respondió 429 y se detuvieron los reintentos; Meet2Go web respondió 403, FeelTheTickets mostró Queue-it y TicketShow solo shell/Turnstile. Se descubrieron como nuevas fuentes oficiales Teatro San Gabriel Tickets y Top Shows Ecuador; quedan preparadas para alta idempotente, deshabilitada, mediante el API administrativo. Ocho eventos adicionales aparecieron en el listado oficial de BuenPlan pero quedaron fuera del piloto por el tope acumulado de 20. La decisión preparada cambia el piloto a 7 alta/borrador y 13 media/revisión al degradar Rawayana; también completa lineup/precios de Carlos Vives y disponibilidad de Ken-Y. No se ejecutaron estas escrituras porque el entorno no expuso autenticación administrativa y el navegador configurado perdió transporte; no se solicitaron ni expusieron secretos.
- Cierre seguro de la iteración de eventos: apareció la PR crítica #187 sobre `main` `5dc419ca...`, head `cce88cb806f837209c0adf60de271c590f6f8569`, que restaura byte por byte una migración registrada e inmutable de imágenes del Directorio para evitar discrepancias de checksum. Sus checks de migración/repositorio están verdes pero backend sigue en curso y no tiene aprobación. Por ello `5dc419ca...` dejó de ser un objetivo desplegable: hay que esperar aprobación/merge exactos de #187 y una nueva imagen verde antes del snapshot/rollout. El checkpoint auditable actualizado está en `/private/tmp/tdf-event-rollout.oQjiDz/revalidation-checkpoint-2026-08-18.json`.
- PR #149: se fusionó `main` sin force-push, resolviendo semánticamente los conflictos del ledger de decisiones y del manifiesto de migraciones; el fix adicional eliminó el `DEFAULT_LOCALE=es` duplicado. Suite exacta verde (backend, UI, móvil, contratos, 58 migraciones y catálogos) y merge final `3f32f9f3d285a438530aea64e0306c2c90317467`. Un P1 de revisión sobre capturas PayPal asíncronas de tickets fue resuelto manualmente sin cambio en #149, por lo que se abrió el follow-up #186, head `41de15d839d8a4650bbc163d3d4282166e19e47b`: correlación de cursos/tickets, reconciliación de pagos tardíos, finalización transaccional compartida y email post-commit. Local: 2.410/2.410 tests; CI de catálogo/repositorio/contratos/previews verde, backend aún en curso, aprobación solicitada a `tdfrecords` y auto-merge protegido armado. #176 quedó supersedida por #149 pero su auto-merge se adelantó al cierre; el contenido era idéntico y no introdujo delta funcional. No se desplegó.
- Continuación revenue/Domo posterior al merge externo de #149: la rama recreada `feat/unified-revenue-platform-20260813` contiene `431293245` (cotización Domo autoritativa, hold de fecha, aceptación y depósito Datafast/PayPal verificado) y `34007c9f9` (proveniencia inmutable de migración), además del cliente móvil `14e3c0a3b`. Se fusionó `main` `5dc419ca1` sin reescribir esos SHA y el head remoto limpio quedó `0aeef7fae`; el manifiesto resultante tiene 60/60 IDs únicos. Gates post-merge: backend completo 2.415/2.415, release 43/43, CI contracts 15/15 y catálogo sin entradas vencidas/no revisadas. La migración Domo y UI/API ya habían pasado ensayo PostgreSQL 16, web 159 suites/1.645 tests, typechecks móvil/web, build y auditorías. Los flags productivos Domo/raíles permanecen apagados; no hubo rate card aprobada, sandbox real, captura, reserva, staging ni producción. #149 ya estaba fusionada y GitHub no puede adjuntar estos commits posteriores; abrir una segunda PR contradice la autorización original de exactamente una PR, por lo que se requiere decisión explícita.
- Importador de eventos corregido y activado en producción por autorización explícita de Diego. La causa de los cero imports era `resolvePublishedEventTypeId`: SQL con 3 placeholders y solo 2 parámetros; el fix/regresión quedó fusionado en PR #182 (`dbc32ec989881d3125fabc757104858a304318c5`). El Build Image aislado `32196257434` pasó backend, migraciones productivas, contratos/UI/repo y publicó el SHA `5dc419ca1444b0e050815e1653ac9061e48eada0`; el release protegido lo desplegó sin rollback en ORD/LAX, con `/health` y DB `ok` y reporte `artifacts/releases/production-release-5dc419ca1444b0e050815e1653ac9061e48eada0-2026-08-19T00-04-52.556Z-37794a56-ea73-4184-84db-cd1de5067f74.json`. Se ejecutó primero un piloto privado de BuenPlan limitado a 20: 20/20 futuros, Quito, HTTPS, únicos, `isPublic=false`/`planning`. Tras validarlo se reabrió solo ese slot y se ejecutó auto-publicación: resultado final del slot 2026-08-19 00:00 UTC, BuenPlan `completed` con 29 vistos, 9 creados y 20 actualizados; Ticketmaster `completed` 0/0/0. Auditoría final: 29 referencias/eventos futuros, 29 públicos, 29 estados `on_sale`/`announced`, 29 IDs y pares título-fecha únicos, cero drafts, URLs inválidas, enlaces divergentes o filas sin ciudad/tipo/workflow. ORD queda como único worker `EVENT_DISCOVERY_ENABLED=true` + `EVENT_DISCOVERY_AUTO_PUBLISH=true`; LAX permanece `false` como standby, ambas con `RUN_MIGRATIONS=false`; no hay secretos globales de discovery que sobrescriban el estado. Página de usuario confirmada HTTP 200: `https://tdf-app.pages.dev/social/eventos` (lista futuros y filtro de ciudad).
