# 2026-08-20

- Se publicó el acceso público a próximos eventos: `/social/eventos` ya no exige sesión y `/social-events/upcoming` devuelve eventos futuros públicos con una proyección segura. Commit `85c4aff0b`; CI/imagen Docker pasaron y la imagen quedó desplegada en las dos máquinas Fly con el importador activo. El endpoint en producción respondió HTTP 200. El release guardado por migraciones se bloqueó por un checksum histórico preexistente; se hizo rollout directo y se dejó `AUTO_APPLY_PRODUCTION_MIGRATIONS=false` para evitar ese mismatch al arrancar.

- La auditoría `audit:catalog-lists` pasaba localmente pero fallaba en GitHub con dos decisiones obsoletas porque recorría `tdf-hq/config/default.env`, un archivo local ignorado por `*.env`. Sus huellas eran `SUPPORTED_CURRENCIES` y `SUPPORTED_LOCALES`; el checkout limpio de CI no tenía el archivo. Se preparó una corrección aún sin commit para limitar el inventario a archivos rastreados por Git (incluido el submódulo), retirar esas dos decisiones locales y añadir una regresión/step de CI. La auditoría quedó en 861 candidatos/861 decisiones, sin no revisados ni obsoletos; la regresión y `test:ci-pipeline` pasan.
- Se implementó, aún sin commit, un sistema de reseñas verificadas para eventos, listings del marketplace, reservas/ofertas y paquetes de servicios en backend, web y móvil. Solo interacciones completadas pueden reseñar; la evidencia queda privada, el contenido es inmutable, hay idempotencia, límite diario, reportes/moderación, contrato OpenAPI y una migración ensayada con rollback/retry. Compilan backend y web; pasan pruebas web, release/migraciones y lint móvil. Tras restaurar el árbol bloqueado de dependencias móviles, el typecheck completo sigue bloqueado solo por ocho errores preexistentes de rutas Expo.
- Se auditó el onboarding completo (adquisición/signup web, primera apertura/auth móvil y experimento post-signup). Los tres riesgos P0 son: el selector de rol móvil no tiene efecto; los enlaces/intenciones web pueden prometer un resultado distinto del acceso Customer gobernado; y `single-feature-onboarding-v1` confunde primera autenticación local con cuenta nueva, no vence a las 24 horas y puede reaparecer. El informe read-only está en `reports/onboarding-ux-audit-2026-08-20.md` y recomienda pausar el treatment hasta corregir cohorte, expiración, fallback determinista y medición control/treatment.
- Se diagnosticó, corrigió y desplegó el bloqueo de guardado de perfiles fan/artista reportado por Fabro: `FanHubPage` consumía por error `Catalogs.listItems`, que apunta a la ruta administrativa `/catalog/genres/items` y exige `catalog.read`, aunque la ruta pública sana es `/catalogs/genres/items`. El fix usa un cliente público explícito y una clave de caché separada. La ausencia de “Cerrar sesión” era un segundo defecto: `/fans` y `/mi-artista` usan `PublicBranding`, cuyo header ignoraba la sesión; ahora muestra el `SessionMenu` existente para usuarios autenticados y conserva “Ingresar” para visitantes. Los commits `3f5d462f9` y `52bfed17a` llegaron a `main`; CI integral quedó verde y Cloudflare producción desplegó el artefacto inmutable `85de8464`. El push volvió a exponer el fallo preexistente de `Catalog list authority` (0 no revisados, 2 decisiones obsoletas), ya diagnosticado por el inventario de `default.env`; no afecta al despliegue web.
- Se cerró la corrección de #173 y todos los hilos accionables de #170. La PR crítica #187 fue aprobada en el SHA exacto `cce88cb806f837209c0adf60de271c590f6f8569`, pasó CI completo y se fusionó como `926efea58d636a59f068b5019a2c55b7f8f3e49c`; su `Build Image` posterior también terminó verde. En `origin/main` (`52bfed17a`) la migración registrada `2026-08-18_music_directory_profile_images.sql` sigue idéntica al head de #173, con SHA-256 `7a7c21f0e30e3233e91602a4dbb69cc51827bab7cb30f74a8224bf173e559807`, y la migración forward IDN/IPv6 permanece registrada con origen ancestral `bc594785274f9f1b692c6da3b69ce439e060b27a`. Los merges #170, #173, #177, #178, #184, #185 y #187 son ancestros del `main` actual; todos los hilos de #170/#173 están resueltos y #187 no tiene hilos.
- El fallo independiente de `Catalog list authority` quedó resuelto en `main` por `62e1218cf677df845c012c5ed01500007b35aae5`, que restringe el descubrimiento a fuentes rastreadas y añade una regresión determinista. El `main` verificado `713b74ad7a77b0628053b1476485020789a4485b` pasó `CI`, `CI Safe Install` y las dos ejecuciones más recientes de `Catalog list authority`. La PR duplicada #191 contenía el mismo parche pero estaba basada en un `main` anterior y mantenía auto-merge; se cerró de forma reversible, sin fusionarla, para impedir que restableciera una huella de decisión obsoleta. No se tocaron los cambios ajenos del worktree principal.
- Se corrigió el fallo `Catalog list authority` del run `32354947536`: `SocialEventsAPI` había ganado `listPublicUpcomingEvents`, por lo que su huella revisada cambió de `8aebd2c39e94632d619f` a `ac1d43fea60bbc54a939`. El archivo de decisiones conserva la clasificación `genuine-technical-constant`. El commit aislado `cda25089c` se publicó en `main`; el run `32359268602` pasó descubrimiento y autoridad, y auditó 861/861 candidatos. El Datadog Synthetic paralelo también terminó verde. Mientras se verificaba, otro proceso creó/pushó `codex/verified-review-system` (`bf0c1b2ab`) y cambió el checkout a esa rama; se mantuvo intacta y el fix se construyó con un índice temporal directamente sobre `origin/main`.
- Se implementaron las correcciones de la auditoría de onboarding, aún sin commit: intentos de producto no privilegiados y destinos autorizados en web/móvil; solicitudes gobernadas para artista/prácticas; primera pantalla reducida; consentimiento versionado y marketing separado; teléfono diferido; política exacta de contraseña; catálogo ES/EN compartido; Google solo aprovisiona cuentas nuevas desde signup consentido; primera acción real (seguir artista, guardar evento o solicitar acceso) limitada a cuentas creadas en las últimas 24 horas; y taxonomía común sin PII. El experimento `single-feature-onboarding-v1` quedó pausado y corregido para identidad, ventana, exposición/completitud one-shot y estados de error. La primera pantalla también quedó protegida contra recorte con texto ampliado: centra solo cuando cabe y hace scroll desde arriba cuando no. Pasan 8 suites móviles (32 tests), lint móvil dirigido, evaluación de Expo config, export web estático de 50 rutas y render compilado a 320/390 px y 200% de texto, además de suites/typecheck/lint web y pruebas Haskell de consentimiento. Expo Doctor pasa 14/18; el typecheck móvil conserva solo ocho errores preexistentes de rutas y `audit:features` sigue bloqueado por la deuda ya documentada en el informe actualizado.
- Campaña `Tu escena, conectada`: revisión read-only de Primary, General, Requests y los hilos exactos de Rovlex, Domo, Tentáculos, Backroom, Blue Records y HNO. Rovlex reaccionó 🔥 a una historia; Domo mencionó a TDF con ❤️; no hubo respuestas textuales nuevas. Tentáculos no dio disponibilidad/capacidad, Backroom no respondió a la propuesta vista, Blue Records no añadió texto y HNO no mostró novedad gestionable. No se enviaron mensajes ni se inició WhatsApp; el grupo de cinco seguimientos finales permanece agotado por silencio.
- Continuación de campaña: `planet_303` respondió `Holaaa`, `Esto me salió` y una captura con un aviso de límite de actividad para ese correo durante el registro. Se respondió sin pedir contraseñas, códigos ni otros datos sensibles: se recomendó no repetir intentos seguidos, probar más tarde y aclarar si el aviso aparece al crear la cuenta o iniciar sesión. El mensaje quedó confirmado; sigue siendo una incidencia pendiente, no una conversión. No se contactó a los otros destinatarios ni se inició WhatsApp.
