# 2026-08-27

- Root PR #214 merged as `8c0b4a2e76e7de9eaa951ad67405cc5d81eb36a4` after every hosted check passed and Codex found no major issue on exact head `4ccfd64b5e6e212d27e20895bb2514b753a7599d`. It reconciles the packaged directory profile image, sends the secure session cookie from the generated role-management client after reload, and retains persisted artist images ahead of compatibility artwork. The merge's automatic Cloudflare/Vercel main web deployments completed successfully; no Fly/backend rollout or database migration was performed.
- The mobile publication preflight on mobile `main` `43185bb4b0562a2aa4de487c0c6f351b658e1851` confirmed all four public store-policy pages return 200, the active Expo identity can access `@cuco.saa/tdf-mobile` with project ID `218aca4d-c096-4892-a353-c1dd7df23448`, and one valid local Apple Distribution identity exists. The first interactive EAS credential attempt reached Apple's two-factor challenge and was cancelled so no secret entered chat. A later frozen-credential, non-interactive preview build reused the active EAS-managed distribution certificate and ad-hoc profile without regenerating them. Build `5c01ac50-e944-4b8e-80dc-32180115d170` completed for app `1.0.1` (17) from exact commit `43185bb4`, and the downloaded IPA passed archive-integrity, bundle/profile identity, certificate-serial/expiration, entitlement, and macOS code-signature checks. No iPhone was connected, so no install or physical smoke test occurred; no App Store submission occurred.
- Activation pilot PR #215 adds a Spanish Quito creator facilitator guide and anonymized 32-field scorecard. It requires a two-adult isolated rehearsal before the five-person cohort, enforces a 15-minute/one-neutral-prompt success limit, and requires the target image deployment to be completed and verified before real recruitment. Its automatic Cloudflare/Vercel main web deployments completed successfully; no invitation, recording, public profile, Fly/backend rollout, database migration, payment, provider action, or campaign expansion occurred.
- A live Social Events screenshot showing both the empty-state welcome card and a load error was diagnosed read-only. Production health and CORS were sound, while the authenticated event-list projection performed per-event artist, source, workflow-state, and capability queries across the LAX/ORD API to the GRU database. Production held 33 upcoming rows for strict admins (27 imported) versus 6 publicly visible rows, and the public six-row route already took about 3.9 seconds, making the frontend's 30-second abort the likely cause.
- The local backend event-list path now batches artists, genre memberships, external sources, workflow states, and capabilities into a bounded query set while preserving strict-admin visibility and DTO/error semantics. The UI now shows the first-event welcome card only after a successful query, avoiding the contradictory empty state during failures. The full backend build and 18 focused handler examples passed; 24 focused UI tests, typecheck, and the production UI build passed. No deployment or production mutation occurred; production remains on the previously deployed backend until normal review and release.
