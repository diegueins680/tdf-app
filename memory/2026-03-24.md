Continuous improvement loop run:
- Verified the loop model and test harness with `npm run verify:auto-loop` and `npm run test:auto-loop`.
- Ran one unrestricted iteration successfully because the loop worker crashes in the sandbox.
- The loop implemented a small `CourseRegistrationsAdminPage` clarity fix, committed and pushed `88965e64` (`fix: improve course registrations admin page`), and GitHub polling completed without auth failures.
- `scripts/start-continuous-improvement-loop.sh` still exits immediately when backgrounded; use a persistent foreground exec session for now.
