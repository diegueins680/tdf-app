# 2026-09-02

- Diego requested a repository-wide migration away from manually entered Party IDs.
- Audit found `GET /parties` is a CRM-wide, sensitive-data list and cannot be used for relationship pickers. A first additive implementation now introduces `/parties/search`, a minimal selector DTO, generated web contract, a MUI `PartySelector`/`UserSelector`, and migrations of event invitations plus logistics-member assignment.
- The request remains materially incomplete: other web flows and the mobile submodule still have manual Party-ID and full-directory patterns. Do not represent the work as a completed rollout or open a PR claiming all acceptance criteria.
- Backend validation was blocked by independent long-running Stack test/build processes holding `.stack-work` lock; do not terminate them without identifying their owner.
