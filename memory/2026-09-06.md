# 2026-09-06

- Campaña **Tu Escena Conectada**: se enviaron dos bloques de cinco DMs nuevos dentro de la franja permitida. Primer bloque, 13:55–13:58: `1800fiebre`, `a_marte.banda`, `acabaundia`, `andre_farra`, `armadadejuguete`. Segundo bloque, 14:27–14:30: `balasdecamelia`, `cementeriodeelefantesoficial`, `cometasucre`, `cadaverexmusic`, `callejonzaruma`. Los diez envíos aparecieron una sola vez y dejaron el compositor vacío; no hubo restricciones. Los diez contactos quedaron creados y verificados en el CRM con Instagram exacto, sin cuentas de usuario ni conversiones. En la revisión del segundo bloque a las 17:08 no había respuestas; `cometasucre` había visto el mensaje. Se alcanzó el límite diario de 10, así que no se deben enviar más DMs nuevos el 6 de septiembre; solo monitorear o contestar respuestas.
- Contextual-reputation rollout documentation closure: PRs #229 (Product/Legal), #230 (operations/observability), #232 (moderation/admin), #233 (staging/accessibility/pilot), and #234 (approval tracker) are now merged into root `main`; #230 merged as `0544b8dde` after its final revisions. Its runbook now explicitly covers context-scoped projections, submitted-only verified evidence, distinct evaluators/interactions, invalidation/outbox fan-out including connected Bradley--Terry components, immutable formula versions with fenced cutovers/rollbacks, pilot/public consent gates, fail-closed age assurance, and public-read-gate-first rollback. No feature flag, deployment, production migration, pilot, or provider configuration changed.
- Dependent index PR #237 was rebased on the post-#230 main as `4abc8e5b5`, marked ready for review, and given normal protected auto-merge. It changes only `docs/contextual-reputation/README.md` to link the already-merged control documents. At handoff it awaits its regenerated hardcoded-list audit, build, Cloudflare preview, review, and normal branch protection; it is not a deployment and `CONTEXTUAL_REPUTATION_ENABLED` remains false.
- #237 merged as `ad8c36b4b` after its checks/review, and #231 is also integrated as `37c4d8301`. The post-merge main checks (build, catalog audit, Cloudflare) passed. `CONTEXTUAL_REPUTATION_ENABLED` remains explicitly false. At Diego’s direction, PR #239 recorded a declared complete Product/Legal approval by `tdfestudiodegrabacion@gmail.com`; it merged protected as `850a7b63d`. The tracker deliberately keeps this as “Aprobación declarada; evidencia enlazada pendiente”: an email/ticket/acta identifier still must be linked before G0 can treat it as formal evidence. Infra/operations, moderation/RBAC, QA/accessibility and staging/pilot gates remain pending; no deployment, migration, or flag activation occurred.
- Contextual-reputation follow-up PR #231 received fresh approval and Diego merged it as `37c4d830159122d31b7c4857514d13b02a1b0fb6`. Its exact post-merge Catalog authority, CI Safe Install, Datadog, and Build Image workflows all passed; Build Image included backend tests, automatic migrations against the production schema, the full migration/checksum/idempotency suite, UI/persona/repository gates, artifact verification, and Docker packaging/push. The exact Vercel deployment was canceled only because the immediately following docs-only #237 merge superseded it; current `main` `ad8c36b4b46216c582bb5d1c9a632a5fe6ff3a19` directly contains #231 and has successful Cloudflare and Vercel deployments. Node 20 notices remain advisory while GitHub forces Node 24. No production backend deployment, migration execution, or contextual-reputation flag activation occurred.
- Mobile onboarding audit: TDF-mobile PR #37 was privacy-hardened, locally validated, and merged as `20c9f626`; PR #39 was rebased onto mobile `main`, corrected to use authenticated Party identity instead of settings fallbacks, and published at exact head `394479d708c92ca6470bd8a2e9551ea03bab7aec`. Its 64 suites / 318 tests, typecheck, lint, Expo/Metro startup, Mobile Validate, and Datadog checks pass. It remains open and clean because independent review has not arrived. `tdfrecords` was later added as a mobile collaborator and a formal exact-head review request is now pending. Do not merge it until the requested review is present.
- Root onboarding PR #238 was audited in isolation, synced forward to `main` `850a7b63d`, and pushed at `e4ce8403127bc620de430af9e3a8e3cff3941f5f`. Two additional privacy boundaries were fixed: legacy persistent contact PII is discarded rather than restored into a new shared-browser tab, and analytics now strips direct/nested sensitive properties and nested URL secrets. Exact-head typecheck/build/static audit/automation/lint passed; production bundle guard is 411,817 gzip bytes. The serial web run passed 182/183 suites before an unchanged 24k-line course-registration suite timed out and cascaded; its isolated rerun passed 526/526, and hosted UI quality passed. All hosted root checks, including backend and production-schema migration verification, are green. PR #238 is ready for review with a fresh formal request to `tdfrecords`, but review is still required. No merge, backend deployment, production migration, or feature activation occurred.
- Contextual-reputation staging worker infrastructure was implemented in isolated clone `/private/tmp/pr225-inspect.P08z85`, rebased onto root `main` `850a7b63d`, and published as PR #242 (`feat/contextual-reputation-staging-worker`). Current commits: `41eb1f3c109c2201a31305775e277abcfe62b383` (durable outbox, staging/test-only simulation worker, leases/advisory locks, retry/DLQ, immutable actions, run/source replay identity, control-event fan-out, non-PII metric views, boot/config/release guards, tests/docs), `df3d867f9` (checksum-manifest registration plus reviewed catalog decisions), `5cdf424db` (database immutability/single-active guard for activated formula versions), and `87780982b` (post-rebase migration ancestry refresh). PostgreSQL 16 focused migration rehearsal, Stack executable/focused tests, repository quality, production-release/CI contracts, catalog audit, and the full CI-matching PostgreSQL 17 production-schema migration/startup/idempotency rehearsal passed locally; production-release was rerun after rebase with 51/51 passing. At handoff GitHub checks had started and PR merge status was blocked only while checks were pending. The tracker remains pending until real staging provisioning, dashboards/alerts/DLQ ownership/on-call and cross-functional approval exist. No merge, deployment, production migration, worker/feature activation, or pilot occurred.
- Root onboarding PR #238 review follow-up: Codex found a light-theme helper-text contrast regression and a P1 analytics delivery regression caused by stripping PostHog's SDK-owned ingestion token. They were fixed in `0c50b8c86` and `7876ed55f`; the latter preserves only the root token equal to the configured project key while still stripping application/nested token fields, with focused analytics 5/5, targeted lint, typecheck, and production build passing (5 preloads / 411,815 gzip bytes). Both review threads were resolved and a fresh Codex review on exact head `7876ed55fa140ef09466a5281e3c40123b6f04e9` found no further major issues. All exact-head hosted checks passed (15 success / 2 expected skips), including UI quality, backend quality in 19m17s, and production-schema migration verification. `tdfrecords` approved the exact head, `main` remains `850a7b63d`, and PR #238 is clean/mergeable. It is intentionally unmerged pending mobile-first completion; no production deployment or migration was performed.
- Mobile PR #39 remains exact head `394479d708c92ca6470bd8a2e9551ea03bab7aec`, clean and green, but has no review or explicit independent approval comment. `tdfrecords` is now a repository collaborator and has a formal pending review request. Preserve the independent-review gate and merge mobile before root #238 once that gate is cleared.
- EventosEcuador: se creó una única ficha de proveedor para **Domo del Pululahua** en la cuenta autenticada de Diego, registro interno `198`, plan `Free`, categoría `Espacios para Eventos` / subcategoría `Centro de Eventos`. Quedó guardada con estado `Borrador`, pendiente de la verificación administrativa que el sitio exige antes de publicar. La ficha conserva ubicación en Quito/Pichincha, mapa, sitio, correo, teléfono/WhatsApp, video oficial, aforo máximo 120, una sala, una portada real de 500×500 y cuatro fotos reales de 2048×1024; una recarga independiente confirmó todos los datos y las cinco imágenes persistidas. No se contrató ningún servicio pagado ni se creó un duplicado.
- PR #242 staging-worker follow-up: fixed the first hosted migration-test permission failure in `4f4f62c62`, then implemented ordinal-only Bayesian Bradley--Terry aggregation, ties, comparable absolute-score blending and atomic transitive-component refresh in `80ec0af27`. Focused PostgreSQL 16, full PostgreSQL 17 production-schema migration/idempotency, production-release 51/51, CI-pipeline 16/16, repository, catalog, backend, UI, mobile, persona, API, preview and aggregate hosted checks all pass on the exact head. The original two review threads are resolved, the PR documents that this migration must use a merge commit, and `tdfrecords` approved the exact head. A fresh Codex review added four valid unresolved findings (active formula downgrade, run high-water cutoff, restored interaction eligibility fan-out, and max-attempt DLQ requeue overflow); implementation is paused for explicit approval. No merge, deploy, migration execution, flag/worker enablement, or pilot occurred.
- PR #242 advanced to exact head `f4aba11e7ab3fa859f2698f4286445140009c26c`: active formulas cannot return to draft; run identity/high-water marks are immutable and bound both evidence selection and decay time; run-backed claims are environment-scoped; restored interaction eligibility emits recalculation; and requeued attempt-20 DLQ events restart at attempt zero while preserving the immutable audit action. Focused PostgreSQL 16, full PostgreSQL 17 migration/startup/idempotency, production-release 51/51, CI-pipeline 16/16 and every hosted check/preview pass. Those four threads are resolved. The newest Codex review now has five valid unresolved findings: post-cutoff source edits still need a fail-closed producer fence or snapshot, evaluator caps must be solved against adjusted effective weight, expired leases must DLQ before overflow, evaluation DELETE needs pre-cascade invalidation, and time decay needs a periodic recalculation producer. Fresh approval is required before the next patch; current review approval was dismissed by the push. No merge/deploy/migration/enablement/pilot occurred.
- Onboarding merge closure: `tdfrecords` formally approved mobile PR #39 at exact head `394479d708c92ca6470bd8a2e9551ea03bab7aec`. It merged normally into TDF-mobile `main` as `9ff71507730808b4a52e8fa6e25d1ca366783543`; post-merge Mobile Validate (including tests and Expo Doctor) and Datadog passed. Root PR #238 then merged normally into root `main` as `4e3fc2fe6862d0b1549571d165e7f84f01fb7d3c`, preserving exact reviewed head `7876ed55fa140ef09466a5281e3c40123b6f04e9` as an ancestor. Every exact post-merge root workflow passed: catalog authority, safe-install build, Datadog, and Build Image, including repository/mobile/UI/persona/backend gates, production-schema migration verification, tested executable artifact verification, and Docker image publish. Cloudflare and Vercel commit statuses also succeeded. No manual backend deployment, production migration execution, or feature activation was performed.
