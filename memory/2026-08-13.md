# 2026-08-13

- Recuperación de HQ UI: el bucle automático había incluido accidentalmente 663 archivos en `main` y roto UI, backend y el submódulo móvil. Se pausó reversiblemente el grupo de procesos PGID 757, se reconstruyó desde `692e8d75` y se restauró `main` primero a `54c89116c` (corrección del presupuesto del bundle) y luego a `4c2940ffd` (endurecimiento de variables frontend). Las protecciones quedaron restauradas: ruleset `Rulz` activo, force-push deshabilitado, una aprobación y resolución de conversaciones requeridas.
- Incidente de seguridad: la producción previa serializaba todo `import.meta.env` y expuso una clave de proyecto OpenAI configurada erróneamente como `VITE_TIDAL_AGENT_API_KEY`. No se registró el valor. El commit `4c2940ffd` reemplaza las lecturas de diccionario por referencias explícitas a variables públicas y añade un escáner de secretos a `check-initial-bundle.mjs`. Pasaron pruebas dirigidas, lint sin errores y un build con clave canario falsa (399.361 bytes gzip); el preview, el artefacto inmutable de producción y finalmente `tdf-app.pages.dev` quedaron sin el nombre filtrado ni firmas de claves OpenAI. La clave expuesta debe revocarse/rotarse manualmente; retirar el bundle no la vuelve segura.
- El bucle `scripts/autoloop-forever.mjs` permanece pausado (`STAT T`, PGID 757). La PR #145/rama `codex/catalog-persistence-20260807` todavía contiene el checkpoint accidental en su historia y no se reescribió sin autorización separada.
- El usuario ordenó continuar el piloto de Instagram “Tu escena, conectada”. A las 08:42 de Ecuador todavía no iniciaba la ventana autorizada de 10:00 a 18:00, así que no se enviaron mensajes.
- Se revalidaron individualmente doce destinatarios restantes del lote aprobado y su coincidencia exacta en `CRM → Contactos`: `gabbygonzalez333`, `takijanaskaec`, `theriskisdead`, `69segundosofficial`, `kinocavalier`, `festival_mestizonica`, `lasabejasclubdevinilos`, `midiendocalles.ec`, `jorgemolina.oficial`, `backroom_session`, `kirmanarecords` y `lordethnic`. Todos conservaron pertinencia pública, no mostraron indicios de minoría y no tuvieron coincidencia exacta en CRM.
- Ocho perfiles mostraron botón `Message` directo: Gabby, 69 Segundos, Kino, Mestizónica, Las Abejas, Midiendo Calles, Jorge y Kirmana. Takijana, Risk, Backroom y Lord Ethnic no mostraron compositor directo. No se reabrió Tentáculos ni se usó un hilo alternativo para esos cinco perfiles, dado el comportamiento anómalo del día anterior.
- La bandeja no mostró respuestas nuevas: permanecen `Listo :)` de Blue Records y `Ya le hago` de HNO. No hubo objeciones, solicitudes de no contacto, CAPTCHA, advertencias ni restricciones.
- La ficha CRM de Blue Records se revisó sin cambiar datos. La interfaz de contacto solo ofrece nombre visible, correo, Instagram y teléfono; no dispone de campos de campaña, estado, respuesta ni notas. La gestión detallada continúa documentada en `docs/campaigns/tu-escena-conectada-piloto-2026-08-12.md` y esos campos se consideran no disponibles en CRM.
- A las 10:01 se envió y confirmó en el hilo la invitación aprobada a `gabbygonzalez333`; se creó después `Gabby Gonzalez` como persona y se verificó una única coincidencia exacta con ese Instagram en CRM.
- A las 10:04 se envió y confirmó en el hilo la invitación aprobada a `69segundosofficial`; se creó después `69 Segundos` como empresa y se verificó una única coincidencia exacta con ese Instagram en CRM.
- Al navegar al tercer candidato del bloque, `kinocavalier`, Instagram redirigió la sesión visible a `/accounts/suspended/`. Se detuvo inmediatamente sin hacer clic, reintentar, intentar resolver la restricción, contactar a Kino ni crear su contacto. Estado acumulado: 9 DMs confirmados, 8 Instagram exactos en CRM, HNO ambiguo, 11 aprobados pendientes, 2 respuestas positivas, 1 restricción de Instagram, 0 no-contacto, 0 duplicados de DM, 0 menores y 0 publicaciones de video.
- El usuario informó que la sesión fue restablecida manualmente. La comprobación visible mostró la portada y la bandeja normales, sin CAPTCHA, verificación ni advertencia visible, y sin respuestas nuevas del piloto. No se enviaron más DMs: la suspensión previa sigue siendo una restricción comprobada y el piloto permanece detenido; no se simuló conducta humana ni se intentaron técnicas de evasión.
- A las 10:23 el usuario volvió a ordenar continuar. Se comprobó que Instagram estaba autenticado y normal, se revisaron Primary, General, From ads y Requests, y no aparecieron respuestas nuevas del piloto; las notificaciones visibles correspondían a hilos no relacionados o antiguos.
- A las 10:27 se envió y confirmó en el hilo el mensaje aprobado a `festival_mestizonica`; se creó después `Festival Mestizónica` como empresa y se verificó una única coincidencia exacta con ese Instagram en CRM.
- Al revisar `lasabejasclubdevinilos`, el perfil seguía activo y pertinente, con contenido del 12 de agosto, botón `Message` y cero coincidencias exactas en CRM. Un único clic en `Message` devolvió `Something went wrong. Please try again.` y no abrió compositor. Se detuvo sin reintentar, sin enviar DM y sin crear contacto.
- Estado acumulado tras la segunda detención: 10 DMs confirmados, 9 Instagram exactos en CRM, HNO ambiguo, 10 aprobados pendientes, 2 respuestas positivas, 1 suspensión comprobada, 1 nuevo error visible al abrir compositor, 0 no-contacto, 0 duplicados, 0 menores y 0 publicaciones de video.
- A las 10:38 el usuario compartió una captura del `Estado de la cuenta` de `tdf.records.label`: las tres categorías visibles, incluida `Funciones que no puedes usar`, muestran indicador verde. No hay restricción formal visible ni acción de apelación pendiente; el fallo `Something went wrong` de Las Abejas permanece como incidente técnico independiente y el lote sigue detenido por el control operativo.
- A las 10:39 el usuario ordenó no esperar hasta mañana y continuar. Se mantuvieron excluidos Kino, Las Abejas y Tentáculos. A las 10:41 se envió y confirmó el DM aprobado a `midiendocalles.ec`; se creó `Midiendo Calles` como empresa y se verificó una única coincidencia exacta en CRM.
- A las 10:44 se envió y confirmó el DM aprobado a `jorgemolina.oficial` después de revalidar el perfil, su condición adulta, el hilo previo sin DM de campaña y cero coincidencias exactas en CRM.
- Después del envío a Jorge se creó `Jorge Molina` como persona. La búsqueda por nombre reveló dos fichas: una preexistente con usuario creado y correo `kuynet@googlemail.com`, y la nueva sin datos. Como podrían ser la misma persona, no se añadió el Instagram, no se editó ni eliminó ninguna ficha y se detuvo antes de Kirmana.
- Estado acumulado tras la tercera detención: 12 DMs confirmados, 10 Instagram exactos en CRM, HNO y Jorge ambiguos, 8 aprobados pendientes, 2 respuestas positivas, 1 suspensión comprobada, 1 error al abrir compositor, 1 nueva ambigüedad de identidad, 0 no-contacto, 0 DMs duplicados, 0 menores y 0 publicaciones de video.
- El usuario confirmó que las dos fichas `Jorge Molina` eran la misma persona y autorizó el merge. Se preservó `partyId 81` como principal porque ya tenía usuario y `kuynet@googlemail.com`; se le añadió `jorgemolina.oficial`. Se comprobó tanto en la interfaz como en producción que la ficha nueva `partyId 153` estaba vacía, sin historial y con cero referencias en todas las claves foráneas. Se eliminó únicamente `partyId 153` en una transacción con validaciones y se verificó una sola coincidencia final por nombre con correo e Instagram exactos.
- El usuario de Jorge era preexistente y no se atribuye como conversión del piloto. Estado tras el merge: 12 DMs confirmados, 11 Instagram exactos en CRM, solo HNO ambiguo y 8 destinatarios aprobados pendientes.
- Tras el merge se completó un bloque de cinco mensajes: `kirmanarecords` a las 10:59, `takijanaskaec` a las 11:03, `theriskisdead` a las 11:06, `backroom_session` a las 11:09 y `lordethnic` a las 11:12. En todos los casos se revalidaron perfil, hilo y CRM, se confirmó el texto completo en Instagram y se creó una sola ficha con el Instagram exacto (Kirmana, Takijana y Backroom como empresas; Risk y Alvaro Pardey como personas).
- Backroom Sessions respondió positivamente que el proyecto de sesiones y eventos encaja con TDF y que les interesa participar. Se clasificó como interesado; no prueba registro ni activación. El usuario aprobó después una propuesta específica de sesión piloto en el Domo del Pululahua y autorizó enviarla. Instagram rechazó el intento de una sola burbuja por longitud sin publicar nada; a las 11:45 se envió íntegramente en dos partes consecutivas, ambas verificadas en el hilo, con propuesta de visita, coproducción, posible temporada, fecha y WhatsApp.
- Cierre operativo: 17 DMs confirmados acumulados, 16 Instagram exactos en CRM, solo HNO ambiguo, 3 respuestas positivas, 0 solicitudes de no contacto, 0 duplicados, 0 menores, 0 publicaciones y 0 conversiones atribuibles. Hoy se alcanzaron los límites de 5 mensajes por bloque y 10 por día. Permanecen sin contactar únicamente Kino, Las Abejas y Tentáculos por sus incidentes previos.
- Corregido localmente el fallo de Cloudflare Pages del commit `692e8d75`: el `manualChunks` de Vite agrupaba todos los iconos MUI usados por rutas diferidas y precargaba ese bloque en la entrada. Al dejar que Rollup ubique cada icono con su consumidor, las precargas bajaron de 6 a 5 y el JavaScript inicial quedó en 399.126 bytes gzip para el SHA fallido y 399.804 bytes para el checkout actual, ambos bajo el límite de 409.600. El build completo de HQ UI pasó; no se hizo commit, push ni despliegue.
- Catálogos: el seguimiento quedó en la PR padre `diegueins680/tdf-app#145`, rama `codex/catalog-persistence-20260807`, con auto-merge activado y todos los checks del SHA `42117121c30e45523ae0fbe76e6ccfd0c3622ba6` en verde. Se solicitó la aprobación requerida a la única cuenta colaboradora elegible, `tdfrecords`; no se usó `--admin` ni se alteró la protección de rama. Las PR móviles `TDF-mobile#22` y `#23` están fusionadas.
- Se detectó y corrigió un `introducedBy` abreviado que impedía incluso el plan de producción; commit `42117121c` añade además una prueba que valida todos los orígenes del manifiesto (25/25 pruebas del runner). El dry-run enumeró 17 migraciones y cero comandos mutantes. El workflow oficial `31751477332` pasó todas las suites y publicó `diegueins680/tdf-hq:42117121c30e45523ae0fbe76e6ccfd0c3622ba6`; un segundo preflight remoto de solo lectura validó la imagen exacta. Producción sigue bloqueada únicamente por tener 1 parte administradora de emergencia autenticable de las 2 requeridas. Producción estaba saludable en `d10e7cdf9429e548aefaa60708610cc8a71e80bd`; el volumen PostgreSQL cifrado conservaba cinco snapshots automáticos, el más reciente `vs_56NP3NqzkZ0cN6AeM7KYOj5`, pero no se hizo ni reclamó el backup nuevo de corte. No hubo escrituras a producción, migración ni rollout.
- `tdfrecords` aprobó la PR padre #145 y el auto-merge la fusionó a `main` el 2026-08-13T23:47:15Z, merge commit `678f0f2289e50a6db9111287436c8f42dce2d3cc`. El plan seco del merge commit también pasó con 17 migraciones y cero comandos mutantes. Vercel reportó éxito para ese SHA; el workflow oficial de imagen de `main` es `31755081382` y quedó revalidando backend/UI antes de publicar la etiqueta final. El rollout sigue detenido hasta una segunda ruta independiente de administrador de emergencia y un backup nuevo de corte.
- Incidente de desfase de producción tras el merge de catálogos: `tdf-app.pages.dev` sirve la UI `678f0f2289e50a6db9111287436c8f42dce2d3cc`, mientras `tdf-hq.fly.dev/version` sigue en `d10e7cdf9429e548aefaa60708610cc8a71e80bd`. El backend anterior no expone `/catalogs/batch` y devuelve preferencias regionales legadas sin `localeId`/`currencyId`; la UI nueva llama `trim()` sobre esos campos ausentes y derriba React para sesiones autenticadas. El rollback inmediato seguro de UI es `4c2940ffd`; la alternativa es completar primero el rollout coordinado de migraciones/backend. Los errores `window.cardano` provienen de extensiones de billetera en conflicto y no del bundle TDF. No se hizo rollback, despliegue ni cambio de código.
- Tras autorización explícita del propietario para el segundo operador, se creó y confirmó antes de cualquier escritura el snapshot Fly `vs_LRqNAqabQP5UoV2lo3JRqy` del volumen PostgreSQL (estado `created`, retención 5 días). Una transacción acotada, ensayada primero con `ROLLBACK`, conservó la credencial determinísticamente vinculada al correo primario único, desactivó reversiblemente un duplicado activo, activó la asignación `Admin` única y añadió dos eventos de auditoría. El gate legacy pasó después con 3 asignaciones, 3 partes asignadas y 2 partes autenticables (`preMigrationReady=true`).
- El endpoint público de recuperación aceptó la solicitud con HTTP 200, quedó exactamente un token de recuperación activo para la cuenta designada y no apareció fallo de envío en el buffer de Fly. Falta que el segundo operador abra personalmente el correo, defina una contraseña privada y complete el login/recovery independiente; no se debe migrar ni desplegar el backend nuevo hasta esa comprobación humana y el gate canónico post-migración.
- La evidencia sin datos personales quedó en el commit `608af30d0` y PR #147, con todos los checks técnicos verdes, Cloudflare y Vercel incluidos, auto-merge activado y revisión solicitada a `tdfrecords`; la protección exige aprobación independiente y no se eludió. Los helpers temporales fueron retirados de la máquina de DB. API/DB y web productivos siguieron saludables, y las dos Machines existentes en `ord` y `lax` permanecieron iniciadas.
- La PR #145 terminó fusionada en `main` como `678f0f2289e50a6db9111287436c8f42dce2d3cc`; el workflow de imagen `31755081382` pasó y publicó el digest inmutable `sha256:32d49f046f2a2f310badde11f83662f6edd24e01b152441ddd8ea03019bbe42a`. La UI fusionada quedó publicada y el escaneo del artefacto en vivo no encontró `VITE_TIDAL_AGENT_API_KEY` ni firma de clave OpenAI.
- Se intentó el release backend protegido para ese SHA. El runner adquirió el lease, completó su lote gobernado de 17 migraciones y la verificación de esquema, pero abortó antes de tocar cualquier Machine porque el gate post-migración permaneció en modo `legacy`: el manifiesto no incluye la creación/cutover del esquema canónico de seguridad. El lease se liberó sin error; no hubo canary, rollout ni rollback de Machines. Producción continuó saludable en `d10e7cdf9429e548aefaa60708610cc8a71e80bd` con ambas Machines en el digest anterior. El reporte local es `artifacts/releases/production-release-678f0f2289e50a6db9111287436c8f42dce2d3cc-2026-08-14T00-42-36.791Z-78b817c5-fb0a-4108-82bd-26e4e8a8bcd5.json` dentro del worktree limpio temporal.
- No se debe reintentar el backend ni ejecutar manualmente `2026-08-07_catalog_backfill_apply.sql`: el programa de catálogos sigue marcado como bloqueado, carece de una migración de creación de esquema canónico registrada en `scripts/production-migrations.json`, y aún falta la comprobación humana independiente de login/recovery del segundo operador. El preflight automatizado actual no representa esa comprobación humana.
- Plataforma de ingresos: la rama `feat/unified-revenue-platform-20260813` y la única PR draft #146 quedaron en `d4cbede536b00a94da2a481ca26d4739e7992a02`. Los commits `b52f48534`, `47fa3af8e` y `d4cbede53` enlazan mixing/mastering con el checkout canónico, registran/ensayan la migración reversible `2026-08-14_service_storefront_checkout_runtime.sql` y actualizan la decisión de catálogo derivada del manifiesto. Datafast y PayPal validan bindings exactos y estados de proveedor; pagos manuales nunca se auto-confirman; producción permanece cerrada por feature flags. El workflow CI `31767195551`, los builds, Vercel, Cloudflare y la auditoría de listas terminaron en verde; la rama quedó limpia, remota y sin merge ni despliegue productivo. No se ejecutó transacción sandbox/real, webhook, reembolso, entrega DSP, statement ni payout. Siguiente corte seguro: inbox firmado/replayable de eventos de proveedor, reembolsos y reconciliación sandbox.
