# 2026-09-07

- Campaña **Tu Escena Conectada**: Diego corrigió la regla permanente para iniciar a las 09:00; la ventana vigente es 09:00–18:00 de `America/Guayaquil`. Se ejecutaron dos bloques de cinco invitaciones. Primer bloque: `17coficial`, `hendry_dti`, `lumy_mc`, `thepicnic.band` y `oficialshawling_music`, registrados exactamente en CRM con `partyId` 201, 202, 203, 204 y 207. Segundo bloque internacional: `hardtomiss.official`, `ellacentricc`, `brontesband`, `manglarbroders` y `labandabravagye`, registrados exactamente con `partyId` 208, 209, 210, 211 y 212. Los diez DMs aparecieron una sola vez, dejaron el compositor vacío y no mostraron errores; Brontës recibió únicamente la solicitud gratuita normal, nunca la opción prioritaria pagada. Todos los tipos, nombres e Instagram fueron revalidados mediante la ficha individual del CRM y ninguno tiene cuenta de usuario. 17C respondió positivamente que revisará la plataforma y recibió los dos mensajes de acompañamiento autorizados; los otros nueve no habían respondido en el control de la bandeja de las 16:41. `cementeriodeelefantesoficial`, contactado el 6 de septiembre, preguntó de qué se trata la plataforma; se respondió el 7 de septiembre explicando que permite crear un perfil público, reunir la música y conectar con el público y otras personas de la escena, indicando además que el enlace está en el mensaje anterior y que puede compartir errores o sugerencias. La respuesta apareció exactamente una vez y dejó el compositor vacío. Su contacto exacto (`partyId 197`) y los diez contactos del día seguían sin cuenta de usuario en la auditoría de las 16:41. Total diario final: 10/10 DMs nuevos y 0 conversiones confirmadas; no enviar más invitaciones nuevas el 7 de septiembre. `1800fiebre` sigue como intención positiva sin cuenta. Además de los aplazamientos anteriores, `chloe.parche`, `sindicatodelbuenfin` y `los_ultratumba` fallaron al abrir el hilo; `mantla_music` y `adahakenji` no mostraron botón de mensaje; `manzana.once` y `osgasmosonico` no estaban disponibles. No se envió a ninguna cuenta con error y no se pagó por priorizar mensajes. Informes: `docs/campaigns/tu-escena-conectada-internacional-2026-09-07.md` y `docs/campaigns/tu-escena-conectada-continuacion-2026-09-06.md`.
- TDF Records YouTube catalog ingestion is prepared in root PR #243 from isolated worktree `/private/tmp/tdf-records-youtube-ingest-20260906`, exact head `2ad18a1afd0f7eb12780d6f90e47439891a2a10b`. Direct channel comparison found 38 public videos: 33 recording videos plus the 5 already-separated numbered Live Sessions, so the migration adds the 27 missing recordings and preserves all 5 sessions. The reviewed migration/rollback preserves unrelated collection membership and exact prior order, is replay-safe, stores durable evidence, canonical URLs/durations/thumbnails, and has a registered immutable introduction SHA; development seed replay also reconciles metadata, ordering, and YouTube thumbnails. Focused PostgreSQL migration/rollback/reapply, full PostgreSQL 17 production-shaped batch twice, Stack build, production release 49/49, catalog/repository checks, and the entire hosted matrix passed on the exact head. Auto-merge with method MERGE is armed, but the final `tdfrecords` exact-head approval request remains pending; do not bypass it. No production snapshot, release, migration, or Fly mutation has occurred. Once approved and merged, wait for the merge-SHA Build Image, create a snapshot of Fly volume `vol_re89q7o0w7ynpx1r` on `tdf-hq-db`, run the guarded exact-SHA backend plan/preflight/release, and verify the production `/records/feed?locale=es` contains all 33 recording IDs in source order and the 5 sessions.
- Contextual-reputation staging-worker integration is complete: PR #242 merged as `f5e5bef685a5328e3c0ff726ecdedd7f531de5b6`, catalog repair #250 merged as `269f3784121b8bdca378ec516d5cb446cc818e39`, and forward-fix PR #251 merged as `b4e64406462c191f2b93da8a3bd67ab0d6f679ff` from exact approved head `56d958e20ac7021d01de78f41cf00eea3bb63ffc`. #251 fixes submitted-to-draft invalidation, suppresses primary-subject tuples for `not_applicable` categories, makes submitted category rows immutable, and serializes category edits with submission in both transaction orderings. Codex caught that editing the already-registered migration would break ledger checksum validation; the final tree restores `2026-09-06_contextual_reputation_staging_worker.sql` byte-for-byte (SHA-256 `5ec11f453dddf2b167c79f4d54ac7ec677591f8c2f9fa7bb2a9bc389878a6959`), permanently guards that checksum, and ships all repairs through registered forward migration `2026-09-07_contextual_reputation_invalidation_repair` introduced by `114400d7eac3b4b1b54de1e1e676d75afc76388d`. Focused PostgreSQL 16 lifecycle/concurrency tests, a PostgreSQL 17 upgrade from main's 84-entry ledger to 85 entries, clean production-schema application/replay, repository/catalog gates, all exact-head hosted checks, exact-head approval, and clean Codex review passed. All #242/#251 review threads are resolved, and the post-merge main CI, catalog audit, previews, safe-install build, and tested backend-image packaging all completed successfully. No production migration, deployment, reputation flag/worker enablement, staging provisioning, or pilot start occurred.
- TDF Records YouTube ingestion is complete in production. PR #243 merged as `48992e37f83ac6c6f45d4f1a2a4888c6bf41bd2`; the production ledger now records `2026-09-06_records_youtube_catalog` with checksum `fe7167bdbaac443a470ea4138abd9375b5ebc07d29f5a57ada75f7d7123f6f4f` and source commit `b181fae331f900a7214feda140b5b36cd3fa9d28`. Guarded preflight first exposed two historical migration-integrity regressions, fixed through forward-safe PR #244 and exact-byte restoration PR #245 (`b181fae331f900a7214feda140b5b36cd3fa9d28`). The first canary then exposed a persistent asset-volume ownership startup bug and rolled back without touching the second machine; PR #247 restored best-effort non-destructive seeding with a deterministic regression test and merged as `9a8697fe76d84d0b4503d2e6324b7fee91d23f7a`. Final snapshot `vs_GeBP6BlqRz1slBRP8q684m` (digest `e45c8684e976280ecc7e183c6589e7e49096784a2e36c46866df1030a3bc81e1`) preceded the successful guarded release of image `diegueins680/tdf-hq@sha256:c454dce56faec89986b1dd1ea3ad2759a38399965fde61d7822ef929804e9ce8`; both Fly machines run exact SHA `9a8697fe...`, report machine-pinned health/db `ok`, the ledger count is 82, and the release lease is empty. Final `/records/feed?locale=es` equality check passed with all 33 recording IDs and all 5 Live Session IDs in exact source order. `EVENT_DISCOVERY_ENABLED=true`, `CONTEXTUAL_REPUTATION_ENABLED=false`, `RUN_MIGRATIONS=false`, and `AUTO_APPLY_PRODUCTION_MIGRATIONS=true` remain effective on both live processes.
- Live browser verification of `https://tdf-app.pages.dev/records` passed after the catalog release: the public feed request returned HTTP 200 at revision 229, the UI exposed all 33 recordings across four pagination pages (`1–10`, `11–20`, `21–30`, `31–33`) with exact expected YouTube ID order and no missing, unexpected, or duplicate entries, and all 5 Live Session links matched their expected IDs. The browser console had zero errors.
- Contextual reputation consent closure: mobile PR #42 merged as `1ce2cb0548ba842d3ecc04ec38140173d69e01c2`; root PR #240 then auto-merged with merge commit `8a5736ba6a44bb1ddf58a5dcc7772d453b5f8005`. The final correction enforces both pilot participation and public visibility before public reads, current approved/evidenced/unexpired guardian authorization for minor grants, and auditable same-state reconsent when disclosure version/locale changes. It adds typed DTO/DB consent boundaries, the documented 403, regenerated web/mobile clients, and an accessible UI that blocks grants while loading or with the pilot disabled and exposes adult attestation after a 403. Local isolated Stack build, OpenAPI generation, web typecheck and focused UI tests passed; the full root hosted CI matrix passed. All 22 review threads were resolved and the PR was independently approved. No migration ran, no deployment occurred and `CONTEXTUAL_REPUTATION_ENABLED` remains false.
- The staging aggregation worker PR #242 was merged as `f5e5bef685a5328e3c0ff726ecdedd7f531de5b6`; its branch merge had surfaced a stale catalog-list fingerprint. A minimal follow-up #250 replaced its two old registry decisions with the current combined-registry decision `40e2658bb787a6393a2f`, passed its PR catalog audit/preview checks, was independently approved, and merged as `269f3784121b8bdca378ec516d5cb446cc818e39`. The main post-merge catalog workflow was still running at handoff. No staging provisioning, production migration, deployment, worker start, pilot, or feature flag activation occurred.
- The post-merge Catalog list authority workflow for `269f3784` completed successfully. The `f5e5bef...` Build Image also completed successfully. Repository work for the contextual-reputation worker/consent rollout is now integrated; the remaining G0/G1 gates are external: real staging provisioning, dashboards/alerts/DLQ/on-call ownership, formal Product/Legal jurisdiction closures, moderation/RBAC sign-off, and QA/device/accessibility/pilot evidence. Keep all reputation flags and workers disabled until those gates are recorded.
- Event discovery was explicitly re-enabled after the Records release. The staged secret was rolled through `ord` and `lax` one Machine at a time; both ended started/passing on `9a8697fe...` with effective `EVENT_DISCOVERY_ENABLED=true`. The first fresh 18:00 UTC slot completed exactly once for each enabled provider: BuenPlan saw 30, created 0, updated 21; Ticketmaster returned 0/0/0. Both sources retain zero consecutive failures and no current error, public health/db are `ok`, and the immutable image digest remains `sha256:db54172fad74d6b60442d2b5ce41fafca3d6d18296989b404cd4976baf70a90d`.
- Reconciled the only orphaned event-discovery run: row `49` (`buenplan`, slot `2026-08-20 12:00 UTC`) had remained `running` with zero activity and no finish time despite 53 later successful BuenPlan slots. A guarded one-row update marked it `failed`, set `finished_at`, and recorded an operational audit explanation without changing source failure counters or current runs. Final verification shows zero `running` rows, both enabled sources at zero consecutive failures, the 18:00 UTC slot unchanged/completed, both Fly regions passing on `9a8697fe...`, and public health/db `ok`.
- Auto-publication was explicitly enabled with staged `EVENT_DISCOVERY_AUTO_PUBLISH=true` and rolled through `ord` and `lax` one Machine at a time. Both VMs ended started/passing on the unchanged `9a8697fe...` SHA and `sha256:db54172...` image with effective discovery and auto-publication both `true`; `CONTEXTUAL_REPUTATION_ENABLED=false` and `RUN_MIGRATIONS=false` remain unchanged. The restart did not replay the completed 18:00 UTC slot: exactly one completed row remains per provider, both sources have zero consecutive failures, and public health/db are `ok`.
- Event intake from two supplied September flyers: created production venue `14`, **TDF Records - Studio Legends**, at Félix Oralabal N45-55 y Zamora, Quito, and event `121`, **Listening Party — Labii & Llama Este Pez**, for Thursday 10 September 2026 at 20:00 `America/Guayaquil` (`2026-09-11T01:00:00Z`). It remains a private `planning` draft, links existing artist profiles Labii `24` and Llama Este Pez `42`, and its original flyer was attached and replicated to both Fly asset volumes. No internal invitation, email, Instagram DM, or public announcement was sent. Audience audit found 79 active unique email recipients, 10 active users with Instagram handles, and zero handles matched to an eligible existing Instagram inbox thread. The second flyer, **Entre Panas en el Domo**, lacks a time and conflicts with existing production event `86`, **Melania - Indie Session**, already occupying Saturday 12 September from 15:00–18:00 local at venue `7`; confirmation is required before replacing or coexisting. External delivery still requires explicit confirmation per workspace safety rules.
- Diego confirmed the 15:00–18:00 Domo slot, replacement of event `86`, email delivery, and public Instagram posting. Event `86` is now **Entre Panas en el Domo — Llama Este Pez**, Saturday 12 September 2026 15:00–18:00 local, $5, capacity 300, reservations `0984755301`, artist `42`, Domo venue `7`; its flyer is replicated on both Fly asset volumes. Events `86` and `121` are both `announced`, public, and present in the public upcoming-events projection. Each has exactly 78 internal invitations to distinct active user parties, excluding organizer party `9`, with no duplicates. The registered-user email dry run matched 84 active account rows and 79 unique valid addresses. The single real broadcast completed with audit row `87` / status `partial`: 9 sent and 70 failed; all 70 were SMTP 554 rejections, 68 explicitly `5.7.1 Spam message rejected`. It was not retried, preventing duplicates and further sender-reputation harm. Instagram's active composer did not support a carousel, so the two supplied flyers were published as two consecutive feed posts on `@tdf.records.label`, with Facebook cross-posting off; the verified profile count advanced once per post from 340 to 341 to 342. No Instagram DMs were sent because zero registered handles mapped to an eligible inbox thread.
- A las 22:21, fuera de la ventana operativa y después de haber alcanzado el máximo diario de 10 DMs nuevos, Diego autorizó continuar con seguidores de `@tdf.records.label` e invitarlos también a probar TDF. No se envió ningún mensaje adicional el 7 de septiembre. Se creó la tarea de una sola ejecución `e126436f-c562-4f7e-bda2-8a7fac85f456` para el 8 de septiembre de 2026 a las 09:00 `America/Guayaquil` (`14:00Z`), con borrado automático tras éxito. La tarea usa una sesión local de Codex verificada y ejecutará como máximo cinco DMs nuevos, solo a seguidores musicales adultos sin contacto, usuario ni CRM previo; combinará la invitación a ambos eventos con la prueba de la plataforma, revalidará hilo y CRM, no usará solicitudes pagadas, se detendrá ante cualquier alerta y documentará únicamente envíos confirmados. Instrucciones: `docs/campaigns/tu-escena-conectada-seguidores-2026-09-08-prompt.md`; lanzador: `scripts/run-instagram-follower-invite-2026-09-08.sh`.
- GitHub Actions Node 24 migration is complete: PR #253 merged from exact approved head `fae86a55d5a90aca139e0e571a27c55b0091a10f` as `29efa030866e5c05b6674f1831495ed8ed406be9`. Active root workflows now use `actions/checkout@v7`, `actions/setup-node@v7`, `actions/cache@v6`, `actions/upload-artifact@v7`, `actions/download-artifact@v8`, `docker/login-action@v4`, `docker/setup-buildx-action@v4`, and `docker/build-push-action@v7`; application Node remains 22. A regression test scans every active root workflow for these exact majors. Local repository/catalog suites, the full exact-head PR matrix, exact-head approval, and clean Codex review passed. Post-merge `main` Catalog list authority, CI Safe Install, Datadog, the complete reusable quality matrix, artifact download/digest verification, and the repository's configured Docker image and Pages publication automation all passed. Check annotations and the complete Build Image log contain no Node 20 deprecation warning; the only annotations are 10 unrelated existing UI lint warnings. No production migration, secret change, manual deployment, or manual workflow dispatch occurred.
