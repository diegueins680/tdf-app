# 2026-08-28

- Diego requested a complete cross-check of CRM contacts without Instagram against the TDF Instagram follower list. The visible follower search was run for all 143 CRM records missing Instagram; exact single-result matches were separated from duplicates, generic names, multi-result names, technical accounts, and no-result searches.
- Twenty-one unambiguous follower-to-contact associations were saved through the visible CRM flow (including the four previously saved in the prior continuation). Duplicate records sharing one Instagram account and ambiguous names remain intentionally unchanged; for the duplicate El Rovlex pair, the Instagram was assigned to the verified company record while the person record was already populated. A short bulk automation was stopped when it attempted to carry a value into the wrong dialog; verification showed no incorrect save, and the affected dialog was cancelled. A direct API attempt returned 401 for every request and made no changes.
- Diego asked to continue the “Tu Escena Conectada” campaign. The authenticated `tdf-instagram` browser profile remains occupied by several Playwright/Chrome instances, so no follower was selected or contacted in this continuation; terminating or releasing an active holder requires current explicit authorization.
- Diego authorized releasing the holder. The stale Playwright/Chrome profile processes were terminated, which freed the profile but also disconnected the `playwright_visible` browser bridge. Its separate generic browser profile reaches Instagram only at the unauthenticated login screen. No campaign message, CRM change, follower selection, or login attempt was made after this recovery attempt.
- With Diego's continued authorization, the isolated `tdf-instagram` Chrome profile was restarted with a recoverable local debugging channel, preserving its authenticated session. The recent follower `nexus_uio` was excluded because the exact campaign DM already existed. One new personalized, standard (non-prioritized) message request was sent and confirmed rendered to `cachocachocacho` / CACHO, a creator who recommends local music and events; the campaign link uses `utm_content=cachocachocacho`. No follow-up, ad, WhatsApp, email, public post, payment, account follow, or CRM mutation was made.
