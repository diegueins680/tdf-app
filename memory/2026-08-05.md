# 2026-08-05

- Continued unfinished work from GitHub Actions `Build Image` run `31015290751`, which failed in
  the quality job while compiling the newly added DDEX ERN 4.3.2 specs. The immediate errors were
  a missing `Data.Text.Text` import and missing `RecordWildCards` in `ParseSpec.hs`.
- Kept the repair isolated in worktree `.tmp/ddex-ui-typecheck-repair` on branch
  `fix/ddex-ui-typecheck` at `6f5ef3468`. No commit, push, PR, or other remote mutation was made.
- Added the DDEX parser and business-rule specs to the main Hspec runner; they had been declared in
  Cabal but were not executed by `test/Spec.hs`.
- Fixed the DDEX parser to select the real XML document root instead of XML Light's `?xml`
  pseudo-element, match child elements by local name across the default DDEX namespace, follow
  nested required-field paths, and correctly parse party names, release references, deal terms,
  and ISO-8601 durations with omitted components.
- Verification in the cached main checkout passed: HQ UI TypeScript typecheck, HQ executable Stack
  build, DDEX parser tests (20 examples), DDEX business-rule tests (11 examples), and the complete
  Haskell suite (2,272 examples). `git diff --check` also passed in the repair worktree.
- Restored the temporary validation mirror from the main checkout after testing. Main was clean
  before this daily memory note was added; the code repair remains uncommitted only in the isolated
  worktree.
- The continuous-improvement supervisor remains stopped/preflight-blocked because its config targets
  `main` while `allowPushToMain` is false. It was not restarted because doing so could create remote
  changes without explicit authorization.
- Committed the DDEX repair as `061d5e25c584da855614bd7854758d767a489355`
  (`DDEX: Fix ERN parser and wire tests`) and pushed branch `fix/ddex-ui-typecheck` to `origin`.
  No PR was created and no branch workflow had started at verification time.
- Detected the unfinished artist-promotion worktree `.tmp/artist-promotion-feature`, whose base was
  already an ancestor of `main` and 3,335 commits behind. Preserved that worktree unchanged and
  ported its changes into `.tmp/artist-promotion-report-current` on current-main branch
  `feat/artist-promotion-report`.
- Completed the artist-promotion integration with admin CRUD/report/PDF routes, the Label Artists
  schedule UI, backend/UI tests, documentation, and the registered production migration
  `2026-08-05_artist_promotion_slots`.
- Artist-promotion verification passed: focused UI tests (3), ESLint, TypeScript, production UI
  bundle, production-release contract tests (21), isolated promotion backend specs (4), isolated
  ServerAdmin specs (95), and the full Haskell executable build/link. The aggregate Haskell test
  target remains blocked on the DDEX test compile defect already fixed by the pushed DDEX branch.
- The artist-promotion branch remains local and uncommitted pending separate publication authority.
- Diagnosed the Codex CLI `MCP startup interrupted` banner from `logs_2.sqlite`. The original
  launch occurred while Docker Desktop was unavailable; `codex_apps` and `openaiDeveloperDocs`
  initialized successfully and were later cancelled as the short-lived CLI process exited.
- Fixed Docker MCP's blocked Git backend by adding the exact tdf-app workspace to
  `MCP_GATEWAY_DOCKER_BIND_ALLOW_WRITABLE_PATHS` under the global `MCP_DOCKER` environment.
  Backed up the prior config as `~/.codex/config.toml.pre-mcp-fix-20260805`.
- Verified unrestricted Codex Doctor connectivity, Docker MCP discovery of Playwright plus Git
  (34 backend tools), and a clean end-to-end Codex launch in which `MCP_DOCKER`, `codex_apps`, and
  `openaiDeveloperDocs` all initialized without startup, interruption, or unsafe-volume errors.
- After authorization, committed the completed artist-promotion feature as
  `45bd44ec73787b71b491bcfb9a43c4cde1b62a59` (`Add artist promotion schedule reporting`) and
  pushed `feat/artist-promotion-report` to `origin`. The remote ref was verified at the exact SHA;
  no PR or branch workflow existed afterward.
- A later unfinished-work audit found the detached platform-auth proof worktree clean and 5,170
  commits behind current `origin/main`, and the original artist-promotion worktree superseded by the
  published branch. The root internationalization batch was intentionally left untouched because
  five active Codex/Qwen processes had the root checkout as their working directory.
- Audited all remote branches against `origin/main`. Six feature/fix branches were already fully
  contained in main; the only valuable unmerged work was `fix/ddex-ui-typecheck` and
  `feat/artist-promotion-report`.
- Created clean integration worktree `.tmp/integrate-valuable-branches-20260805` on local branch
  `integrate/valuable-branches-20260805`, based on `origin/main` at `6f5ef3468`, to avoid disturbing
  the concurrently edited root checkout.
- Merged the DDEX branch in `c311478e1` and the artist-promotion branch in `ada8b9a02`. Resolved the
  sole conflict in `tdf-hq/test/Spec.hs` by retaining the API, DDEX parser/business-rule, artist
  promotion, and event discovery specs in the aggregate test runner.
- The integrated branch is clean and four commits ahead of `origin/main`; both source branches are
  verified ancestors and `git diff --check` passes. Validation passed: production UI build, focused
  Label Artists tests (3), targeted artist-file ESLint, production-release tests (21), and the full
  Haskell suite (2,277 examples, zero failures). The integration branch remains local because this
  merge request did not separately authorize a push or PR.
- After explicit authorization, pushed `integrate/valuable-branches-20260805` to `origin` and
  verified the remote ref at exact merge SHA `ada8b9a02ca52232425a0f237324e2cfdacf8293`.
  GitHub reported no open PRs and no branch workflow runs; no PR was created.
- The post-push audit found no local or remote branches outside the integration branch with unique
  committed work. The remaining unfinished batch is the large uncommitted internationalization
  change set in the root checkout. It was left untouched because three Codex sessions, three Codex
  code-mode hosts, and multiple Qwen processes currently have that checkout as their working
  directory. The clean integration worktree remains synchronized with its remote.
- Adopted all three running Qwen sessions by reading their persisted runtime metadata and final
  transcripts. Session `b15c00ed-e624-4a1d-842f-212512579948` had completed and pushed the DDEX
  implementation; `a9193ef4-def5-49c5-adc4-3c8d9424ac3b` had completed and pushed the storefront
  implementation before its follow-up hit the weekly quota; and
  `f511ae11-f234-41ad-9fbb-1d79dcdaed29` had finished the DDEX UI/CI repairs that were captured in
  `STOREFRONT-4`, with its final lint and TypeScript checks passing before the same quota error.
- Gracefully stopped the three Qwen process groups and terminated their orphaned leaf workers after
  confirming there were no active child shell commands. Rechecked the process table and confirmed
  that no `qwen-code` process remains. Their JSONL transcripts and file-history snapshots remain
  available under `~/.qwen`; no working-tree files changed during shutdown.
