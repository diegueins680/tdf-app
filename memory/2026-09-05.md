# 2026-09-05

- Contextual reputation delivery is integrated in root `main` through merge `857267d0ab51dcb20757fa1b3399161606564e04`; final pre- and post-merge CI, migration-schema verification, and Build Image/Docker packaging passed. `CONTEXTUAL_REPUTATION_ENABLED` remains false; no production deployment, migration execution, pilot, or feature activation occurred.
- Superseded migration-only PR #224 was closed. The mobile companion PR #38 was already merged.
- Four scoped documentation PRs were opened in draft for the real external rollout gates: #229 Product/Legal policy, #230 operations/observability runbook, #232 moderation/admin playbook, #233 staging/accessibility/pilot protocol. PR #234 adds the cross-functional approval tracker. Each is docs-only and #229/#230/#232/#233 checks passed; #234 catalog/build passed while Cloudflare preview was still running. The documentation PRs must remain drafts pending approval by their named human owners; do not merge or activate the flag merely because their CI is green.
- Fixed production access-request submissions failing with PostgreSQL `23514`: `notification_notif_type_check` allowed only four legacy values while the live backend emitted `access_request_submitted`, `access_request_review`, and `access_request_decided` in the same transaction. PR #228 merged as `ce0ff93c346c02a6f66315fb143e3919c52a409a` after all checks and required review passed. Before production mutation, created Fly volume snapshot `vs_6aLP3L8M1Z0s4X947a22OZJ` (digest `74edf2598e780b2dd837ad15d5386ee5608df83172b644826e79139e1c8f8d5b`). Applied only registered migration `2026-09-04_access_request_notification_types`, checksum `200c8405c62fecfcd2479d5d889da511830f39a4ac08d6612c51abbbfbabceab`, under the release advisory lock; ledger is now 68 and the exact seven-value constraint is validated. No app binary or unrelated pending migrations were deployed; both machines remain healthy on `a2e8dbf9930fe900a060195d4b3808fa6f138cc5`. The failed attempts rolled back entirely, so the user must retry the request once.
- Re-verified the live notification constraint and ran a production transaction probe for all three access-request notification types against Paola's party `190`; all inserts succeeded and the rollback left zero probe rows. Replied once in the exact Instagram thread `@jennypao2019` at about 00:38 local: thanked Paola for testing and reporting the issue, confirmed the artist-profile access request problem was corrected, and asked her to retry and report any further issue. Visual and DOM checks confirmed the sent bubble and an empty composer.
- The guarded release dry-run exposed five stale contextual-reputation `introducedBy` SHAs left by earlier branch-history integration. PR #235 corrected only those provenance SHAs, passed all applicable CI, and merged as `6ea83ff6dfbdf759ae5378599a303ab024138480`; the exact-main release plan now succeeds. This was tooling hygiene only: no backend image or unrelated pending migration was deployed for Paola's fix.
- Contextual-reputation rollout documentation: #232 and #234 have merged. PRs #229 (Product/Legal), #230 (operations/observability), and #233 (staging/accessibility/pilot) are approved with auto-merge enabled but must integrate sequentially because protected branches require an up-to-date base. #229 was rebased on current main and its repository/Vercel checks passed; Cloudflare Pages has remained externally pending since 05:59 UTC with no annotations. A dependent, no-auto-merge draft #237 (`c359ae878`) now indexes the five rollout-control documents in `docs/contextual-reputation/README.md`; it must remain draft until #229/#230/#233 merge, so it cannot publish broken links. No feature flag, deployment, migration, pilot, or provider configuration changed.
- Subsequent Codex review comments on #229/#230/#233 were corrected and their threads resolved only after the relevant commits were published: #229 `a8b033189` now covers distinct evaluators, current-consent delivery suppression, ranking/trend withdrawal, security/legal evidence access, all preference/evaluation retention, compatible version renewal, and provisional cases; #230 `132bccb83` defines single-context projections/API selection, invalidation outbox events, submitted-only aggregation, distinct interaction/evaluator thresholds, scheduled temporal decay and public-read-gate-first rollback; #233 `d5be21641` breaks the G0/G2 pilot-gate cycle and makes public-read-gate disablement the first rollback action. All three now await regenerated CI/review; no feature flag, deployment, migration, pilot, or provider configuration changed.
- Contextual-reputation follow-up PR #231 is green and mergeable at head `272691b14736c9d5d507abd5d354fc4779d00ba6` on base `ca64c3cded76b6ebcf099c81a7e1ba2e66290ff7`. It preserves the already-applied `2026-09-04_contextual_reputation_preference_save` bytes and ships the rounding repair as forward migration `2026-09-05_contextual_reputation_preference_rounding`, introduced by ancestor commit `000ef9f0c9be11250eebfbe13e35c373c767512e`. Both review threads are resolved; the later ancestry P1 was false/stale because `000ef9f0c` is the direct parent of the registering commit and an ancestor of GitHub's current synthetic merge, while the hosted production-migrations check passed. The only remaining PR gate is a fresh required human approval after the latest main merge; no merge, deployment, production migration, or flag change occurred.
- Tu Escena Conectada expansion: normalized 142 unique Instagram handles from Diego's three screenshots. Exact CRM comparison found only `arkabuz.galapagos` and `verde70oficial`, both existing contacts without TDF accounts; the other 140 had no exact Instagram match. `1800fiebre`, `a_marte.banda`, `acabaundia`, `andre_farra`, and `armadadejuguete` passed the preliminary public-profile, exact-thread, and CRM checks and are the next local block, but no DM was sent because review occurred around 01:34, outside the approved 10:00–18:00 window. International research produced a no-CRM shortlist led by `17coficial`, `folker_oficial`, `thepicnic.band`, `hendry_dti`, `laiza_valdes`, `lumy_mc`, `ellacentricc`, `chloe.parche`, and `oshikikeigo`; none were messaged and all still require a fresh adult/activity/thread/language check. `dellfireofficial` and `dddriftingcloudsss` do not resolve on Instagram, while `myshaannn` could not be tied reliably to the official artist. Full evidence, drafts, sources, and the 142-handle queue are in `docs/campaigns/tu-escena-conectada-expansion-2026-09-05.md`.
