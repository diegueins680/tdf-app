- Produccion curso grupal: el endpoint publico `/public/courses/produccion-musical*` en Fly devolvia 500 porque produccion no tenia columnas nuevas (`course.stripe_subscription_price_id`, luego tambien `party.stripe_customer_id` y columnas Stripe de `course_registration`). Se aplicaron manualmente las migraciones SQL idempotentes de Stripe del 2026-05-28 en `tdf_hq`. El endpoint quedo 200 y resolviendo el proximo cohort `produccion-musical-ago-2026` con inicio 2026-08-01 y landing `https://tdf-app.pages.dev/curso/produccion-musical-ago-2026`.
- Se intento alinear el secret `RUN_MIGRATIONS=true` con `fly.toml`, pero Persistent empezo a correr migraciones amplias y la app quedo en `starting`; se revirtio el secret a `RUN_MIGRATIONS=false` para recuperar servicio. Leccion: no activar migraciones automaticas en produccion sin ventana/control; aplicar migraciones acotadas o preparar un plan de migracion separado.
