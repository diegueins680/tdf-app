# 2026-07-28

- Added browser-side magnet-link and `.torrent` audio playback to the HQ radio widget using a lazy-loaded WebTorrent 2.8.5 browser bundle and scoped service worker. Uploaded torrent metadata stays session-only; magnet stations persist like other custom stations. Torrent playback selects the largest browser-compatible audio file and closes its P2P session when stopped or switched.
- Replaced the dense inline social-event form with a guided collaborative creator at `/social/eventos/nuevo`. It needs only a title and start time, uses duration/type defaults, autosaves locally, searches venues and teammate accounts, accepts human-readable USD prices, creates private planning drafts by default, and grants selected teammates editor/viewer access to event logistics with partial-failure retry. The full HQ UI suite passed (113 suites, 1,480 tests) and the production build completed.
- Fixed the public `/feedback` flow: the API endpoint now accepts anonymous submissions while optionally attributing a valid bearer/cookie session, the browser includes session cookies, and non-email usernames such as `admin` are no longer copied into the contact-email field. Targeted UI tests, UI typecheck/build, backend compilation, and 23 backend feedback tests passed.
- Repaired production migration ordering in PR #135, then repaired frontend dependency CI in PR #137 by upgrading DOMPurify and React Router, adding exact expiring `audit-ci` exceptions for browser/RSC-inapplicable advisory paths, and updating the Jest/router compatibility layer. PR #137 merged to `main` as `9b4b86db2`; clean install, audit, lint, typecheck, 1,474 UI tests, production build, production migrations, API contracts, and the main-branch safe-install workflow passed.
