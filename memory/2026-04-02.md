# 2026-04-02

- Published the Event Moments work in two repositories.
- In `tdf-mobile`, committed `feat: add event moments feed` as `83bbee5` on `codex/mobile-publishing-audit-docs` and pushed it to `origin`.
- In the root `tdf-app` repo, committed the `tdf-hq` Event Moments backend plus the updated `tdf-mobile` submodule pointer as `f6f9b42f` with message `feat: add shared event moments`, then pushed `codex/service-marketplace-admin-users` to `origin`.
- Deliberately left unrelated dirty worktree changes in `tdf-hq-ui`, `tdf-hq`, and `tdf-mobile` untouched.
- Implemented the architecture cleanup pass across backend, web, mobile, CI, and docs:
  backend session cookie support plus `/session`, frontend permission source-of-truth and lazy route splitting, boot extraction from `app/Main.hs`, mobile OpenAPI type generation, and a new PR CI workflow.
- Verification passed with `npm run typecheck --workspace=tdf-hq-ui`, targeted Jest auth/routing tests, `npm --prefix tdf-mobile run typecheck`, `npm --prefix tdf-mobile run test -- --runTestsByPath __tests__/AuthScreen.test.tsx`, `npm run build --workspace=tdf-hq-ui`, and `stack test` in `tdf-hq`.
- Continued the backend cleanup by extracting auth/session flows out of `tdf-hq/src/TDF/Server.hs` into `tdf-hq/src/TDF/ServerAuth.hs`, wiring the new module into `tdf-hq.cabal`, and updating `TDF.ServerSpec` to import password-reset helpers from the extracted module.
- Verified the extraction with `stack build tdf-hq:exe:tdf-hq-exe` and `stack test`; the backend test suite still passes with 130 examples and 0 failures.
