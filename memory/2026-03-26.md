Continuous improvement loop discovery policy update:
- Replaced the UI-only fallback with an explicit two-lane scheduler in `scripts/lib/discovery.mjs`.
- The scheduler now keeps `backend` and `ui` lanes, prefers real findings over synthetic fallback prompts, and alternates equal-priority lanes using persisted state in `tmp/continuous-improvement-loop-discovery.json`.
- Added formal verification for the discovery scheduler and extended `verifyImprovementLoopModel()` so fairness/non-starvation is checked alongside the loop state machine.
- Verified with `npm run verify:auto-loop`, `npm run test:auto-loop`, and two consecutive `npm run loop:idea` runs that emitted `backend` then `ui`.
