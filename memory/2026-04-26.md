- Se reemplazo la imagen hero de la pagina publica de Domo del Pululahua por un asset local generado con un ambiente limpio, acogedor y de alta gama. Tambien se agregaron tres imagenes de apoyo para terraza, entrada iluminada y jardin nocturno en `tdf-hq-ui/public/assets/tdf-ui/`, y `DomoVenuePage.tsx` ahora usa esos assets locales.
- La seccion "Sesiones en vivo TDF" de Records dejo de usar videos hard-coded en el frontend. Ahora consume el slug CMS/BD `records-sessions` con `videos[]`, mantiene fallback legacy para `records-session-*`, y el seed del backend crea la coleccion con la playlist TDF Live Sessions si no existe.
- La seccion "Lanzamientos TDF Records" ahora consume primero el slug CMS/BD `records-releases` con `tracks[]` y mantiene fallback legacy para `records-release-*`. El seed del backend agrega el playlist Spotify "RELEASES by TDF" (`4FSMAk7z9GFk4pUH9Uffbt`) con 67 tracks, portada compartida y enlaces Spotify. Validado con `npm run typecheck --workspace=tdf-hq-ui`, tests CMS enfocados, `npm run build --workspace=tdf-hq-ui` y `cabal build`. El deploy automatico de backend quedo bloqueado por el submodulo `tdf-mobile` apuntando a un commit que GitHub no puede fetch, falla preexistente en Build Image.
