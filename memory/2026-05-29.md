# 2026-05-29

- Fixed the TDF HQ blank-page recovery path after browser back/navigation:
  - Removed the blocking ChatKit CDN script from `index.html`; ChatKit now loads lazily only when the ChatKit UI is opened.
  - Added an app-level error boundary so route render failures show a reload action instead of a white page.
  - Added Cloudflare Pages `_redirects` SPA fallback for deep links.
  - Verified focused tests and production UI build; repo-wide lint still has pre-existing failures outside the touched files.
- Fixed a public booking backend path that could surface as a generic `Internal server error` on the reservation page:
  - `TDF.Server.runDb` now catches `ServerError` exceptions raised inside DB actions and rethrows them through Servant `Handler`, preserving intended 400/409 responses.
  - Public/admin booking creation now runs `resolveResourcesForBooking` through `runDB`, so unavailable or malformed room/resource selections return actionable booking errors instead of escaping as 500s.
  - Added a regression spec for DJ booth resource conflicts, but `cabal test tdf-hq-test --test-options='--match /resolveResourcesForBooking/'` is currently blocked by unrelated stale spec compile failures in `ServerAdminSpec` and older `ServerSpec` fixtures. `cabal build tdf-hq-exe` passes.
