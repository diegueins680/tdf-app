# Memory 2026-05-23

## Social inbox: native Instagram DMs not visible in TDF HQ

- `/social/inbox` reads backend `instagram_message` rows through `/instagram/messages`; it does not live-poll Instagram's native inbox.
- Instagram rows are populated by Meta webhook ingestion (`/instagram/webhook`) or by the explicit `/meta/backfill` flow.
- The deployed inbox screenshot showed only old failed Instagram rows, so the UI collapsed to `Fallidos`. Recent native Instagram threads were not present in the backend table.
- Current repo docs still record Meta App Review as not approved for Instagram messaging permissions, which likely blocks production/non-role inbound access unless webhook/backfill permissions are restored and the asset is reconnected.

## Instagram backfill attempt

- Ran production `/meta/backfill` for `platform=instagram`, `conversationLimit=100`, `messagesPerConversation=50`, `onlyUnread=true`, `dryRun=false`.
- Result: `imported=0`, `incomingScanned=0`, `successes=0`, `failures=2`.
- Failure causes returned by Meta:
  - One source: app does not have the capability to make the conversations API call.
  - One source: access token expired on Saturday, 23-May-26 17:00:00 PDT.
- Follow-up count check still showed 100 Instagram incoming rows, all failed, newest row from 2026-05-13.
- Later confirmed the deployed Instagram token can read `/{FACEBOOK_MESSAGING_PAGE_ID}/conversations?platform=instagram`, while `/{INSTAGRAM_MESSAGING_ACCOUNT_ID}/conversations?platform=instagram` returns the capability error. This points to a backend target-path bug in the Instagram backfill.
- Ran a manual production backfill through the working Page conversations endpoint, fetched 50 recent conversation batches, filtered inbound messages newer than the newest existing app row, and posted 37 signed Instagram webhook events into the running Fly app.
- Verification through `/instagram/messages?limit=150&direction=incoming` showed 37 newly ingested rows at `2026-05-24T02:56:04Z`; at verification time those were 16 replied, 17 failed, and 4 still pending.
