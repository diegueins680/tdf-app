# 2026-06-14

- Implemented mobile fanclub live broadcast feature in `tdf-mobile`: Event Detail now has an `En Vivo` tab, follower-gated broadcast studio with camera/mic permissions, live broadcast cards, local/remote repository fallback, and tests.
- Key caveat: current mobile dependencies provide camera preview and stream session orchestration, but native camera-to-WHIP/RTMP publishing still needs a dedicated mobile media publisher module. The feature preserves `playbackUrl`, `ingestUrl`, `whipUrl`, and `streamKey` for that integration.
- Continued the feature into `tdf-hq`: added event-scoped live broadcast DTO/API/model/handler support for `/social-events/events/:eventId/live-broadcasts`, follower-only access, broadcaster-owned end, heartbeats, and radio-style playback/ingest/WHIP endpoint generation.
- Backend live-broadcast follow-up verified cleanly: `stack build --fast` passed, and `stack test --fast --test-arguments "--match \"social event live broadcast request FromJSON\""` passed 4 examples for the new DTO decoders.
- Continued mobile fanclub live broadcasting to real WHIP publishing: restored the current `tdf-mobile` checkout’s live feature, added `react-native-webrtc`, a WHIP publisher helper, `En Vivo` event tab, follower-gated artist selection, live session repository/API/local fallback, permission updates, iOS pods, docs, and focused tests. Verified `npm run typecheck`, focused Jest, targeted ESLint, `pod install` with UTF-8/network, and `npm run expo:config`.
- Fixed mobile menu access drift in `tdf-mobile`: added explicit `Crear` and `Inventario` tab menu entries, moved tab/quick-action metadata to `src/navigation/menu.ts`, and made quick actions reach direct event creation, inventory, venue explorer, and user profile. Verified with `npm test -- NavigationMenu.test.ts` and `npm run typecheck`.
- Finished reachable ticket-sales flows: wired `tdf-hq-ui` event Tickets section into `StripeCheckoutModal` and corrected the Stripe payment-intent payload to backend `ticketPurchase*` fields. Added mobile ticket tier/order API mappings plus in-app event-detail tier selection, purchase posting, and order display. Verification passed focused web checkout tests/typecheck/lint and mobile API/event-detail tests/typecheck/lint. Caveat: mobile still does not have native Stripe SDK/PaymentSheet, so mobile card capture remains a dependency gap.
- Implemented rolling production-course scheduling in `tdf-hq`: `/curso/produccion-musical-jun-2026` and other production course slugs now resolve/create a cohort whose first session is at least 28 days out; as of 2026-06-14 the next generated start is 2026-07-18. Verified focused Haskell tests. Did not deploy because the worktree contains unrelated dirty Social Events/Ticketing changes.
- Closed the mobile Stripe parity gap in `tdf-mobile`: installed `@stripe/stripe-react-native@0.50.3`, added the Expo config plugin and `StripeProvider`, wired Event Detail ticket purchase to `create-payment-intent` + native PaymentSheet using `Constants.API_VERSIONS.CORE`, added env knobs for publishable key/merchant id, and ran `pod install` with UTF-8 env so iOS `Podfile.lock`/Xcode resources include Stripe pods. Verification passed typecheck, focused Jest, targeted ESLint, `expo config --type public`, and CocoaPods install.
- Started the TDF artist-fan engagement upgrade: added root artist profile/search/public/profile-photo API routes, direct public artist slug lookup, engagement_event persistence for follow/unfollow, and first-follower notifications. Frontend fans API/path tests and `tdf-hq-ui` typecheck passed; Cabal backend verification compiled changed DTO/profile/API modules but failed in shared `dist-newstyle` with a filesystem rename collision while another build was active.
- Continued the TDF artist-fan engagement verification in an isolated Cabal build directory. Patched a stale `test/Spec.hs` expectation so `validateEventArtistIds` matches the stricter existing contract requiring explicit `eventArtists[].artistId`; `cabal test tdf-hq-test --builddir=/private/tmp/tdf-hq-cabal-artist-engagement --disable-optimization --test-options='--match Artist'` passed 40 examples.
- Finished the mobile PaymentSheet cleanup path: Event Detail now cancels the pending ticket order when the user dismisses native card capture, and `tdf-hq` lets the buyer cancel only their own pending order while preserving manager-only access for other status changes. Backend `cabal test` was started and compiled the edited SocialEvents handler, but the full suite was stopped after a long quiet build; a separate pre-existing Stack/GHC build was left alone.
- Triggered an Android production EAS build for `tdf-mobile` from latest green ref `70cadd5`: https://expo.dev/accounts/cuco.saa/projects/tdf-mobile/builds/4dfbe3ce-f10a-4bbb-b5ef-28eb08315e02. EAS incremented Android `versionCode` from 13 to 14 during build preparation.
- Ran `eas submit --platform android` for build `4dfbe3ce-f10a-4bbb-b5ef-28eb08315e02`; EAS scheduled submission `64e6b2f3-935a-4b63-baf7-1a5eb436f0c8` to Google Play internal track as DRAFT, but Google Play rejected automation because the app's first Android submission must be performed manually in Play Console.
- Retried Android EAS submit with `--latest --non-interactive`; EAS scheduled submission `88c93104-314c-4645-8424-12d1506c57f5` for the same latest green build (`4dfbe3ce`, `tdf-mobile` commit `70cadd591`, app `1.0.1`, versionCode `14`), but Google Play returned the same first-manual-submission blocker. Downloaded the AAB for manual Play Console upload to `/private/tmp/tdf-mobile-1.0.1-v14-70cadd591.aab`; SHA-256 `d6d57a358c9c7b6ab3bbf98395fc56094afdba16d8e6ed89f19fdc5bbb6ceb32`.
- While checking release readiness on pinned `tdf-mobile` commit `70cadd591`, `npm run release:check` failed on lint only: one `no-explicit-any` in `__tests__/Analytics.test.ts` and stale `no-console` disable comments in `src/analytics/posthog.ts`. A temporary narrow patch made `release:check` pass, then was reverted so the workspace stayed pinned to the EAS artifact commit.
- Re-applied and verified the mobile Stripe PaymentSheet work in the active `tdf-mobile` checkout: added ticket tier/order/payment types and API calls, Event Detail native card-capture purchase UI, cancellation cleanup, Stripe provider/config/env knobs, mapper tests, and iOS Pods. Verification passed `npm run typecheck`, focused Jest for `socialApiMapperSanitization` + `EventDetailMoments`, targeted ESLint with `--max-warnings=0`, `npx expo config --type public`, and UTF-8 `pod install`.
- Continued SPEC-1-TDF Fan Hub refactor in `tdf-hq-ui`: split release player helpers, `useReleaseFeed`, `FanProfileEditor`, `FollowedArtists`, `ProfileSectionCard`, and `FanClubPreview` out of `FanHubPage.tsx`, reducing it from 2,558 to 2,334 lines without intended behavior changes. Verification passed focused Jest (`FanProfileEditor`, `FanClubPreview`, `ReleasePlayerActions`), targeted ESLint on touched frontend files, and `npm run typecheck`.
- Continued mobile Stripe validation to full project checks: fixed the existing analytics lint blockers (`as any` in `Analytics.test.ts` and stale `no-console` disables in `posthog.ts`), then `npm run lint -- --max-warnings=0`, `npm run typecheck`, full `npm test -- --watch=false` (26 suites/136 tests), and `npm run release:check` all passed in `tdf-mobile`.
