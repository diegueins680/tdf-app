# 2026-09-08

- Cleared the full 102-warning `tdf-hq-ui` ESLint backlog in isolated branch `chore/clear-ui-lint-annotations`, preserving logical-OR fallback behavior with a tested `firstNonEmptyString` helper and replacing unsafe `any`/hook dependency patterns.
- Made UI lint warnings fail CI via `eslint "src/**/*.{ts,tsx}" --max-warnings=0`.
- Stabilized the large course-registration provider-title matrix with a documented 15-second per-test budget after the default 5-second timeout reproducibly cascaded under the full suite; the unchanged assertions pass both alone (526/526) and in the complete run.
- Exact local `npm run quality:ui` passed: 185 suites, 1,749 tests, production build, and 412,543-byte gzip initial-JS budget. `npm run quality:repo`, `npm run audit:catalog-lists`, and `git diff --check` also passed.
- PR #261 (`UI: eliminate lint warning backlog`) passed every hosted check. The exact-head UI check on `2cc270321959a206dc39fd11bd40ef58b0114ea8` completed with zero annotations; Codex found no major issues, `tdfrecords` approved that exact head, and there were no review threads.
- Merged PR #261 as `a3196ba046ec1d28a2897d8f832a229cda450eab`. Automatic post-merge catalog audit, safe install/audit/build/test, Datadog workflow, Cloudflare Pages, and Vercel publication all completed successfully with zero check annotations. No manual deployment or workflow dispatch was performed.
- Added event-card deletion controls for organizers and strict admins, with confirmation, inline failure feedback, cache refresh, and an encoded client API call. The backend now enforces organizer-or-strict-admin deletion instead of allowing ownerless imported events to be claimed during deletion. Focused UI/API tests (27), UI typecheck, zero-warning focused lint, and compilation of the changed Haskell handler/test modules passed; the monolithic Haskell test link was stopped after an excessive quiet rebuild, so the new pure authorization example remains compiled but not executed. No event was deleted and nothing was deployed. A read-only live API check at 02:08 local returned 36 upcoming public events, including KEN-Y and both manual events, confirming the earlier two-card screenshot was transient while automated publication reconciliation was pending.
- Repaired PR #259's failing catalog-list gate by refreshing the reviewed production-migration registry fingerprint (`f9b0d054d59c83140a5b` -> `4ea8cb0894ec797bc01e`). Local catalog audit, repository quality, production-release tests, staging-baseline tests, and diff checks passed; hosted catalog, migration, backend, persona, safe-install, Cloudflare, Vercel, and aggregate quality checks all passed on head `a1ee18345475fd8d0eb8579cb53b99699033d0f4`.
- PR #259 auto-merged as squash commit `46f6ecdc78e49c6745fd2c9450c5763dc60d71dd` before its exact-head Codex review completed. The completed review then correctly flagged that the forward repair migration's `introducedBy` SHA (`269f3784...`) was an unrelated catalog merge rather than stable studio-audit provenance.
- Opened follow-up PR #263 with clean one-line head `7b2b65a9ccd383270bb1b79edd58a68fda5f701a`, anchoring the repair migration to the original studio-audit feature commit `dac84b099b18b51032fb94f58273120f5375eb85`. All CI/deployment checks and exact-head Codex review passed with zero threads; `tdfrecords` approved the exact head and protected auto-merge produced main commit `64ca07833dab3ad6df6e527466b8895290faf877`.
- Verified PR #263 post-merge catalog audit, safe install/audit/build/test, Datadog, Cloudflare Pages, and Vercel checks all passed. The final `main` manifest contains the corrected provenance. Removed the redundant remote branches for merged PRs #259 and #263 after confirming neither had an open PR; their history remains recoverable from the merged PRs and commits.
- Staging deployment of merged PR #259 (`46f6ecdc`) reached the checksum-reconciled audit migration and correctly applied its forward repair, then exposed separate inherited schema drift: `notification.notif_type` is nullable, while the 2026-09-04 access-request notification migration requires non-null text. A temporary staging-only deploy with automatic migrations disabled allowed safe read-only inspection: column is nullable but has zero null rows; no production mutation occurred. Draft PR #265 (`fix/staging-notification-nullability`) adds an idempotent non-null repair migration plus safe handling in the unapplied migration, preserves data by failing explicitly if nulls exist, and has local Docker migration, release-contract (46/46), and catalog gates passing. Keep staging `AUTO_APPLY_PRODUCTION_MIGRATIONS=false` until #265 passes CI/review and the separate provenance follow-up #263 is merged/rebased.
- PR #265 auto-merged its earlier head `fc8da31b1bf11a7d8c1ec00f0ec3b854254c0dba` as `d5ecbf3f97f62940c7d66537e5b9a1141c45128c` before the final hardening commit landed. The omitted hardening corrected the forward notification repair's `introducedBy` from unrelated studio-audit merge `46f6ecdc...` to the original notification feature commit `7bee4009634844fe20a02bd3bce09eeb4bdb05e3`, and strengthened the Docker rehearsal with real NULL rows to prove both migrations fail closed without deleting rejected data.
- Opened follow-up PR #266 from merged `main` with clean exact head `fbbda77354bc2191b0124ad1c6b002ea4cd8db71`. Local Docker notification migration, production-release (52/52), catalog-list audit, repository quality, shell syntax, ancestry, and diff checks passed. Hosted migration, production-migration, repository, persona, safe-install, catalog, Datadog, Cloudflare, Vercel, and aggregate quality checks passed; exact-head Codex review completed with no findings, no review threads existed, and `tdfrecords` approved the exact commit.
- Protected squash merge produced PR #266 main commit `3b91a9b05ddd8bbab15274f21ee9af68fe54d4ae`. Verified remote `main` and the merged two-file diff, then verified post-merge catalog authority, safe install/audit/build/test, Datadog, Cloudflare Pages, and Vercel all green. No manual deployment, migration, or workflow dispatch was performed. Removed the redundant remote branches for #265 and #266 after confirming they were merged and no open PR referenced them; their commits remain recoverable from PR history.
- Added a conditional “Compartir entradas” action to the protected social-event detail header. It appears after at least one ticket tier loads, shares the canonical public `/eventos/:id/entradas` checkout URL through the native share sheet, falls back to the clipboard, treats share-sheet cancellation as neutral, and reports success/failure inline. Three focused UI tests, the full UI typecheck, focused zero-warning lint, and diff checks passed. Nothing was deployed.
- At 02:46 Diego said to continue the authorized Instagram-follower outreach. The one-shot job `e126436f-c562-4f7e-bda2-8a7fac85f456` remained enabled for 09:00 `America/Guayaquil`; no early DM was sent. The OpenClaw Chrome browser was accepting CDP connections but all page targets were unresponsive, so the dedicated browser was cleanly stopped and restarted with the same persistent profile; the authenticated `@tdf.records.label` session and profile (4,091 followers, 342 posts) then passed a full snapshot. A read-only follower scan collected 106 current followers. Public-profile review plus a production `BEGIN READ ONLY` CRM audit produced the revalidation queue `gabbydiazart`, `biieege`, `domingo.caballero.techno`, `percuto`, `dj_robalino`, with `almaconvoz`, `sophiesilvermusic` and `fusionimperium` as alternates; none matched the CRM by exact Instagram or expected exact public name during preparation. `amwordhouse` (`partyId 169`), `klausrevolucion` (`176`), `nigredo.hex` (`181`), `iraizoviedocanta` (`185`) and `calavery.inc` (`186`) were excluded because exact CRM contacts already exist; `cachorroblackec` and `calandriasincendiandoelespejo` lacked sufficient music-specific fit. The scheduled prompt was updated to require fresh follower/profile/thread/CRM checks before every send, retain the five-DM block cap, and stop on any ambiguity or Instagram warning.
- A read-only staging audit after #266 found the supported legacy baseline exactly: `notification.notif_type` is unconstrained unbounded `varchar`, `NOT NULL`, has zero NULLs, and retains the valid `test_sink_captured` / `test_sink_failed` rows; neither notification repair appeared in the migration ledger. No staging data or configuration was mutated, and `AUTO_APPLY_PRODUCTION_MIGRATIONS=false` plus `RUN_MIGRATIONS=false` remain unchanged. PR #267 auto-merged as `807a53af382d2db243c6d36a515d51ddb72f076f` but its exact-head Codex review then exposed two P1 provenance/verification gaps. Consolidated follow-up PR #269 exact head `f9b3511c6b239c57b9555b5efeb746401483ef47` accepts only a truly unconstrained baseline, fails closed on malformed/unvalidated notification checks, losslessly widens varchar to text in a new forward migration, restores the released nullability repair byte-for-byte (SHA-256 `f83049fb3f27f4d57ac2c5594849ec10cef83b847363a25e76712f9369d2cf5d`), and records the briefly released alternate checksum compatibly. Local repository/catalog/release/Docker/full PostgreSQL 17 migration rehearsals and every hosted check passed; exact-head Codex completed with no findings and all #269 threads were resolved. After exact-head `tdfrecords` approval, protected squash merge produced `main` commit `714060ffcc1db56448f6d3cb82e055e0085af4e7`; its tree exactly matches the reviewed head, the remote feature branch was deleted, and post-merge Catalog, Safe Install, Datadog, Build Image, Cloudflare, and Vercel all passed. Both P1 threads on #267 are linked to #269 and resolved. No manual deployment, migration, staging mutation, or workflow dispatch occurred.
- Campaña **Tu Escena Conectada — seguidores**: el preflight comenzó a las 09:00 dentro de la ventana autorizada y confirmó un total diario previo de 0 DMs. Antes de revisar seguidores, hilos o CRM se detectó una sesión concurrente: el navegador OpenClaw estaba abierto en `tdf.records.label` y otro Chrome persistente `tdf-instagram`, iniciado el 7 de septiembre, mantenía abierta la bandeja de mensajes. Se detuvo el bloque a las 09:04 sin cerrar ni reemplazar perfiles, sin seleccionar destinatarios, sin enviar DMs y sin crear o editar contactos. Corte final: 0 DMs confirmados, 0 contactos CRM verificados y 0 conversiones; informe `docs/campaigns/tu-escena-conectada-seguidores-2026-09-08.md`.
- After #269 merged, a separate concurrent process created studio-audit staging release v16 at 23:36 UTC; this session did not initiate the deploy or migration. Its startup temporarily enabled the reviewed migration lane, applied `2026-09-08_notification_notif_type_text_compatibility` at 23:38:01 UTC, passed the full schema verifier, and returned the final Machine to `AUTO_APPLY_PRODUCTION_MIGRATIONS=false` / `RUN_MIGRATIONS=false`. Read-only verification found `notification.notif_type` is now unbounded `text` and `NOT NULL`, zero NULLs, no type allowlist, and the `test_sink_captured` / `test_sink_failed` rows preserved exactly once. The ledger retains the #267-compatible access/nullability checksums (`90a2e34...`, `79422886...`) and records forward checksum `e495d71e...`; public health is HTTP 200 with DB/status `ok`. The live release is not provenance-identical to the reviewed `fly.studio-audit-staging.toml`: `/version` reports `commit=dev`, and Machine configuration uses a different volume/assets path, preview-origin defaults, and auto-stop settings. No reconciliation was attempted because that requires a separately authorized staging mutation.
- Staging notification compatibility was subsequently deployed manually under the approved controlled runbook. PR #269 merge `714060ffcc1db56448f6d3cb82e055e0085af4e7` was deployed as Fly staging release v16 on clean machine `080e906db375e8`. With public reputation and aggregation worker still disabled, a one-time `AUTO_APPLY_PRODUCTION_MIGRATIONS=true` run applied `2026-09-08_notification_notif_type_text_compatibility` and passed the complete schema verification. Read-only verification: `notification.notif_type` is `text|NO`, ledger checksum is `e495d71e1f6553735351d58edd65afbb4cb0a5aaa841112937c3b068faeec204`, `/health` returned `{db:ok,status:ok}`, and effective flags are `auto=false`, public=false, worker=false, mode=simulation. The old staging machine was destroyed only after creating a snapshot; its detached encrypted volume `vol_r682dn7k9x9j71j4` and snapshots remain preserved. Current clean attached volume is `vol_vdej5owg087momw4`.
- At 18:48, after Diego again said to continue the Instagram-follower campaign, the persistent `tdf-instagram` Chrome that had blocked the 09:00 run was audited as orphaned: its parent was PID 1, CDP port 9333 had no connected clients, and no OpenClaw durable task was running. It was closed gracefully through CDP `Browser.close`; the browser profile, cookies and files were left untouched. Port 9333 and the old PID then disappeared, while the authenticated OpenClaw profile remained healthy on `@tdf.records.label`. No DM was sent outside the 09:00–18:00 window. A replacement one-shot command job, `70df8ef3-e7ae-436c-9462-62bed8d2f773`, was scheduled for 2026-09-09 09:00 `America/Guayaquil`, deletes itself after success, and runs `scripts/run-instagram-follower-invite-2026-09-09.sh`. Its prompt preserves the five-message block cap and ten-per-day ceiling, freshly revalidates follower/profile/thread/CRM eligibility for every candidate, invites recipients to both events and to test TDF, stops on any ambiguity or Instagram warning, and writes the Sep 9 evidence report before claiming success.
- At 19:00 Diego expressly extended the campaign's permanent Instagram operating window from 09:00–18:00 to 09:00–20:00 `America/Guayaquil` and asked to continue immediately. Both the current and next-day execution prompts were updated so that the local time is rechecked before every send and no new DM starts after 20:00.
- Contextual-reputation/staging hardening continued without deployment. PRs #276, #277 and #280 merged with Axe regressions for category priority, contextual person rankings, and public aggregate-only reputation respectively. Axe exposed and #277 fixed invalid ordered-list nesting in `ContextualRankingPrototype`. PR #281 then aligned the tracked studio-audit staging manifest with the observed safe runtime (`AUTO_APPLY_PRODUCTION_MIGRATIONS=false`), while adding an explicit Fly `release_command` preflight: it temporarily enables only the reviewed checksum-pinned bundle, completes full schema verification, and exits before starting the backend. Ordinary cold starts therefore remain non-mutating. The P2 review thread was resolved only after 15 focused entrypoint/staging tests and 54 production-release tests passed, the full hosted matrix was green, exact-head `tdfrecords` approval landed, and a completed Codex review had no new findings. Protected auto-merge produced main `7f60f6ec03171a1b6e558bf8247f59e5914ea8f0`; no Fly deploy or runtime configuration mutation was performed.
- PR #285 completed the remaining renderer-localization gap without deployment. It localizes contextual-ranking and public-reputation copy (including keyboard controls, live-status strings and accessible labels) across ES, EN, FR, DE and PT. The locale completeness suite now enforces keys and interpolation variables for both namespaces. An initial P2 correctly identified partial non-ES/EN resources; it was fixed in exact reviewed head `2fd09fe93cd8b272caeb6702463c0e6b0de2bce3`, with full hosted CI green, resolved thread and exact `tdfrecords` approval. Squash merge produced main `10c74d003a567c123abdc8b1a313e540325175fb`. No feature flag, worker, Fly deployment or runtime configuration was changed.
- The resumed follower block confirmed `gabbydiazart` was still a follower and publicly active as an adult Ecuadorian techno/hard-techno artist. Her exact thread had only old story mentions and no campaign invitation, objection or opt-out; a fresh production read-only audit found no exact CRM/name match or user account. At 19:43 one combined DM inviting her to both September events and to test TDF was confirmed as a single complete bubble with the correct `utm_content=gabbydiazart`, empty composer and subsequent Seen state. Only afterward, an idempotent transaction created and uniquely revalidated `Gabby Díaz` as Persona `partyId 213`, Instagram `gabbydiazart`, `has_user=false`; no conversion was attributed. Persistent OpenClaw transport latency left insufficient time to safely validate another recipient before 20:00, so the day closed at 1 DM and 1 CRM contact. The Sep 9 one-shot remains scheduled but its prompt now limits the remainder to four and explicitly excludes Gabby.
- Diego said `Continue` again at 19:52. A read-only return to the TDF profile was attempted for the next candidate, but CDP timed out before the followers list could be deterministically reopened. The retry stopped at 19:55 with no candidate profile/thread opened, no composer text, no send, no follow-state change and no CRM write. The Sep 8 total remains 1 confirmed DM and 1 verified CRM contact; the four-message remainder stays scheduled for Sep 9 at 09:00.
- Diego again said `Continue` at 19:57. With fewer than three minutes remaining, no new DM was started. Read-only diagnostics showed the Instagram Chrome target still present but OpenClaw gateway/CLI calls intermittently timing out; the log also showed numerous unrelated background agent crons repeatedly failing on Anthropic billing, which likely contributed load but were not modified. Added `scripts/instagram-openclaw-cdp.mjs` as a local direct-CDP fallback for the Sep 9 executor and hardened its prompt to use supported CLI syntax, refresh every DOM/snapshot reference, never touch Follow/Remove controls, and preserve all profile/thread/CRM/bubble checks. The helper performs no autonomous selection or send.
- After the 20:00 Instagram cutoff, Diego said `Continue` and only technical recovery proceeded. The wedged managed Chrome was closed cleanly through CDP `Browser.close`, preserving the profile and cookies. OpenClaw doctor found a healthy gateway/plugin/profile, no `Singleton*` locks and no port conflict, although two supervisor launch attempts timed out. A direct launch with the same managed flags restored Chrome 152 on port 18800. The user-data directory has an unauthenticated `Default` (`openclaw`) subprofile and the authenticated `Profile 1` (`Diego`); opening the latter restored the `tdf.records.label` inbox, updated `Local State.profile.last_used` to `Profile 1`, and the diagnostic login tab was closed. Final OpenClaw tabs/snapshot and the hardened direct-CDP helper all verified the authenticated inbox; the helper now rejects targets that cannot prove `tdf.records.label`. The inbox also showed that Gabby reacted `🙌` to the invitation; no follow-up or conversion attribution was made. One-shot job `70df8ef3-e7ae-436c-9462-62bed8d2f773` remained enabled for Sep 9 09:00 with four DMs maximum. No post-cutoff DM or CRM write occurred; Sep 8 totals remain 1 DM, 1 CRM contact and 0 conversions.
- At 22:35 another `Continue` triggered only out-of-window technical prep. The CDP port and authenticated inbox were healthy even though an overloaded gateway status call transiently reported `running: false`; direct `lsof`, `/json/version` and the identity-gated helper proved Chrome 152 was still listening. The Sep 9 launcher now performs its own deterministic identity preflight, opens only `Profile 1` if the probe fails, enforces an actual 45-second overall deadline with four-second probe timeouts, and tells the executor to stop without sending if `tdf.records.label` cannot be proven. Normal helper diagnostics keep their 30-second timeout. POSIX shell syntax, the pinned Node path, the live short-timeout identity probe and diff checks passed. The one-shot schedule and Sep 8 totals are unchanged.
- Diego then expressly authorized reconciliation of the drifted studio-audit staging release. Fresh rollback snapshots were created for the API volume (`vs_YnmGJkoMzALLUxQKXzoK`, digest `67afd39f...`) and database volume (`vs_oZ7ywx8JY17fnxN975LJVvO`, digest `082acc51...`) before deploying the exact reviewed #269 CI image as Fly release v17 (`VRl7314XXwvlkH3omPlby76Q`), amd64 digest `sha256:38318e0b6c1c6d64748de83715b25a16214f5006f62f6beab0808ff5e712f99f`. The single API Machine remains started with auto-stop disabled/minimum one, the active clean volume `vol_vdej5owg087momw4` preserved, exact staging CORS, sandbox providers, automatic reviewed migrations enabled, ORM migrations/reset/seed and all discovery/enrichment/reputation workers disabled, and evidence isolated under `/data/audit-evidence`. `/version` now reports commit `714060ffcc1db56448f6d3cb82e055e0085af4e7` and build time `2026-09-08T23:35:06Z`; `/health` is HTTP 200/DB OK; startup reported every migration already applied and schema verification passed. CORS allows only the staging web origin and rejects `https://evil.example` with HTTP 400. Read-only SQL reconfirmed `notification.notif_type` is unbounded `text NOT NULL`, zero NULLs, both synthetic rows exactly once, and checksums `90a2e34...`, `79422886...`, and `e495d71e...`; the evidence table remains empty and the volume still contains the same 13 assets (~1.5 MiB). PR #278 persisted the active volume and explicit reputation safety settings with a regression test; full local studio-audit/config validation and every hosted check passed, Codex found no issues, `tdfrecords` approved exact head `341f39f15b6f536fc7e03ac8127e2d759fdc64bd`, and protected merge produced `f4412560fc71191c525c7c555d0289d5c26d220e`. Concurrent PR #277 became the first parent immediately before merge; #278's two reviewed files remained byte-identical and the merge changed only those files relative to that parent. All post-merge checks passed and the remote branch was deleted. No production app or production database was changed; the old detached staging volume remains preserved.
- PR #282 persisted the verified staging v17 reconciliation in the studio-audit deployment/test records and contextual-reputation rollout evidence. Codex's initial P2 caught a stale claim that PR #200 remained open; exact head `fab19a49b917d42cedd6f8dbe2fb364c0c68a6d7` corrected the row with its 2026-08-25 merge provenance. Focused audit tests passed 8/8, full repository quality and every hosted exact-head gate passed, Codex's fresh review found no issues, and `tdfrecords` approved the exact head. Protected merge produced `0d7996b42c09f8c363519d40921502737516e4dc`; its first parent was the reviewed base, only the three intended evidence files changed, and all were byte-identical to the approved head. Post-merge Catalog and Datadog workflows passed, the remote branch was deleted, and no deployment or production mutation occurred.
- PR #281 moved staging's checksum-pinned migration/schema verification from every ordinary API start into Fly's explicit pre-release gate. The committed runtime now keeps `AUTO_APPLY_PRODUCTION_MIGRATIONS=false` and `RUN_MIGRATIONS=false`; `release_command` temporarily enables only the reviewed SQL runner with `TDF_MIGRATION_PRECHECK_ONLY=true`, exits after verification, and prevents a failed release from starting. Entrypoint and manifest regression tests were added. Exact reviewed head `ae7d719c24d82e635d145115ce9fd5dfdcdb4d86` merged as `7f60f6ec03171a1b6e558bf8247f59e5914ea8f0` while the runbook consistency follow-up was being validated; its five-file merge diff was as intended, and Build Image, Safe Install, Catalog, and Datadog post-merge workflows passed.
- PR #284 then corrected the remaining runbook contradiction by changing its required steady-state value to `AUTO_APPLY_PRODUCTION_MIGRATIONS=false`, documenting the exact release command, and asserting both facts in the studio-audit regression test. The clean one-commit head `0f852be7ff6a92e522577201f95cc42958a44115` passed 15 focused entrypoint/audit tests, full repository quality, Fly config validation, all hosted checks, exact-head `tdfrecords` approval, and a Codex review with no findings or threads. Protected merge produced `323b7ba69243e3a2eb193118bc6521dcfe5c1591`; only the two approved files changed and were byte-identical to the reviewed head. Catalog, Datadog, and Safe Install post-merge workflows passed; both follow-up and accidentally recreated #281 remote branches were deleted. No deploy occurred: staging still reports healthy v17 source `714060ffcc1db56448f6d3cb82e055e0085af4e7`, so its live startup flag remains unchanged until a separately authorized future deploy applies the new release-gate configuration. Production was untouched.
- After Diego explicitly continued, the merged release-gate configuration was deployed only to isolated app `tdf-hq-studio-audit-staging`. Fresh pre-deploy snapshots were created for the API volume (`vs_91lMXgA2jawwCj0w4l7RZ`, digest `42b7c3a0ce571ba9d0234526c0208fe53638b4d98409bee1022d4c1afc684760`) and database volume (`vs_a496agR5OG9tMLyAM9m3NkK`, digest `49892605b0d9de22879af874296070afa37f25d4a7fc39ade11dbd7d35feff74`). Fly release v18 (`l8w0PB4QQoOwMTBXMJk9j9B0`) used the immutable #281 merge image: source `7f60f6ec03171a1b6e558bf8247f59e5914ea8f0`, build time `2026-09-09T01:12:01Z`, CI multi-platform digest `sha256:9165e11128a7adfe8794ed1fb8089e1fe05e6c588f84ed89570518913fd7ff9a`, Fly amd64 digest `sha256:32df7500d4868dfe6c66cd04cde67fb8fd5c48204c0f8696b73da1335bda4017`. The ephemeral release-command Machine confirmed all 88 reviewed migrations already applied, passed full schema verification and exited 0 before rollout; ordinary startup kept `AUTO_APPLY_PRODUCTION_MIGRATIONS=false` and `RUN_MIGRATIONS=false`. Health/version and exact CORS checks passed. Read-only SQL reconfirmed unbounded `text NOT NULL`, zero notification NULLs, both synthetic rows exactly once, no allowlist checks, checksums `90a2e34...` / `79422886...` / `e495d71e...`, and 88 ledger rows. Active volume `vol_vdej5owg087momw4` retained 13 assets (~1.5 MiB) and zero evidence rows. PR #287 recorded this evidence in three documentation files; exact head `8006bfd119f775ad11e89c3bee7f4adb0a6b0e6a` passed focused tests (8/8), full repository quality and every hosted gate, Codex completed with no findings, `tdfrecords` approved the exact head, and no review threads existed. Guarded merge produced `effc084630fa2646247fca7798a6f6a73999ff75`; concurrent PR #285 was its first parent, but #287 added exactly the three reviewed files and their blobs were byte-identical to the approved head. Catalog and Datadog post-merge workflows passed and the remote evidence branch was deleted. Production was untouched.
