# Formal audit delivery continuation — 2026-09-20

Operational delivery: pending current-head CI, independent approval, merge and release.
Formal assurance: partial, qualified below; global consistency and whole-system implementation
conformance remain open. No production or mobile release is claimed by this continuation.

## Baseline and scope

The shared checkout remains at `17a33eca11d585d84435af85340beece9b51d14e` with unrelated
uncommitted work. Live main was refreshed to `968006b69`; existing PR #455 head was
`b966595e267c6365830fa5e60abd3e707838ad59`. Reused local continuation `2d6856dfc` in an
isolated worktree, then integrated current main. No other open feature stack is implicitly
approved or imported. PR #453's canonical identity recovery dependency was already incorporated
in that continuation. Main supplies the recovery-dialog fix (#457) and SMTP log fix (#458).

The five confirmed discrepancies and acceptance criteria remain [SYS-D01–D05](README.md).
The actionable review finding on #455 is part of D03/D04's regression delivery: register the
independent Integer oracle in normal `stack test`. The new `TDF.Commerce.PaymentArithmeticSpec`
is imported by `test/Spec.hs` and listed in the Cabal test suite. Its 400 capture/refund and
512 ledger cases retain every previous input; case names now identify failing inputs.
The compatibility probe delegates to the same suite and the duplicate bespoke CI step is removed.
No migration, API shape, provider activation or mobile runtime change is introduced.

## Authority, inventory and claims

[Inventory](inventory.json) indexes the discovered source classes and preserves proposed/inferred
status. [Requirements](requirements.json) links the seven corrective requirements to actual
implementation, verification and explicit limitations. Existing domain specifications remain
canonical; this index does not supersede them. [README](README.md) records the authority conflicts and
four system-level gaps, glossary, component coverage and assumptions.

- SYS-MONEY-001/002: source-derived Z3 bitvector obligations for the recognized actual Haskell
  capture/refund expressions over signed Int64, with nonnegative current/ceiling. Trusted
  recognizer and solver; dispatch preconditions inspected manually. Not a compiler-verified
  whole-program or SQL proof. Positive witnesses and old-code/mutation failures required.
- SYS-MONEY-003: Integer accumulation plus actual Haskell boundary regression. No unrestricted
  persisted-ledger proof; no discovered production caller of this helper.
- SYS-VERIFY-001: exact pinned PlusCal regeneration and mutation rejection. TLC/Alloy cover
  explicit finite configurations, successful witnesses and named negative controls; temporal
  guarantees depend on the fairness documented in each model.
- SYS-VERIFY-002: actual evidence admission function checked over its finite truth table;
  source/tool receipts, all-change PR/main and scheduled triggers. The runner is trusted.
- SYS-RELEASE-001: actual Git ancestry/recovery logic regressions and bounded recovery model;
  cloud execution, credentials, immutable image identity and runtime configuration are assumptions
  until release preflight and deployment establish corresponding observations.

The integrated Alloy scenario supplies finite simultaneous event constraints, not global
composition of all domains. Model passes do not discharge cross-service or application refinement.
The payment library adapter has no discovered current HTTP/worker caller; no live payment outage
or financial loss is inferred from the corrected arithmetic defect.

## Execution and remaining delivery

Fresh receipts are retained under `evidence/continuation-2026-09-20/` and hosted run links will be
recorded there. Historical receipts elsewhere retain their original source identities.
Reproduction commands and pinned tools are in [README](README.md#reproduction-and-contribution)
and [payment correspondence](payment-arithmetic.md).

Production read-only observation: `/version` reports `592916f2a7fbcd740409acda42e69d1a6bfb128a`,
build time `2026-09-18T18:53:50Z`; `/health` reports database/status `ok`. Fly staging app
`tdf-hq-studio-audit-staging` is accessible. These are observations of existing deployment only.
GitHub requires one approving review, dismisses stale reviews and requires resolved conversations.
Existing #455 approval covers its older head, so a changed head requires fresh independent review.
The audit must not self-approve or use admin bypass. No external reviewer message is authorized.

Before production: complete exact-head CI, independent review and protected merge; build the exact
integrated immutable image; validate staging; then use the guarded release lane with compatible
recovery, snapshot, migration/configuration validation and post-deploy health/version evidence.
Mobile build/submission/publication are distinct; this correction changes no mobile source.
Whole-system semantic extraction, cross-domain consistency/refinement and external environmental
obligations remain open and are not erased by successful corrective delivery.

## Fresh verification checkpoint and concurrent integration

At implementation `633f9f906e7cb9d9a0911887fc057ca7ff68906c`, local Z3 safety and admission
obligations returned UNSAT, witnesses and legacy counterexamples SAT, and all three verifier
regressions passed. The registered Hspec oracle passed 912 examples. After installing locked
Node dependencies, the complete local repository gate passed (the initial missing-React run
remains an environment failure, not a pass). Local and hosted TLC/Alloy runs completed with
identical input digest `ab66e6d577374d1c2abeed5f66ec33fafdd341c004bec1b0863f4ac91c364455`.
Hosted synthetic merge `da80b963da8b88f7db4ea123ad750e7ce837c649` and that implementation share
Git tree `8eaf8a64f0dcaa771f6357103d363d2e72029b76`. Hosted runs:
[formal](https://github.com/diegueins680/tdf-app/actions/runs/35551938342),
[social models/persistence](https://github.com/diegueins680/tdf-app/actions/runs/35551938226),
[backend/browser/mobile and integration](https://github.com/diegueins680/tdf-app/actions/runs/35551938225).
Each run's status and revision must be inspected; this list alone does not assert every job passed.

Protected main then advanced to `3d0b43942e3db9597cd159ffa37bf10713069a0b` through PR #451.
It is integrated without changing this audit's arithmetic implementation or formal models.
It adds the reviewed `2026-09-18_records_resource_availability` migration, so the old 113-entry
recovery image in the identity runbook is no longer eligible. The updated release must use a
verified ancestor with the same 114-entry manifest and SQL checksums, such as this main revision
once its Build Image run passes. No migration or recovery guard is bypassed. Regenerated discovery
and fresh hosted checks are required for the resulting integrated tree; the preceding receipts
remain evidence only for their named revision/source digest.

The exact mobile gitlink `2a0e5a99535d9ef199a3e3464a660192f882f72b` is accessible through GitHub
although absent from the shared local object database. Its untruncated 851-entry tree and selected
build/time/release observations are retained in `evidence/continuation-2026-09-20/mobile-inventory.json`.
This closes source discoverability, not native runtime, all-rule extraction or store verification.

## Inventory completeness correction (SYS-D06)

Final artifact inspection found that backend-local prose specifications and OpenAPI fragments
were present but omitted by the root-doc-only discovery predicate. Discovery now includes those
component contracts, component README files, technical root guidance and canonical formal
indices, with regression checks for coverage, privacy exclusions and non-promotion of authority.
This is a necessary scope addition for the requested comprehensive inventory, not new product work.
The legacy escrow document's nominal Payment-row postconditions conflict with accepted ADR 0101;
its payment-evidence rule is superseded, not an authorization to reactivate legacy escrow.
Semantic extraction of all implicit rules remains explicitly open. The final source/revision and
CI receipts are linked from PR #455; older receipts above retain their original boundary.


## Protected integration and explicit escrow decision

PR #455 merged through the protected workflow as `d517d6ed12a0f9ed3929df124507c7f4b025d16d`.
Independent approval covered head `6fbae058e23c989599116c629a80fe216505c1e4`; all 22 checks passed.
The merged tree matches the checked synthetic merge. Final backend CI
[35553494963](https://github.com/diegueins680/tdf-app/actions/runs/35553494963) reported
3511 examples, zero failures and six pending entry points delegated to passing separate runtime
scripts. Post-merge [formal verification](https://github.com/diegueins680/tdf-app/actions/runs/35555279829)
and [social verification](https://github.com/diegueins680/tdf-app/actions/runs/35555279871) succeeded.
The immutable image [build](https://github.com/diegueins680/tdf-app/actions/runs/35555279975) succeeded:
`sha256:18cc762956c97d2530e67a4117d1f27348503a7bba4774c0ec0c0df3fa4a0ef6`.
It was deployed to existing staging after a confirmed database volume snapshot
`vs_AY5zPmV7Ky5hKDyD9DbgwG`. The deployment command completed successfully; this is not a
production release. Vercel deployment 6560713779 and the exact-commit Cloudflare check succeeded.

The user then explicitly resolved SYS-C06: **disable both unverified financial writes**.
SYS-D07 is the justified scope addition. Production rollout is held until the corrective handlers,
regressions, compatible recovery, independent review and fresh exact-image checks are complete.
The previously eligible recovery binaries still contain the nominal writers and must not be used
for a release containing this disablement. See [legacy-escrow.md](legacy-escrow.md) for boundaries,
provenance, machine-checkable correspondence and open obligations. No mobile source change,
financial transaction, migration, historical payment rewrite or production rollout accompanies
this intermediate checkpoint.


### Escrow correction verification checkpoint

Corrective PR [#461](https://github.com/diegueins680/tdf-app/pull/461) adds the explicit user
scope decision. Head `0c34b2c0abe5d2db557ad8ae57db34cddcbfbde7` passed the complete
[PR CI](https://github.com/diegueins680/tdf-app/actions/runs/35558077063), including actual
backend handler regression executions. The separate recovery image run
[35558075245](https://github.com/diegueins680/tdf-app/actions/runs/35558075245) passed backend,
repository, migration, API, mobile and UI checks but failed the WebKit recovery-dialog close check;
no image was produced. This failed run is not release evidence. The same browser check passed in
PR CI; an isolated failure is retained as a limitation, not claimed fixed by the escrow change.

Concurrent protected main `3dd26e0fa8cab6405bc3105078a4ee651d43bbca` adds the approved
single daily event schedule and incomplete-inventory rejection. It is integrated without changing
migration registrations or resetting effective production discovery flags. Synthetic-merge formal
CI correctly rejected stale inventory; discovery is regenerated for the integrated source.
Recovery now checks the exact handler bodies in both target and candidate source blobs, addressing
review feedback about squash/cherry-pick ancestry and also detecting later reintroduction of writes.
Source/guard regressions passed 84 checks locally. Fresh exact-head CI, image build and independent
approval remain mandatory before protected merge and production delivery.


The subsequent integration also includes protected main `5b29fcf68768fce18acbc2424b733459909856d7`
(PR #459, bounded public YouTube metadata validation), with no migration-manifest change. The exact
mobile gitlink is now checked out in the isolated worktree, removing the local source-access gap;
native/store verification remains open. Catalog authority CI identified the new `disabledEscrowWrites`
source-contract array; its two requirement/handler/message bindings are explicitly classified as
technical verification constants, with a specific reviewed decision rather than an audit exemption.
The recovery delivery uses a standard merge commit, preserving a separately tested PR-head image
as a compatible ancestor. Initial squash integrations with no compatible ancestor intentionally
remain blocked; this audit does not relax that release protection.


### Required recovery-dialog gate (SYS-D08)

WebKit dismissal failed again in [731 PR CI](https://github.com/diegueins680/tdf-app/actions/runs/35561739455),
this time in English. The saved trace from the preceding Spanish failure shows the URL's `recover`
parameter removed after click while the modal remains visible beyond the unchanged eight-second
assertion. This is the already documented intermittent recovery issue, not a financial-handler defect.
It is a necessary release-gate dependency. The recovery Dialog now unmounts when closed, avoiding
reliance on the exit transition to remove the modal. A new actual-component regression fails against
the prior implementation and passes after the change; all 11 LoginPage component tests pass. Six
isolated pre-change Mac WebKit runs passed, so they do not reproduce or invalidate the hosted Linux
counterexamples. Fresh browser validation is required. No timeout, retry count, focus assertion,
locale assertion or accessibility rule was weakened. This evidence is testing, not a formal UI proof.

The recovery image for `7314245c17a1413df3d86ff5c0cf6dc78185ac78` was subsequently built successfully
by run35561775443 (the image run's independent browser matrix passed). Index digest:
`sha256:1b482a9dc538c7e0d41198a21fc87e225778e9d79d1614328efbb49e4f702920`;
amd64 digest `sha256:5d1a5570e21feaf72e7cb4dbc0f92d06f05ee3a5dc6872c170220c1cbf4ebd5a`.
Its correction source passed bounded event/social analysis and the source-derived arithmetic checks.
This image is an intermediate staging candidate, not a claim of final merged production delivery.
The attempted local full Stack build was interrupted (exit130) after integration changed its source;
only hosted clean-revision backend checks supply the complete-backend result for this checkpoint.


Review #461 identified a recovery correspondence gap: candidate images could keep unused safe
function definitions while routing a financial endpoint to a different implementation. The source
contract now checks each candidate's marketplace dispatch, API ordering and authenticated parent/root
bindings together with the bodies. Negative controls exercise rerouting each financial operation,
replacing either parent dispatcher and changing/reordering API alternatives. The actual recovery
callback is also checked not to run for a rerouted candidate. The 24 focused route/recovery checks
pass locally; no unrestricted call-graph or whole-program proof is claimed.
